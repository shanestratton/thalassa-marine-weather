project_id = "pcisdplnodrphauixcau"

# ── Edge function JWT verification ────────────────────────────────────────
#
# Whether the platform verifies a JWT before a function runs is otherwise a
# DEPLOY-TIME FLAG (`--no-verify-jwt`), which means it lives in whoever's
# shell history ran the last deploy. That is exactly the kind of unwritten
# state that drifts: a blanket `--no-verify-jwt` across a batch silently
# strips edge auth from functions that had it, and nothing in the repo would
# show it. Declared here instead, so the setting is reviewable and a deploy
# cannot quietly change it.
#
# Anything NOT listed defaults to verify_jwt = true. Verification still permits
# anonymous callers — it only rejects requests carrying no credential at all.
# Measured on production 2026-09-05: proxy-ukho-msi and proxy-linz-msi run with
# verify_jwt = true and answered HTTP 200 to a request whose only credential was
# the sb_publishable_ key. So the ONLY callers a `true` breaks are those that
# send nothing — bare fetch(url), map tile URLs, WebSockets, Apple/Pi/cron POSTs.
#
# RECONCILIATION (audit item 17; 2026-09-05, revised 2026-09-06). 22 functions ran
# verify_jwt=false on production against a `true` here — the flag lived in shell
# history. The plan was to flip production to match this file, until the
# dashboard's own toggle text was read on 2026-09-06:
#
#   "Verify JWT with legacy secret — Requires a JWT signed only by the legacy
#    secret in the Authorization header. The anon key satisfies this.
#    Recommended: OFF with JWT and custom auth logic in your function code."
#
# The check is bound to the LEGACY JWT secret; once a project moves to the new
# asymmetric signing keys, `true` rejects real user tokens. Every drifted
# function already carries its own guard (requireAuthenticatedQuota,
# requireAuthenticatedOrPublicQuota, a per-client public quota, or a
# service-role check) — the "custom auth logic" the platform means. So the drift
# resolved the other way: production stays OFF and this file now says so, with
# the guard named per function. Nothing was toggled in the dashboard.
#
# `true` remains only where the gateway check is wanted as belt-and-braces on an
# irreversible or credential-issuing path AND the function verifies the session
# itself (delete-account, register-apple-token, crew-*, the email workers). If
# the project adopts new JWT signing keys, review those too.

[functions.delete-account]
# Irreversible authenticated mutation. Keep gateway verification explicit in
# source control in addition to the function's own getUser() identity check.
verify_jwt = true

[functions.register-apple-token]
# Accepts a one-time Apple authorization code only after native Supabase auth,
# then stores the resulting refresh token behind the service role. Both the
# gateway and the function independently verify the caller's session.
verify_jwt = true

[functions.apple-server-notification]
# Apple cannot present a Supabase JWT. The receiver accepts no browser
# credential and instead verifies Apple's RS256 JWS, issuer, and exact App-ID
# audience before writing a service-role-only pending event.
verify_jwt = false

[functions.telemetry-relay]
# The Pi's live instrument snapshot. Authenticated by the SAME per-pairing relay
# token as diary-relay (_shared/pi-relay-auth.ts) — the Pi holds no user JWT by
# design, so the gateway must not demand one.
verify_jwt = false

[functions.diary-relay]
# The gateway must NOT demand a JWT here: the Pi relay authenticates with the
# per-pairing relay token (X-Thalassa-Pi-Relay-*) and holds no user JWT by
# design — that is the whole point of the pairing. With the default in force,
# every Pi-originated call (entry relay, cancellation, video upload grants)
# died at the gateway with UNAUTHORIZED_NO_AUTH_HEADER while the phone's
# direct path silently did all the work and masked it. The function's own
# code is the real gate: pair/upsert/cancel authenticate the user JWT
# internally via requireAuthenticatedQuota, and relay ingestion + video
# grants authenticate the relay token against its stored hash.
verify_jwt = false

[functions.voyage-log]
# The public voyage log. src/voyageLogApi.ts:206 fetches this with a bare
# `fetch(url)` — no apikey, no Authorization — because the page is meant to
# work for a shore contact holding nothing but the link. Verification here
# would take the public log offline. The function guards itself instead: it
# resolves the handle and returns only what that vessel has published.
verify_jwt = false

[functions.float-plan]
# Historical public route-plan API. The source is now a 410 tombstone with no
# database access, and must remain reachable without a JWT so old public links
# receive the explicit fail-closed response rather than an ambiguous gateway
# error. Do not restore service-role route lookup for the public beta.
verify_jwt = false

[functions.proxy-himawari-ir]
# Satellite tile proxy. Tile URLs are consumed by the map's raster source,
# which cannot attach an Authorization header, so verification is impossible
# rather than merely undesirable.
verify_jwt = false

[functions.proxy-rainbow]
# Rainbow.ai proxy (Glass radar card + RainForecastCard). Matches deployed
# reality as of 2026-08-04: credentialless requests reach the function (the
# Pi cache passthrough fetches tiles server-side with no Supabase credential
# at all, so platform verification must stay off). The function self-guards
# via requireAuthenticatedOrPublicQuota — real user JWTs get the full
# 240/hr + 4000 tiles/hr buckets; anon-key or credentialless callers land in
# small per-IP public buckets (30/hr snapshots — exhausting THAT is what
# killed the old dashboard radar, which fetched its snapshot with no auth).
verify_jwt = false

# ── Credentialed functions reconciled from the live gateway ───────────────
#
# Each caller below already supplies a user or anon JWT. Pinning `true` adds
# the gateway check without removing the function's own identity/quota guard.

[functions.elevenlabs-tts]
# Signed-in voice callers; the function verifies the session itself via
# requireAuthenticatedQuota. OFF per platform guidance (see header).
verify_jwt = false

[functions.fetch-wind-grid]
# App and Pi-cache callers; per-client public quota inside the function. OFF
# per platform guidance — the Pi's headers no longer matter to this decision.
verify_jwt = false

[functions.fetch-wind-velocity]
# No client caller remains (MapboxVelocityOverlayArchitecture forbids it); the
# function still enforces a per-client public quota. OFF; candidate to retire.
verify_jwt = false

[functions.gebco-depth]
# Phone-only callers, public bathymetry; the function enforces a per-client
# public quota. OFF per platform guidance.
verify_jwt = false

[functions.get-weather]
# App and Pi-cache callers; requireAuthenticatedOrPublicQuota inside the
# function selects the quota. OFF per platform guidance.
verify_jwt = false

[functions.lookup-vessel]
# Signed-in vessel caller; the function verifies the session itself via
# requireAuthenticatedQuota. OFF per platform guidance.
verify_jwt = false

[functions.crew-phone-verification]
# Crew List phone verification carries a phone number or one-time code. The
# gateway verifies the JWT and the function independently resolves that token
# to a current, email-confirmed Supabase user before any Twilio or database work.
verify_jwt = true

[functions.crew-profile-publication]
# A signed-in sailor may request publication, but the function accepts no
# client profile data or verdict. It fetches the canonical private profile and
# exact Storage objects itself, then uses service-role-only snapshot RPCs.
verify_jwt = true

[functions.maritime-intel]
# Public news feed; the function meters the caller itself via
# requireAuthenticatedOrPublicQuota and caches (audit item 16). OFF.
verify_jwt = false

[functions.proxy-amsa-msi]
# Notice-to-mariners feed; per-client public quota inside the function. OFF.
verify_jwt = false

[functions.proxy-gemini]
# Signed-in AI callers; the function verifies the session itself via
# requireAuthenticatedQuota. OFF per platform guidance.
verify_jwt = false

[functions.proxy-nga-msi]
# Notice-to-mariners feed; per-client public quota inside the function. OFF.
verify_jwt = false

[functions.proxy-openmeteo]
# App and Pi-cache callers; per-client public quota inside the function. OFF
# per platform guidance.
verify_jwt = false

[functions.proxy-spoonacular]
# Server-disabled provider (SPOONACULAR_ENABLED) plus a public quota inside
# the function; the client uses credentialed functions.invoke. OFF.
verify_jwt = false

[functions.proxy-stormglass]
# Signed-in weather caller; the function verifies the session itself via
# requireAuthenticatedQuota. OFF per platform guidance.
verify_jwt = false

[functions.proxy-tides]
# App and Pi-cache callers; per-client public quota inside the function. OFF
# per platform guidance.
verify_jwt = false

[functions.route-bathymetric]
# No client caller remains — routing moved client-side (bathymetricRouter);
# the function still enforces a public quota. OFF; candidate to retire.
verify_jwt = false

[functions.route-weather]
# Credentialed route caller; service-role check plus a public quota inside
# the function. OFF per platform guidance.
verify_jwt = false

[functions.founding-skipper-application]
# Public application form. The function enforces exact-origin CORS, a bounded
# body, strict validation, an HMAC-per-IP quota, and a service-role-only RPC.
verify_jwt = false

[functions.founding-skipper-email-worker]
# Privileged queue worker. The gateway verifies the fast wake's public anon JWT
# or cron's legacy service-role JWT; the function then requires either its
# bounded internal worker secret or the verified service_role claim.
verify_jwt = true

[functions.feedback-submission]
# Public bug/feature form. The function enforces exact-origin CORS, a bounded
# body, strict field/kind validation, honeypot discard, an HMAC-per-IP quota,
# and a service-role-only idempotent database transaction.
verify_jwt = false

[functions.feedback-email-worker]
# Privileged durable-outbox worker. The gateway verifies the fast wake's anon
# JWT or cron's legacy service-role JWT; function code then requires the exact
# bounded internal secret or the verified service_role claim.
verify_jwt = true

[functions.check-weather-alerts]
# Cron entry point. pg_cron/pg_net POSTs with the service key and the function
# requires an exact service-role POST before any work; no user JWT exists.
verify_jwt = false

[functions.deepgram-ws-proxy]
# Browser WebSocket upgrade: no Authorization header is possible. The function
# consumes a 60-second one-use ticket minted by deepgram-token (which IS
# JWT-verified) — the ticket is the credential.
verify_jwt = false

[functions.fetch-gfs-tracker]
# CycloneTrackingService calls this with a bare fetch(url) and the function
# enforces a per-client public quota. Public cyclone positions, not sensitive.
verify_jwt = false

[functions.satellite-tile]
# Mapbox raster source URL: the map engine fetches tiles itself and cannot
# attach Authorization. Per-client public quota inside the function.
verify_jwt = false

[functions.send-anchor-alarm]
# Invoked by a database trigger over pg_net with the service key; the function
# requires an exact service-role POST before delivering an alarm.
verify_jwt = false

[functions.send-push]
# Invoked by a database trigger / retry sweep over pg_net with the service key;
# the function requires an exact service-role POST before delivering.
verify_jwt = false

[functions.moderate-chat-message]
# Invoked by the chat_messages AFTER INSERT trigger and the retry sweep over
# pg_net with the service key; the function requires an exact service-role
# POST before any database or Gemini work. No user JWT exists in that path.
verify_jwt = false
