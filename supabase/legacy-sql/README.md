# Legacy SQL

These files predate the Supabase CLI migration convention and were silently
ignored because their filenames do not begin with a migration version.

They are retained only as implementation history. Do not execute them: several
contain destructive or prototype-era security behaviour. Their supported,
idempotent replacements live in `supabase/migrations/`.
