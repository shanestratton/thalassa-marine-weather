// The same real-app journey runs against source in the layout suite and the
// immutable dist bundle here. No fixture-only Vessel markup or backend writes.
import '../browser-tests/vessel-scroll.spec';
