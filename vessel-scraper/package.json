{
    "name": "thalassa-vessel-scraper",
    "version": "1.0.0",
    "description": "The Railroad — vessel metadata scraper for Thalassa. Runs on Railway.app as a cron job (every 15 min).",
    "main": "dist/index.js",
    "scripts": {
        "build": "tsc",
        "start": "node dist/index.js",
        "dev": "ts-node src/index.ts"
    },
    "dependencies": {
        "@supabase/supabase-js": "^2.45.0"
    },
    "devDependencies": {
        "typescript": "^5.5.0",
        "ts-node": "^10.9.0",
        "@types/node": "^20.14.0"
    },
    "engines": {
        "node": ">=20.0.0"
    }
}
