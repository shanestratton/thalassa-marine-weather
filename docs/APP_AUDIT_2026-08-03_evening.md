# Thalassa Full-App Audit — 2026-08-03 evening (commit 35e44f50, post burn-down)

Protocol: identical 11-agent harness as the morning audit (wf_16a221f0-ff0).
Morning baseline: 63.25. June baseline: 56.1. ENC layer excluded (own track, 86.15).

## Scorecard

| Dimension                                        | Score  | Max | Verifier delta  |
| ------------------------------------------------ | ------ | --- | --------------- |
| **ENGINEERING**                                  | **24** |     |                 |
| build                                            | 6.5    | 8   |                 |
| test-health                                      | 6      | 8   |                 |
| architecture                                     | 3.5    | 8   |                 |
| DRY                                              | 1.75   | 6   |                 |
| consistency                                      | 2.75   | 5   |                 |
| Code Quality — security                          | 3.5    | 5   |                 |
| **UI**                                           | **18** |     |                 |
| design-system adherence                          | 3.75   | 8   | raw 4, -0.25    |
| theming                                          | 3      | 7   |                 |
| visual-consistency                               | 5.5    | 8   |                 |
| component-polish                                 | 5.75   | 7   |                 |
| **UX**                                           | **24** |     |                 |
| states: loading/empty/error/offline              | 5      | 6   |                 |
| navigation/IA                                    | 4.5    | 6   | raw 4.25, +0.25 |
| accessibility                                    | 4.25   | 6   |                 |
| marine-safety-UX                                 | 5.5    | 6   |                 |
| UX — performance (experienced jank/latency/boot) | 4.75   | 6   |                 |

**TOTAL: 66 / 100**

## ENGINEERING — deduction ledger

### build — 6.5/8 (pre-verify)

- **−0.75** No pre-merge gate: direct pushes to master regularly turn CI red, and it happened AGAIN today — the safety burn-down push itself broke master's unit tests for ~12 minutes.
    - Evidence: gh run view 30783010387 (branch master, 'fix: harden the safety batch', 2026-08-03T03:55Z) → failed step 'Run unit tests with coverage'; fixed only by follow-up commit 35e44f50 'test: uiStore transition specs updated to the shipped 5-tab IA' (green run 30783521384 at 04:07Z). Prior pattern intact: run 30768650684 (2026-08-02T21:47Z) red on 'Edge function type check', next green 30771194288 at 22:56Z (~70 min red); CI history reliably green only since 07-30 (07-28: 23 failures, 07-29: 18). .github/workflows/ci.yml has no required-review/merge-queue mechanism — push-to-master is the workflow (per repo convention).
- **−0.25** Dependency staleness and vulnerabilities persist: 59 outdated packages, 9 npm-audit vulnerabilities (1 high, 2 moderate), several majors behind; dependabot was only revived this morning and none of its 13 PRs have landed (most fail CI).
    - Evidence: npm outdated → 59 rows; npm audit → '9 vulnerabilities (6 low, 2 moderate, 1 high)' — high = tmp <=0.2.5 (GHSA-52f5-9888-hmc6) via the @lhci/cli dev chain, moderate incl. uuid <11.1.1; .github/dependabot.yml target-branch fixed to 'master' today (comment at line ~12 admits 'main' silently killed every npm PR); gh pr list → 13 open dependabot PRs created 2026-08-03T04:01-04:03 (vite 7.3.6→8.2.0, tailwindcss 3.4.19→4.3.3, jsdom 24→30, @testing-library/react 14→16, 39-package minor-and-patch group), with 10 of 11 corresponding CI runs at 04:0x failing; GitHub Actions bumps (PR #8 actions/checkout 4→7) open since 2026-06-22. Improvement credited vs morning: automation genuinely fixed, brace-expansion high vuln patched (was 10 vulns / 2 high).
- **−0.25** Dead declared dependencies survived today's 'free engineering points' cleanup batch: maplibre-gl and zod are still in package.json with zero imports repo-wide, and no depcheck/knip step exists to catch them.
    - Evidence: package.json:81 '"maplibre-gl": "^5.24.0"' and package.json:94 '"zod": "^3.22.4"'; grep -rn "from 'zod'|from 'maplibre" across all .ts/.tsx (excl. node_modules/.claude) → zero hits; both were flagged in this morning's audit (docs/APP_AUDIT_2026-08-03.md:108) and commit 8c00ee04 ('audit burn-down batch 1 — free engineering points') did not remove them; .github/workflows/ci.yml has 12 steps but no unused-dependency check.
- **−0.25** Peer-dependency resolution is disabled project-wide rather than resolved: .npmrc pins legacy-peer-deps=true and all three CI workflows install with --legacy-peer-deps, permanently masking real peer conflicts (an RC package sits in production deps).
    - Evidence: .npmrc:1 'legacy-peer-deps=true'; .github/workflows/ci.yml:33, lighthouse.yml:31, preview-smoke.yml:36 all 'npm ci --legacy-peer-deps'; package.json dependencies include '@codetrix-studio/capacitor-google-auth': '^3.4.0-rc.4' (release-candidate pin in prod deps alongside Capacitor 8) — the conflict is suppressed everywhere instead of being fixed, so a genuinely incompatible peer bump (e.g. the pending React group PR) installs silently.

### test-health — 6/8 (pre-verify)

- **−1** Absolute coverage is ~25% lines / ~21% branches against the repo's own stated 80% target — roughly three quarters of instrumented product code has zero coverage, and large god-modules remain untested.
    - Evidence: vitest.config.ts:47-56 — thresholds lines:25 / functions:20.5 / branches:20.5 / statements:25, comment 'raised to sit just below the verified full-suite baseline (25.33/20.97/21.00/25.29)' and 'TARGET: 80% across the board'; verified today: grep -rl over tests/ → components/dashboard/HeroSlide.tsx 0 refs, components/DiaryPage.tsx 0 refs, services/ChartLockerService.ts still referenced by a single test file. Unchanged since the morning audit.
- **−0.25** Coverage ratchet is global-only — no per-glob thresholds pin the life-safety directories (MobService, anchor-watch, services/shiplog), so a safety-code coverage regression passes CI as long as the app-wide 25% floor holds.
    - Evidence: vitest.config.ts:46-56 — a single global thresholds block; coverage.include spans services/**, components/**, hooks/**, stores/** etc. with no glob-keyed threshold entries (vitest supports them), so unrelated additions can mask safety-dir regressions.
- **−0.25** The documented race-class flake in segmentRoute.test.ts remains parked with no fix commit — the green-master signal still carries a known 1-in-8 unreliable test.
    - Evidence: docs/ROUTING_COLLAB.md:3156-3161 (reply 60): segmentRoute 'rare single failure under MAX parallel contention… Watching; not touching'; git log --oneline -3 -- tests/routing/segmentRoute.test.ts → a5c89db3, 7ab62e38, ee60af86 — all behavior changes, none a flake fix; file still present at tests/routing/segmentRoute.test.ts.
- **−0.25** The full suite still cannot run unmanaged on the dev machine (OOM without --maxWorkers=2) and the config papers over parallel-contention breakage with a 4x timeout bump — CI is the only place the complete suite actually runs, so pre-push whole-suite verification is impractical.
    - Evidence: vitest.config.ts:9-16 — comment: real-ENC repros 'can blow the 5s default under full-suite parallel CPU contention, surfacing as an intermittent STACK_TRACE_ERROR', testTimeout/hookTimeout raised to 20000; --maxWorkers=2 OOM constraint confirmed as a given; consequence demonstrated today — the burn-down was pushed without a full local run and broke master's unit-test step (run 30783010387).
- **−0.25** Today's safety burn-down shipped ~372 LOC of NEW life-safety code — MOB chart marker, anchor swing ring, app-level anchor alarm gate — with zero accompanying tests; the only test changes since the morning audit are two post-hoc spec updates to re-green existing suites.
    - Evidence: git diff --name-only 6ea47741..HEAD → new/changed components/map/useMobMarker.ts (153 LOC), components/map/useAnchorSwingLayer.ts (183 LOC), components/anchor-watch/GlobalAnchorAlarmGate.tsx (36 LOC); grep -rl 'useMobMarker|useAnchorSwingLayer|GlobalAnchorAlarmGate' across tests/, services/, hooks/ → zero test references (vitest include globs DO cover components/\*\*.test.tsx, so absence is real); only test files in the diff: tests/stores/uiStore.test.ts (+18/-4) and tests/useAppBootstrap.test.ts (+4/-1), both updated after the push broke master.

### architecture — 3.5/8 (pre-verify)

- **−1.75** God-modules are severe and still accreting: MapHub.tsx GREW during today's burn-down (5476 this morning -> 5491 now), the project violates its own declared 500-line component cap in 78 files, and the pieces extracted from MapHub are themselves god-modules.
    - Evidence: components/map/MapHub.tsx = 5491 lines at HEAD (git show 10dadaa4:components/map/MapHub.tsx | wc -l = 5491, vs 6441 at f9e87ddc and 2893 at the 06-21 baseline — decomposition trend is real but current state stands). CONTRIBUTING.md:28 'Max 500 lines — extract sub-components'; 78 .tsx files in components/+pages/ exceed 500, 25 exceed 1000 (components/VesselHub.tsx 2468, components/CrewManagement.tsx 2390, components/music/MusicPage.tsx 2261, components/voice/BosunConsole.tsx 2156). Extracted MapHub pieces: components/map/usePassagePlanner.ts 2575, components/map/useWeatherLayers.ts 2397, components/map/useMapInit.ts 1417. 27 services exceed 1000 lines (services/InshoreRouter.ts 3691, services/shiplog/OfflineQueue.ts 3094, services/DiaryService.ts 2847, services/routeTracer.ts 2568).
- **−1.25** 21 circular-dependency chains outside the certified ENC track (27 total via madge from the app entry), including a service-chain that cycles through the settings store and routing-tier modules that are mutually dependent.
    - Evidence: npx madge --circular --ts-config tsconfig.json index.tsx (861 files) reports 27 cycles; 6 are ENC-internal (EncHazardService/EncCellStore chains, excluded per the certified ENC track), leaving 21 in scope. Examples: services/ShipLogService.ts > services/shiplog/CapturePipeline.ts > services/shiplog/EntrySave.ts > services/shiplog/LiveTrickle.ts > stores/settingsStore.ts (cycle 3); services/inshoreRouterEngine.ts > services/engine/tierPipeline.ts > services/tier3/tier3Router.ts and > services/tier4/tier4Router.ts (cycles 23-24); services/vessel/LocalDatabase.ts > services/ShoppingListService.ts > services/PassageProvisionsService.ts > services/GalleyRecipeService.ts (cycle 1); components/CrewManagement.tsx > components/crew/ReadinessCardStack.tsx (cycle 27).
- **−0.75** Layering is muddled versus the declared UI->Hooks->Services flow: over half of all components import services directly, and a React hook living in services/ imports from the components layer.
    - Evidence: ARCHITECTURE.md system diagram declares UI --> Hooks --> Services, yet 174 of 337 non-test .tsx files in components/+pages/ import from services/ directly (grep "from '../services" variants). services/weather/useGribDownload.ts:13 imports getGribBoundsFromMap from '../../components/map/ThalassaMap' — the services layer reaching up into components. (The store<->service bidirectional coupling is billed once under circular deps above, not re-billed here.)
- **−0.75** State management runs four coexisting paradigms mid-migration: zustand stores, hand-rolled 'Zustand-like' pub/sub stores, deprecated context bridges still consumed by 23 files, and a store-shaped singleton living in services/.
    - Evidence: stores/ mixes zustand (stores/settingsStore.ts, stores/authStore.ts, stores/uiStore.ts...) with hand-rolled stores self-described as 'Zustand-like API without the dependency' (stores/LocationStore.ts, stores/PassageStore.ts, stores/WindStore.ts — grep 'Zustand-like' stores/\*.ts). context/SettingsContext.tsx:16 marks useSettings() '@deprecated Use useSettingsStore() directly' yet 23 non-test files still call useSettings() (grep count at HEAD). context/ holds 8 bridge files while a stray second directory contexts/ holds only CrewCountContext.tsx. services/NmeaStore.ts is a subscriber-model store in the services layer (madge cycle 4: services/NmeaGpsProvider.ts > services/NmeaStore.ts). Mitigating credit already applied: the bridges are clean, documented delegations (context/ThemeContext.tsx:1-21), so the migration is coherent in direction.

### DRY — 1.75/6 (pre-verify)

- **−2** The haversine sprawl is fully intact and grew AGAIN today: 43 files contain the inline haversine formula, 8 competing exported haversines coexist, the canonical utils/navigationCalculations.ts has exactly 2 production importers, and today's burn-down added a fresh private earth-radius copy.
    - Evidence: grep 'Math.sin(dLat' across components/services/stores/hooks/utils = 43 non-test files (e.g. services/MobService.ts:61 EARTH_M=6371008.8, services/AnchorWatchService.ts:153 R=6371000, stores/followRouteStore.ts:67, components/map/ThreatBanner.tsx:73, services/routing/landBackstop.ts:85, services/weather/spitfire.ts:116). Eight competing EXPORTED implementations: utils/navigationCalculations.ts:14 calculateDistance (2 importers), utils/math.ts:120 (5 importers), utils/gpsFollow.ts:32 haversineNM, src/geo.ts:15 haversineNm, services/isochrone/geodesy.ts:12 haversineNm (6 importers), services/engine/geometry.ts:16 haversineM (12 importers), services/shiplog/GpsTrackBuffer.ts:41 haversineMeters. New today: components/map/useAnchorSwingLayer.ts:34 'const EARTH_RADIUS_M = 6371000' landed in commit 10dadaa4 (2026-08-03) — a new private copy added the same day the morning audit flagged the sprawl.
- **−0.75** utils/fetchWithRetry.ts remains dead canonical code — zero production importers — while at least 11 services hand-roll their own retry/backoff loops.
    - Evidence: Repo-wide grep for fetchWithRetry matches only utils/fetchWithRetry.ts (definition) and tests/fetchWithRetry.test.ts:5; utils/index.ts does not re-export it. Hand-rolled attempt-loop retries exist in services/AnchorWatchSyncService.ts, services/ChartLockerService.ts, services/CrewService.ts, services/EncImportService.ts, services/PiCacheService.ts, services/PiProvisionService.ts, services/SeamarkService.ts, services/shiplog/EntrySave.ts, services/voice/deepgramRecognizer.ts, services/weather/ResumableGribFetcher.ts, services/weather/shelter/coastlineSource.ts (grep 'for (let attempt' / maxRetries patterns).
- **−0.5** Five toast implementations still ship: one canonical global system plus four independents each duplicating timer/dismiss/animation/z-index scaffolding.
    - Evidence: components/Toast.tsx (282 lines, self-described 'singleton toast manager... triggered from anywhere') vs components/PushToast.tsx (269 lines, its own parallel global singleton stack with auto-dismiss and exit animation), components/ui/UndoToast.tsx (98 lines, own timer + progress lifecycle), components/dashboard/OffshoreBoundaryToast.tsx (140 lines, own portal + 5s lifecycle + z-[9998]), components/map/PerfDowntierToast.tsx (71 lines, own one-shot show/dismiss). Unchanged from this morning's ledger; the useAppController consolidation credit is already reflected in the deduction size.
- **−0.5** Metres/feet conversion has four separately exported canonical constants plus 25+ inlined 3.28084/0.3048 literals; drift risk is contained only by the parity test on the exported ones.
    - Evidence: Four exported/module constants all defining 3.28084: services/units.ts:19 FEET_PER_METRE, services/GlobalUnitService.ts:187, services/weather/api/marine.ts:72, services/weather/shelter/index.ts:17-18. Raw literals inlined at components/OnboardingWizard.tsx:360-361, components/settings/VesselTab.tsx:230-231, components/AnchorWatchPage.tsx:101, components/DiaryPage.tsx:457, services/weather/transformers.ts:237/242/260/305/320/397/544, services/weather/api/openmeteo.ts:352/442/495, services/shiplog/helpers.ts:277. (The 06-21 drifted-3.281 finding IS fixed — components/map/WeatherInspectPopup.tsx:57 and components/VoyageResults.tsx:159 carry the remediation comments; no deduction for it.)
- **−0.5** Micro-helper duplication is pervasive: 12 formatCoord/formatLat variants (three byte-identical), 5 separate formatDistance definitions, and 22 private toRad copies.
    - Evidence: formatCoord is byte-identical in components/DiaryPage.tsx:48, components/diary/SwipeableDiaryCard.tsx:23 and components/diary/DiaryEntryView.tsx:34, with 9 more variants (components/VesselHub.tsx:1482, components/map/SeamarkPopup.tsx:20, components/vessel/MobPage.tsx:25, components/vessel/RadioConsolePage.tsx:30, components/passage/PassageSummaryCard.tsx:90, components/passage/HazardReportPanel.tsx:74, components/chat/ChatAttachmentSheets.tsx:157, components/vessel/GpxImportPage.tsx:260, services/gpxService.ts:527). Five formatDistance definitions: components/vessel/MobPage.tsx:39, components/anchor-watch/anchorUtils.ts:48, components/passage/HazardReportPanel.tsx:68, services/WeatherRoutingService.ts:368, services/weather/api/dataSourceMerger.ts:53. 22 private 'const toRad =' / 'function toRad(' definitions (grep count), despite utils/navigationCalculations.ts:68 exporting toRadians.

### consistency — 2.75/5 (pre-verify)

- **−0.75** File-naming convention is split roughly 2:1 in services/ and stores/ with no rule — PascalCase and camelCase coexist, including a confusing side-by-side pair for the same routing subsystem.
    - Evidence: services/\*.ts top level: 101 PascalCase vs 55 camelCase files (ls basename count at HEAD) — services/InshoreRouter.ts sits beside services/inshoreRouterEngine.ts; services/ShipLogService.ts beside services/routeTracer.ts and services/gpxService.ts. stores/: PascalCase files (LocationStore.ts, MapFitTargetStore.ts, PassageStore.ts, WindStore.ts) vs camelCase (authStore.ts, settingsStore.ts, uiStore.ts, weatherStore.ts...), where casing exactly tracks the hand-rolled-vs-zustand implementation split.
- **−0.75** File organization is inconsistent: pages/ holds exactly one page while a dozen \*Page.tsx components live under components/; duplicate context/ and contexts/ directories; a second app subtree in src/ with overlapping component names and a competing exported haversine; and a React hook living in services/.
    - Evidence: pages/ contains only LogPage.tsx + log/; meanwhile components/AnchorWatchPage.tsx, components/DiaryPage.tsx, components/GuardianPage.tsx, components/music/MusicPage.tsx, components/vessel/AvNavPage.tsx (and more vessel/\*Page.tsx) are pages living in components/. context/ (8 files) coexists with contexts/ (1 file: CrewCountContext.tsx). src/ is a second entry point (logs.html:41 loads src/logs-main.tsx) whose src/components/ duplicates root-level component names (CompassRose.tsx, WindBarb.tsx, TopNav.tsx) and whose src/geo.ts:15 exports a competing haversineNm. services/weather/useGribDownload.ts is a React hook in the services layer.
- **−0.25** Declared docs misstate the codebase by ~6x and still diagram a deleted feature; CONTRIBUTING's 'avoid as any' rule is violated 65 times. (The 500-line-cap drift is billed once under architecture, not here.)
    - Evidence: ARCHITECTURE.md:8-10 claims '67 components' / '65 Services' vs actual ~420 files in components/ and ~390 in services/; ARCHITECTURE.md:119 'Chat --> Marketplace' still diagrams the Marketplace deleted 2026-07-29 (commit bc065281). CONTRIBUTING.md:35 'Avoid as any' vs 65 occurrences in non-test source (grep over services/components/stores/hooks/utils).
- **−0.5** Error-handling and async idioms vary by module with no shared convention: throw vs return-null-in-catch vs bespoke one-off result shapes, and legacy .then() chains persist alongside async/await.
    - Evidence: 388 'throw new Error' sites in services/ coexist with 78 return-null-in-catch sites (grep counts, non-test) and one-off result shapes like services/VesselFleetService.ts FleetMutationResult used nowhere else as a pattern; hooks/useAppController.ts showToast infers severity by string-matching the message text. 45 non-test service files still use .then() chains alongside the await-dominant style, mixed within the weather API layer.

### Code Quality — security — 3.5/5 (pre-verify)

- **−0.5** OAuth/auth tokens stored in iOS UserDefaults (Capacitor Preferences) and localStorage fallback, not the Keychain — including a Gmail refresh token carrying gmail.send scope; the code itself admits Preferences is 'NOT a secrets vault' and Keychain migration is future work.
    - Evidence: services/voice/integrations/gmail.ts:14-19 ('iOS uses UserDefaults... A future native secure-storage backend should move the refresh token into the Keychain/Keystore'); services/supabase.ts:43-89,205 (capacitorAuthStorage adapter persists the Supabase session via Preferences with localStorage fallback)
- **−0.5** No runtime schema validation at fetch boundaries: zod is a declared dependency but imported zero times anywhere in the repo; 48 `await res.json()` sites in services/ of which 33 are blind `as T` casts, including semi-trusted LAN (Pi-cache) and workers.dev proxy payloads that flow into navigation state.
    - Evidence: package.json:94 ('zod': '^3.22.4', grep for zod imports across the tree returns nothing); services/anchorages/AnchorageService.ts:44 `return (await res.json()) as T;`; services/PiCacheService.ts:760; services/weather/api/unified.ts:406; services/InshoreRouter.ts:2579
- **−0.25** script-src 'unsafe-inline' in both the WKWebView meta CSP and the deployed Vercel header, where the only inline code is two static boot scripts that could be hash-allowlisted — neutering CSP as an XSS backstop for an app with chat/UGC surfaces.
    - Evidence: index.html:23 (script-src 'self' 'unsafe-inline') covering the two static inline scripts at index.html:207-229 and 237-269; vercel.json CSP header repeats 'unsafe-inline'; SECURITY.md:34 documents it as an accepted medium risk rather than hashing
- **−0.25** community_tracks keeps a public FOR SELECT USING (true) policy over recorded voyage tracks — position history — which the project's own July 30 hardening migration flags as 'STILL FOR REVIEW... deserves the same scrutiny guardian_profiles just got', and no later migration resolves it.
    - Evidence: supabase/migrations/20260730120000_close_public_read_on_personal_tables.sql:104-107 (open review item); supabase/migrations/20260303080000_community_tracks.sql public-read policy still effective; latest migration 20260802120000_drop_voyage_summaries_view.sql does not touch it

### Verifier adjustments (engineering)

**Group verdict:** The Engineering group survives adversarial verification intact — zero refutations, zero missed findings, zero double-billing adjustments. I independently re-verified roughly 25 load-bearing claims and every one reproduced exactly: MapHub.tsx is 5491 lines at HEAD; madge from index.tsx reports exactly 27 cycles of which 6 are ENC-internal (cycles 14-19), leaving the claimed 21 in scope; services/ naming splits exactly 101 PascalCase : 55 camelCase; 'as any' count is exactly 65; utils/navigationCalculations.ts has exactly 2 production importers; 43 files carry the inline haversine; the new EARTH_RADIUS_M copy sits at useAnchorSwingLayer.ts:34; fetchWithRetry is imported only by its own test; all five toast files exist; 76+2=78 components/pages exceed the CONTRIBUTING.md:28 500-line cap; ARCHITECTURE.md:8-10 still claims 67 components/65 services against actual 328/385 and line 119 still diagrams the deleted Marketplace; pages/ holds only LogPage.tsx beside duplicate context/+contexts/ dirs; useGribDownload.ts:13 imports from components/. Build/test claims verified live: gh run list confirms run 30783010387 failed at 03:55Z and was re-greened at 04:07Z by 35e44f50; maplibre-gl (package.json:81) and zod (:94) have zero imports repo-wide; .npmrc:1 pins legacy-peer-deps=true and all three workflows install with the flag; dependabot.yml targets master with 13 open PRs; vitest thresholds are 25/20.5/20.5/25 global-only with the 80% target comment; the segmentRoute flake note sits verbatim in ROUTING_COLLAB.md (reply 60, '1-in-8, watching not touching'); the three new safety modules (useMobMarker, useAnchorSwingLayer, GlobalAnchorAlarmGate) have zero test references — only production wiring in App.tsx/MapHub/AnchorWatchPage. Security fix claims are real (anchor anon policies dropped and rebuilt owner-scoped in 20260723090000; community_tracks self-flagged 'STILL FOR REVIEW' in 20260730120000; gmail.ts:14-19 admits the Keychain gap). My independent hunt found no missed material findings: the two dangerouslySetInnerHTML sites are static documented markup, and all six innerHTML sinks — including the AIS vessel popup fed by attacker-broadcastable AIS fields — escape consistently via escapeAisPopupHtml/safeAisImageUrl. Apparent double-billing candidates were examined and cleared: zod appears in both build (dead-dep hygiene, bundled with maplibre-gl at 0.25) and security (unvalidated fetch boundaries standing on 33 blind casts) as distinct defects; today's CI break illustrates both the missing pre-merge gate (build) and the local-suite impracticality (test-health) but each deduction stands on independent evidence; MapHub/500-line drift was explicitly billed once under architecture with consistency deferring. Scores stand as submitted: build 6.5/8, test-health 6/8, architecture 3.5/8, DRY 1.75/6, consistency 2.75/5, security 3.5/5 — group total 24.0/40. The lane's honesty is notable: deduction sizing is calibrated (DRY at 1.75/6 is harsh but the 43-file haversine sprawl growing again on audit day earns it), improvements since the morning audit are credited where real (dependabot revival, anchor RLS hardening, logger consolidation), and no auditor rounded up.

## UI — deduction ledger

### design-system adherence — 4/8 (pre-verify)

- **−1.75** Token bypass rate GREW since the 6-week-old baseline; the token layer exists but adoption collapsed — bypass is the norm and worsening
    - Evidence: Recounted 2026-08-03 at HEAD 35e44f50: `grep -rEoh '#[0-9a-fA-F]{6}\b|#[0-9a-fA-F]{3}\b' components | wc -l` = 1169 (baseline 464; 540 even excluding components/map where Mapbox paint needs literals); `grep -rEoh 'text-\[[0-9]+px\]' components | wc -l` = 1898 (baseline ~1504; 1993 across components+pages+modules+src); 60 arbitrary-hex Tailwind utilities like bg-[#1a1d2e]/text-[#04131d]. Only 21 of 352 .tsx files import theme (`import { t } from '../theme'`, theme.ts:436-437 — its own comment claims 56); t.button tokens (theme.ts:374-383) have ZERO consumers repo-wide vs 1055 raw <button> elements; typography tokens used 12 times vs 1993 arbitrary text sizes; components/VesselHub.tsx:541,576,579 hardcodes palette hex (#ef4444/#cbd5e1/#94a3b8) as icon props instead of token constants.
- **−1.25** Type scale still degenerate and self-violating: --text-micro == --text-label == 11px unchanged, 8-tier scale exists only as a comment, and 394 usages sit below the file's own declared 11px WCAG floor
    - Evidence: index.css:60-64 (`--text-micro: 11px; --text-label: 11px` — two names, one size, baseline finding verbatim); the '8-Tier type scale (standardised)' exists only as a comment block (theme.ts:270-279) — tailwind.config.js:28-30 has `theme: { extend: {} }`, so no fontSize tokens are registered; semantic adoption is 102 uses (80 text-label + 22 text-micro) vs 1423 raw text-[11px]; 321× text-[10px] + 73× text-[9px] violate the comment at index.css:61 ('11px is the WCAG-safe floor'), e.g. components/map/MapHub.tsx:4305 and components/VesselHub.tsx:872,898,927 (text-[9px] labels).
- **−1** Component library is half-real: no Button primitive at all, two dead abstraction layers (Pressable, ui barrel), and ad-hoc overlays outnumber the ModalSheet primitive ~3:1
    - Evidence: components/ui/ has no Button.tsx and t.button has 0 consumers (1055 raw <button> styled ad-hoc); components/ui/Pressable.tsx has zero importers (grep for 'Pressable' outside its own file and ui/index: 0 hits); components/ui/index.ts:1-5 instructs 'Import from here instead of individual files' yet has zero importers (all 105 primitive imports go to individual files); 37 files hand-roll `fixed inset-0 z-` overlay shells vs 14 files using ModalSheet.

### theming — 3/7 (pre-verify)

- **−1** Onshore colour-temperature inversion still skips every gray-\* utility — now 1636 usages (up from baseline 1565) with zero .theme-onshore rules, so the theme's entire premise (warm vs cool) breaks on the app's most-used text colours
    - Evidence: `grep -rEoh '\b(text|bg|border)-gray-[0-9]+' components pages modules src | wc -l` = 1636 (931× text-gray-400, 407× text-gray-500, 197× text-gray-300); `grep -n 'theme-onshore.*gray' index.css` returns nothing — the .theme-onshore block (index.css:135-326) overrides only slate-\* selectors. The theme IS live (ThemeProvider mounted at context/ThalassaContext.tsx:24 applies `theme-${environment}`), so onshore users get warm stone surfaces with cool gray text everywhere.
- **−0.75** Onshore slate override sheet covers only ~17 variants; heavily-used variants escape it, producing a warm/cool patchwork — a structural whack-a-mole across three parallel theming mechanisms
    - Evidence: index.css:220-326 covers bg-slate-950, 900(+/80,/70,/50,/60,/90), 800(+/50,/60,/40), 700, text-slate-300..700, border-slate-700/800 only. Used-but-uncovered (grep counts): bg-slate-900/95 ×31, text-slate-200 ×20, bg-slate-900/40 ×18, bg-slate-800/80 ×12, bg-slate-900/85 ×11, bg-slate-800/90 ×10, bg-slate-600 ×9, text-slate-100 ×7. Any new opacity variant silently escapes both override sheets since tokens (theme.ts), .theme-onshore and .display-light are maintained by hand in parallel; the --tw-slate-\* custom props at index.css:136-168 are dead weight (Tailwind v3 compiles colours inline, as index.css:217-219 itself admits).
- **−0.75** Dark-mode contrast: the muted-text tier fails WCAG AA at normal sizes at scale — text-gray-500 is 4.17:1 on slate-950 and 3.69:1 on slate-900, with 300 instances co-located with 9-12px type
    - Evidence: Computed WCAG ratios: #6b7280 (gray-500) on #020617 (slate-950) = 4.17:1, on #0f172a (slate-900) = 3.69:1 — both below the 4.5:1 AA threshold for normal text; 407 text-gray-500 usages, of which 300 share a line with text-[9-12px]/text-xs (grep 'text-gray-500' | grep -cE 'text-\[(9|10|11)px\]|text-xs|text-\[12px\]' = 300), e.g. components/map/MapHub.tsx:4305 (`text-[9px] leading-snug text-gray-500`); also 104× text-slate-500 at 4.24:1 on slate-950.
- **−0.5** Onshore accent collapse persists in the token file: sky===amber and purple===red, destroying colour-as-information for any onshore token consumer (currently latent — near-zero dynamic accent consumers — but a loaded landmine)
    - Evidence: theme.ts:216-229 — onshore accent.sky is bg-amber-500/10 / text-amber-400 / border-amber-500/\*, byte-identical family to accent.amber; theme.ts:244-249 — onshore accent.purple is bg-red-500/10 / text-red-400, identical family to accent.red (theme.ts:237-243). Mitigation verified: only 3 components consume the dynamic theme (ChatHeader, StatusBadges, HeroSlide via useTheme) and none reference .accent — so today's visible harm is small, hence 0.5 not the baseline's larger hit.
- **−0.5** Light mode still has dark-wash holes: six slate opacity variants in active use have zero .display-light overrides
    - Evidence: grep of index.css display-light rules vs usage counts: bg-slate-900/85 (11 uses), bg-slate-900/40 (18), bg-slate-800/80 (12), bg-slate-950/95 (5), bg-slate-950/85 (4), bg-slate-900/30 (4) — `grep -c "display-light .*bg-slate-900\\\\/85" index.css` = 0 (same for the other five). The burn-down patched the high-frequency washes (index.css:455-459) but these ~54 usages still render dark panels on the light bg.
- **−0.5** Night theming is brightness-dim, not S-52 palette: two uncoordinated uniform scrims, no dusk tier, and the chart's white DEPARE ramp is dimmed rather than re-coloured as a true night colour table would
    - Evidence: App.tsx:1323-1329 (displayMode 'night' = one rgba(69,10,10,0.25) full-screen scrim) and components/map/EncVectorLayer.ts:1453-1456 (ENC dim = #1a0505 at 0.45 DOM overlay) are separate mechanisms with separate persistence — the chart dim hangs off its own toggle (components/map/useTideDepthMode.ts:75, usePersistedState(ENC_NIGHT_DIM_KEY)) rather than the displayMode; S-52 specifies distinct DAY/DUSK/NIGHT colour tables (EncVectorLayer.ts:9 says 'day palette' only) and no dusk tier exists anywhere. Real credit given: this shipped, covers DOM+map, and tests/NightVisionScrimCeiling.test.ts enforces alarms outrank the scrim.

### visual-consistency — 5.5/8 (pre-verify)

- **−0.75** Header treatment drift persists across top-level surfaces: three bespoke header styles ship alongside the PageHeader standard adopted by 25 pages
    - Evidence: components/ui/PageHeader.tsx:63 defines the standard (text-xl font-extrabold uppercase; adopted by pages/LogPage.tsx:1054, components/CrewManagement.tsx:1796, components/DiaryPage.tsx:1303, 25 files total); components/SettingsModal.tsx:474-480 (desktop) and :654-661 (mobile) still use 'text-2xl font-black text-transparent bg-clip-text bg-gradient-to-r from-white to-sky-300' plus a font-mono 'Control Center' subtitle; components/VesselHub.tsx:569 'text-lg font-black tracking-wide' Boat Binder header; components/vessel/GalleyPage.tsx:253 bespoke 'text-base font-black' header (its 36px chevron was fixed to w-11 h-11 at :245, but the header style itself was not unified)
- **−0.75** Empty-state visual language remains split: the sanctioned EmptyState component (15 adopters) coexists with ad-hoc emoji+grey-text blocks on shipped surfaces, all four baseline instances verified unchanged today
    - Evidence: components/ui/EmptyState.tsx:4-5 ('Replaces ad-hoc emoji + grey text patterns', 15 adopting files by grep); yet components/vessel/GalleyPage.tsx:314-320 (<span className="text-5xl">🍽️</span> + 'No Active Meals') and :489-495 (📖 'No Saved Recipes'), components/crew/CrewRoster.tsx:174-181 (👥 'No crew yet'), components/vessel/CastOffPanel.tsx:571-578 (🗺️ 'No draft voyages yet') all still hand-roll the pattern EmptyState was built to replace
- **−0.5** Two typography token systems still each claim single-source-of-truth with conflicting values for the same tier names, splitting consumers into two populations
    - Evidence: styles/typeScale.ts:2 ('Thalassa Type Scale — Single Source of Truth') with :12-18 caption=12px, body=13px, subhead=14px vs theme.ts:270-289 ('8-Tier type scale (standardised)') with caption=11px (theme.ts:289 'text-[11px] text-slate-500'), body=14px — same tier names, different px values; both live: components/Toast.tsx:17 imports FONT/SIZE from typeScale while t.\* consumers read theme.ts, so 'caption' means 12px or 11px depending on which file a component happens to import
- **−0.5** Icon language remains mixed on shipped surfaces: emoji-as-icon persists in Galley tabs/bullets and nine Voyage Log glyphs despite the categorized SVG icon system and a documented emoji-to-SVG migration
    - Evidence: components/vessel/GalleyPage.tsx:282 ('🍳 Active Meals' tab), :299 ('📖 Saved Recipes' tab), 8 emoji occurrences in the file; pages/LogPage.tsx carries 9 emoji glyphs used as UI icons (:1123 📊, :1132 🗺, :1141 📤, :1150 📥, :1158 🔗, :1304 🛣, :1310 ⚡, :1846 ⛵ and ⚙); components/PushToast.tsx:49-51 documents the deliberate 'migrated from emoji' SVG policy that never reached Galley/Log/CrewRoster; components/icons/ provides the categorized SVG set. (Note: the sub-11px type-floor violation is billed once in the design-system lane per the 2026-08-03 verifier's double-billing ruling, so it is deliberately NOT re-deducted here despite text-[9px]/text-[10px] remaining widespread, e.g. RouteTrackPicker.tsx:238 text-[9px].)

### component-polish — 5.75/7 (pre-verify)

- **−0.75** Seven orphaned UI primitives remain in components/ui with zero app importers, one still barrel-exported — unchanged since the morning audit
    - Evidence: grep across components/, pages/, src/, hooks/, App.tsx, ApplicationShell.tsx, viewRegistry.tsx: components/ui/CacheAgeBadge.tsx, Pressable.tsx, FirstRunHint.tsx, DataFreshness.tsx have zero importers (the 'DataFreshness' hits in components/nmea/TheGlassPage.tsx:18 and useNmeaStore.tsx:11 are a type import from services/NmeaStore, not the ui component); GestureTutorial.tsx is imported only by OnboardingTooltips.tsx which itself has zero app importers (both test-only); ListSkeleton.tsx is exported from components/ui/index.ts:15 yet has no app importer — components/ui/ShimmerBlock.tsx:61 defines its own private ListSkeleton rather than importing it. Baseline items confirmed resolved and not billed: ConnectivityBanner deleted, StalenessBanner mounted at Dashboard.tsx:1109, RetryCard mounted at ForecastSheet.tsx:5
- **−0.5** Sub-44pt wet-finger targets persist on the chart surface itself after the burn-down fixed only the five cited instances — including in RouteTrackPicker, shipped two commits ago
    - Evidence: Against the app's own doctrine (components/ui/BackButton.tsx:10-13 / theme.ts:338-352, 44pt 'not negotiable'): components/map/MapHub.tsx:4784 and :4790 route approve/reject buttons are bare '✓'/'✕' glyphs at px-1 (~16-20px tap area), :5298 offline-notice dismiss is w-6 h-6 (24px), :4212-4227 and :4481-4501 chart panel action rows are py-1.5 text-[10px] (~29px tall); components/map/RouteTrackPicker.tsx:159-166 close button is a bare '×' with style fontSize:16/padding:'0 4px' (~24x16px) and :192-197 Retry is px-2.5 py-1 (~26px tall) — all on the chart, the app's most wet-finger surface. The five baseline-cited targets were verified fixed to 44px (GalleyPage.tsx:245, SettingsModal.tsx:639, DiaryEntryView.tsx:115, CastOffPanel.tsx:684-694, LogPage.tsx:1461), hence the reduced deduction vs the morning's 0.75

### Verifier adjustments (ui)

- **-0.25** [design-system adherence] Missed finding: MusicPage's bespoke off-system palette persists verbatim from the morning ledger and was never billed today — a whole shipped page violating theme.ts's explicit brand-palette reservation ('NEVER mix these into UI surfaces', theme.ts:29-33) with cyan brand accents on UI chrome, custom hex backgrounds, and off-scale radii/shadows. This is qualitatively distinct from the count-based token-bypass deduction (which covers the raw hex totals): it breaks a written design-system RULE, not just adoption. Only −0.25 (not the morning's −0.5) because the other half of that morning deduction — VesselHub hex-as-icon-props — is already inside today's 1.75 bypass evidence.

**Group verdict:** Both auditors did reproducible, current-state work: every headline count re-ran to the exact or near-exact figure (1169 hex in components, 1898/1989 text-[Npx], 21/352 theme importers, 0 t.button consumers vs 1063 raw buttons, 1636 gray-_ with zero .theme-onshore gray rules, 394 sub-11px, 37 overlay hand-rollers vs 13-14 ModalSheet users, 300 gray-500/small-text co-locations), the WCAG contrast ratios recomputed independently to the hundredth (4.17:1 on slate-950, 3.69:1 on slate-900), the onshore accent collapse (sky≡amber, purple≡red) is byte-identical in theme.ts:216-249, all six uncovered slate opacity variants confirmed at zero rules in BOTH override sheets, and every spot-checked file:line (SettingsModal gradient headers, VesselHub/Galley bespoke headers, four emoji empty-states, typeScale.ts-vs-theme.ts conflicting tier values, MapHub px-1 glyph approve/reject and w-6 dismiss, RouteTrackPicker ~24px close, all seven ui/ orphans including the DataFreshness type-import and private-ListSkeleton nuances) landed verbatim. Mounted-vs-dead checks pass: the onshore theme is genuinely live (applied at context/ThemeContext.tsx:32 via ThemeProvider — auditor 1's ThalassaContext.tsx:24 citation is the wrong file but right substance), MapHub/GalleyPage/SettingsModal/RouteTrackPicker are all default-path surfaces, and burn-down fixes verified beyond the five cited (StalenessBanner Retry and both RadialHelmMenu Clear All pills now min-h-[44px]), supporting auditor 2's reduced 0.5 touch-target deduction. Unlike this morning's pair, today's auditors split the shared root causes cleanly — gray-_ billed once (theming), the sub-11px floor billed once (design-system, with visual-consistency explicitly abstaining per the morning verifier's ruling), contrast billed once (theming) — so no double-billing needed unwinding; the design-system bypass evidence (hex + font sizes + adoption metrics) and the theming override-sheet gaps cite disjoint utility populations, and the onshore and display-light holes are two independently broken user-facing modes. Nothing was refuted. One miss: the MusicPage brand-reservation/off-scale-radii violation from the morning ledger persists verbatim and went unbilled (−0.25 to design-system). Adjusted totals: design-system 3.75/8, theming 3.0/7, visual-consistency 5.5/8, component-polish 5.75/7 — UI group 18.0/30, up from 17.5 this morning, with the gain fully attributable to verified burn-down polish (44pt fixes, honest loading, mounted banners, night scrim) while the structural adoption story — token bypass growing since June, dueling type systems, hand-maintained theme sheets — is unchanged and correctly graded harshly against the Windy/ORCA/PredictWind bar.

## UX — deduction ledger

### states: loading/empty/error/offline — 5/6 (pre-verify)

- **−0.5** The chart surface still has no cached-data-age surfacing and its weather inspect popup fails silently — unchanged from the morning baseline. Tapping a spot offline/on-failure drops the skeleton and paints the popup with no weather, no error message, and no retry; the purpose-built CacheAgeBadge and DataFreshness components remain orphaned with zero consumers, so a skipper reading GRIB/isobar layers offshore has no way to see how old the chart's weather data is.
    - Evidence: components/map/useWeatherInspectPopup.tsx:245-252 (.catch(() => { inspectLoadingRef.current = false; /_ Drop the skeleton rather than spinning forever _/ view.loading = false; paint(); }) — no error state, no retry); components/ui/CacheAgeBadge.tsx and components/ui/DataFreshness.tsx: grep -rn across components/ pages/ src/ hooks/ App.tsx finds zero importers of either (exit 1 both); components/map/useWeatherLayers.ts:640-705 realigns stale GFS cycles internally (computePressureNowIndex) but surfaces no age indicator to the user
- **−0.25** Offline detection is still split into two contradictory mechanisms on the same surfaces: the burn-down fixed ConnectivityChip/VesselHub/BosunConsole to probe-driven uiStore.isOffline, but the chart's own offline BEHAVIOUR — the offline notice card and the OSM raster fallback base layer — plus the 'Saved locally' OfflineBadge on six vessel forms still key off raw navigator.onLine via useOnlineStatus. On a boat LAN with a dead WAN uplink (the app's own documented normal case) the chip says offline while the fallback base layer never engages and offline-save reassurance never shows.
    - Evidence: hooks/useOnlineStatus.ts:10 (useState(navigator.onLine) + raw online/offline events, no probe); components/map/MapHub.tsx:338 (const isOnline = useOnlineStatus()), :2998 (useOfflineBaseLayer(mapRef, mapReady, isOnline)), :5282 (offline card gated on !isOnline); components/ui/OfflineBadge.tsx:11 (useOnlineStatus) consumed by components/vessel/EquipmentList.tsx, MaintenanceHub.tsx, DocumentsHub.tsx, ChecklistsPage.tsx, InventoryList.tsx, components/diary/DiaryComposeForm.tsx; doctrine at services/internetProbe.ts:5-14 and the probe-driven chip on the SAME chart at components/map/ConnectivityChip.tsx:36-40
- **−0.25** Partial-source failure is silent end-to-end: a marine-source outage returns null by design, the offshore hero renders '--' with no distinction between 'source unreachable' and 'no data here', there is no per-metric retry, and per-source provenance/age was deliberately deleted rather than fixed — so one weather source down degrades the Glass invisibly while the StalenessBanner error tier only fires on whole-report failure. PredictWind/Windy-class competitors show model identity and run age per source.
    - Evidence: services/weather/api/marine.ts:198-243 ('Pi first, Supabase second, null last… Nothing here throws' — every catch falls through to null); components/dashboard/HeroSlide.tsx:1285 (offshore always shows marine widgets with '--' for missing data); components/dashboard/StatusBadges.tsx:292-305 (Data Sources modal 'Removed 2026-04-23… Better to show nothing than to mislead' — provenance props underscore-ignored); services/WeatherOrchestrator.ts:875 (setError only on whole-report catch), :938-951 (premium-only StormGlass fallback, free users none)

### navigation/IA — 4.25/6 (pre-verify)

- **−0.75** Still no back-stack or history integration anywhere — unchanged from baseline: previousView is a single slot, the implemented iOS edge-swipe-back is disabled at its only mount, there is no pushState/popstate integration so browser back exits the web app with no shareable URL state (vs Windy's URL-first web UX), deep links are boot-time one-way only, and Settings still fakes one level of stack via a localStorage return-to key.
    - Evidence: App.tsx:816 (canSwipeBack={false} at the sole PageTransition mount; the gesture exists at components/ui/PageTransition.tsx:120); stores/uiStore.ts:32-33,69 (single previousView slot, set({currentView, previousView})); grep -rn 'popstate|pushState' across App.tsx hooks/ services/ components/ → zero hits (only CaptainsTable's backButtonRef DOM ref); services/deepLink.ts:4-5 ('The SPA has no router — navigation is uiStore.setPage() in-memory state'); viewRegistry.tsx:183 (authScopedStorageKey('thalassa_settings_return_to') workaround)
- **−0.25** The transition state machine is only half-repaired: TAB_PAGES now matches the shipped 5-tab IA, but VESSEL_CHILDREN still hand-lists only 9 of the 21 registered vessel-group views — mob, guardian, radio, galley, crew, checklists, notices, avnav, glass, weatherWindow, skipperReference and gpx-import are all missing, so drilling into or backing out of those pages animates as a flat tab crossfade instead of push/pop, even though viewRegistry exports a derived VESSEL_VIEWS set that uiStore never consumes.
    - Evidence: stores/uiStore.ts:17 (TAB_PAGES = dashboard/map/voyage/details/vessel — fixed by 8c00ee04) vs uiStore.ts:18-28 (VESSEL_CHILDREN = chat/compass/inventory/maintenance/polars/nmea/equipment/documents/diary only); viewRegistry.tsx:267-394 registers weatherWindow(:267), skipperReference(:273), glass(:309), avnav(:315), notices(:321), gpx-import(:327), crew(:352), checklists(:358), guardian(:365), radio(:371), mob(:380), galley(:389) as group:'vessel'; viewRegistry.tsx:401 exports the derived VESSEL_VIEWS set uiStore ignores; uiStore.ts:57-67 falls to default 'tab' direction for any page in neither set
- **−0.25** Still no iOS universal links: the entitlements carry no associated-domains, so boat.thalassawx.app/plan and voyage-log links always open Safari and can never deep-link into the installed native app, despite the web middleware and deepLink service being built for exactly these URLs.
    - Evidence: ios/App/App/App.entitlements contains only aps-environment, com.apple.developer.applesignin, com.apple.developer.weatherkit — no com.apple.developer.associated-domains (file read in full); the web-side routes exist at middleware.ts:1-60 (<handle>.thalassawx.app/plan → planner SPA) and services/deepLink.ts:24-28 (BUILDER_PATHS /plan,/builder)
- **−0.25** Modal stacking still runs two parallel systems: the sanctioned OverlayPortal tier scale coexists with 8 ad-hoc five-digit z-index literals in the map/passage ecosystem — the exact uncoordinated-z pattern that produced the documented night-scrim regression — unchanged from baseline.
    - Evidence: grep -rn 'z-\[10[0-9][0-9][0-9]\]' components/ → 8 hits: components/map/ChartDepthControls.tsx:203 (z-[10060]), components/map/MapHub.tsx:260 (z-[10050]), components/passage/TripLegPicker.tsx:153 (z-[10060]), components/passage/PassageSummaryCard.tsx:1032 (z-[10000]), components/map/TraceReportModal.tsx:299 (z-[10050]), components/passage/PlanOnWebHint.tsx:94 (z-[10070]) plus 2 more; sanctioned scale at components/ui/OverlayPortal.tsx:13-31
- **−0.25** MOB reached the chart (real progress: radial-menu item, chart marker, active-state highlight) but marking a MOB still takes 3 taps from the helm — open the radial FAB, tap MOB, then tap the activate button on MobPage — and the Glass dashboard has no MOB affordance at all (also 3 taps via Vessel hub). Plotter-class competitors (ORCA, dedicated MFDs) keep MOB one action from the primary nav surface; the radial item navigates to a page rather than dropping the mark.
    - Evidence: components/map/RadialHelmMenu.tsx:195-206 (MOB item, action: tacticalState.onOpenMob — inside the FAB fan, which requires a tap to open); components/map/MapHub.tsx:3885 (onOpenMob: () => setPage('mob') — navigates, does not mark); components/vessel/MobPage.tsx:5,274-276 ('Idle: one large red button marks the current GPS fix' — handleActivate bound to the button, activation requires the tap); grep of components/dashboard/, components/SystemStatusButton.tsx → zero MOB entries on the Glass

### accessibility — 4.25/6 (pre-verify)

- **−0.75** No path to enlarge text at all: iOS Dynamic Type is inert on the app's hardcoded px classes, no in-app text-size setting exists, and 394 sub-11px labels violate the app's own declared 11px WCAG floor — including the brand-new safety chips
    - Evidence: grep -rEoh 'text-\[(8|9|10)px\]' components/ pages/ src/ → 321× text-[10px] + 73× text-[9px] (up from 386 at the morning audit); styles/typeScale.ts:8-10 declares the '11px floor for all text (WCAG 1.4.4)'; components/settings/ contains no text-size control (AestheticsTab.tsx offers only Display Mode + Orientation, and grep for fontScale/textScale/fontSizeMultiplier across stores/, services/, components/, hooks/ returns nothing); today's own-ship GPS-age chip and SOG badge ship at font-size: 9px (components/map/useVesselTracker.ts:99,121) and the radial menu's layer labels at text-[10px] (components/map/RadialHelmMenu.tsx:900). A low-vision skipper cannot enlarge 9-10px data labels by any mechanism.
- **−0.5** Onshore theme still collapses sky accent into amber (byte-identical bg/text classes) and remaps purple to red, destroying info-vs-warning and non-danger-vs-danger colour semantics ashore — unfixed baseline finding
    - Evidence: theme.ts:216-222 (onshoreColors.accent.sky: bg-amber-500/10 / text-amber-400) vs theme.ts:223-229 (accent.amber: bg-amber-500/10 / text-amber-400 — bg and text identical, only bgActive /20-vs-/30 and borderActive /40-vs-/60 differ); theme.ts:244-249 maps accent.purple to bg-red-500/10 / text-red-400. Compare offshore purple at theme.ts:170-175 (genuinely purple). Applied app-wide via the theme-onshore environment class.
- **−0.25** Residual sub-44pt touch targets survive on live surfaces after the burn-down fixed only the named set, against the app's own 'not negotiable' 44pt marine doctrine
    - Evidence: components/chat/MealCalendar.tsx:791-793 recipe-picker close button is w-8 h-8 (32px) with no min-h/min-w override; components/music/MusicPage.tsx:2063 and :2087 transport prev/next are h-9 w-9 (36px, while the adjacent play/pause was sized h-11 w-11). Doctrine at components/NavButton.tsx:29 ('Minimum 44×44 touch target for vessel movement/pitching safety') and theme.ts:342-350 ('Every tappable control should clear 44pt'). Contrast with the fixed set: CastOffPanel.tsx:685,694 and GalleyPage.tsx:244 now w-11 h-11, StalenessBanner.tsx:215 and RadialHelmMenu.tsx:949,1089 now min-h-[44px], ChannelList.tsx:204 patched with min-h-[44px] min-w-[44px].
- **−0.25** Sub-AA contrast survives on the alarm screen: the burn-down raised the /40 fine print but left the bearing-to-anchor caption at text-red-400/60 (~3.1:1) — direction-to-safety info on the 3am drag-alarm surface
    - Evidence: components/anchor-watch/AnchorAlarmOverlay.tsx:101-104 renders the bearing line ('{bearing}° {cardinal} to anchor') as text-sm text-red-400/60 font-mono over the radial-gradient(#450a0a→#1c0505→#0a0202) background (AnchorAlarmOverlay.tsx:33): #f87171 at 60% alpha composited over ~#300808 ≈ rgb(168,71,71), contrast ≈ 3.1:1 vs the 4.5:1 AA floor for 14px text. The fixed instance is right below it — AnchorAlarmOverlay.tsx:146-148, now text-red-200/90 with the fix comment — confirming the pass touched this file but stopped one caption short.

### marine-safety-UX — 5.5/6 (pre-verify)

- **−0.25** MOB is still three taps from the helm, not one: the new radial entry lives in the tier-2 Tactical grid behind the category arc, short of the one-press bar ORCA and dedicated plotters set
    - Evidence: components/map/RadialHelmMenu.tsx:199-206 pushes the MOB item into the 'tactical' category's items array (RadialHelmMenu.tsx:290-297), and tier 1 of the radial renders only category bubbles — the keyboard-nav selector at RadialHelmMenu.tsx:699-701 targets '[data-helm-category]' when no category is active, items ('[data-helm-item]') only after a category opens. Path from the chart: layer FAB → Tactical → MOB → MobPage → activate (4th tap; activation deliberately stays on the page per RadialHelmMenu.tsx:53-55). The commit message for 10dadaa4 claims 'MOB item FIRST in the fan', but it is first in the tier-2 grid, not the fan. Genuine improvement over the baseline OBS→Vessel→MOB→Mark (and useMobMarker now plots the datum: MapHub.tsx:2572), so only the one-action residual is billed.
- **−0.25** Alarm escalation lacks iOS critical alerts — with the phone locked, mute switch on and iOS having reclaimed the app, the 20×30s local-notification fallback plays no sound; the code itself flags the entitlement as pending
    - Evidence: services/AnchorWatchService.ts:41-45 ('(future) a critical-alerts variant — pending Apple entitlement') and :906-917 (local-notification fallback '20 repeats at 30 s intervals' with 'Future: switch interruptionLevel to critical once Apple' — standard notification sounds are silenced by the ringer switch). The mute-bypassing path (ios/App/App/AlarmAudioPlugin.swift:7 'bypass the iOS silent/mute switch', :213 raises system volume to max) requires the JS runtime to be alive; AnchorWatchService.ts:893 concedes backgrounded audio is 'unreliable — that's what the local notification below is for'. Dedicated anchor-alarm competitors ship critical alerts; the layered design is honest but the last rung of the escalation ladder is missing.

### UX — performance (experienced jank/latency/boot) — 4.75/6 (pre-verify)

- **−0.5** Underway Glass tick re-renders the entire app shell every 3s: App root subscribes to LocationStore, whose notify() always publishes a fresh {...state} snapshot with a new timestamp, and Dashboard's React.memo is defeated by inline closures at its only call site — unchanged since this morning's audit.
    - Evidence: App.tsx:402 (`const locationStore = useLocationStore()` at App root); stores/LocationStore.ts:55-63 (notify() builds `const snapshot = { ...state }` per emission) and :144-155 (setFromGPS always stamps `timestamp: Date.now()`), :184-189 (useLocationStore setState with that fresh object — React can never bail); hooks/useLiveLocationName.ts:46-47 (POLL_INTERVAL_MS = 3_000, MIN_MOVEMENT_M = 25) with :122/:131 calling LocationStore.setFromGPS each pass while moving; components/Dashboard.tsx:109 (React.memo) nullified by App.tsx:927-932 (`onOpenMap={() => {...}}`, `onTriggerUpgrade={() => setIsUpgradeOpen(true)}` inline closures recreated every App render). Burn-down commits f095e54c..35e44f50 touched none of these lines.
- **−0.25** MapHub is still an unmemoized 5,491-line monolith (64 useState / 40 useEffect) whose entire body re-executes on every internal setState — including its own LocationStore subscription firing a fresh snapshot every 3s underway — while GL layers animate; the leg-grading jank batch already proved this render path produces user-visible stalls.
    - Evidence: components/map/MapHub.tsx:312 (`export const MapHub: React.FC<MapHubProps> = ({` — no React.memo; wc -l = 5491, grep -c useState = 64, useEffect = 40, both up slightly from this morning's 5476/63/39); MapHub.tsx:1597 (`const location = useLocationStore()` — fresh object every notify per stores/LocationStore.ts:55-63). Decomposition acknowledged in-progress but not landed.
- **−0.25** Boot bridge chatter unfixed: the Supabase auth storage adapter still has no in-memory read cache, so every getSession() reads through Preferences.get over the native bridge — ~40 identical same-key round-trips in today's device boot log — behind getCurrentUserId, which is explicitly documented for hot paths.
    - Evidence: services/supabase.ts:43-59 (capacitorAuthStorage.getItem: awaits authStorageQueue then `Preferences.get({ key })` on every call, no memo layer — byte-identical to this morning's cited code); services/supabase.ts:229-240 (getCurrentUserId — 'Use this in hot paths' — each call runs supabase.auth.getSession(), which reads through the adapter); device boot log (verified this morning, no adapter change since) shows ~40 same-key reads. A write-through cache in the adapter remains a one-line fix.
- **−0.25** Boot-critical bundle leak unfixed in today's build: ShipLogService (243KB) is statically welded into the ApplicationShell boot chunk via SystemStatusButton, nullifying App.tsx's deliberate dynamic-import deferral, while vite.config.ts still suppresses the exact MIXED_IMPORTS warning class that would surface the defeat.
    - Evidence: components/SystemStatusButton.tsx:15 (`import { ShipLogService } from '../services/ShipLogService'`) with SystemStatusButton statically imported at App.tsx:24; App.tsx:346 (`void import('./services/ShipLogService')` — the intended deferral, still moot); dist/assets/ApplicationShell-CNzNveYB.js (today's 14:38 build, 582KB) contains static `from"./ShipLogService-Cg-R7R-_.js"` (243,006 bytes on disk); vite.config.ts:367-368 onwarn returns on MIXED_IMPORTS / 'dynamic import will not move module'.

### Verifier adjustments (ux)

- **+0.25** [navigation/IA] Duplicate billing of the same residual across two lanes: auditor 1 (nav/IA) and auditor 2 (marine-safety-UX) each deducted 0.25 for MOB not being one press from the helm. It is one defect and marine-safety-UX is where it belongs — and auditor 2's evidence is the accurate one (MOB sits in the tier-2 Tactical grid behind the category arc: RadialHelmMenu.tsx:199-206 item pushed into the 'tactical' items array, tier-1 keyboard/drag selectors target '[data-helm-category]' only when no category is active at :699-701, so the path is FAB → Tactical → MOB → activate). Auditor 1's nav/IA evidence was also factually imprecise, claiming the MOB item is directly in the fan (FAB → MOB = 3 taps) — the in-code comment 'MOB first, always' refers to ordering within the Tactical category, not the tier-1 fan. Restore 0.25 to navigation/IA; the charge stands once, in marine-safety-UX.

**Group verdict:** The three UX auditors' work is solid and current: every deduction I opened survived at HEAD (now 71a26363, one diary-pin commit past 35e44f50, touching none of the cited files). Confirmed verbatim: canSwipeBack={false} at App.tsx:816 with zero popstate/pushState anywhere; VESSEL_CHILDREN still hand-lists 9 views against 21 group:'vessel' registrations in viewRegistry.tsx while the derived VESSEL_VIEWS export goes unconsumed by uiStore; App.entitlements has no associated-domains; exactly 8 five-digit z-index literals; the weather-inspect catch silently drops the skeleton (useWeatherInspectPopup.tsx:245-252) and CacheAgeBadge has zero importers (the DataFreshness grep hits are an unrelated NmeaStore type, not the orphaned UI component — auditor 1's claim still holds); MapHub.tsx:338/:2998/:5282 still key the offline card and OSM fallback off raw navigator.onLine while ConnectivityChip.tsx:36-40 reads the probe on the same surface; theme.ts onshore sky and amber are byte-identical in bg/text with purple mapped to red; the alarm-screen bearing caption remains text-red-400/60 directly above the corrected text-red-200/90 line; the new GPS-age/SOG chips ship at font-size:9px against a grep-verified 321×text-[10px] + 73×text-[9px] with no app-wide scaling mechanism (the sole Dynamic Type implementation lives in the ENC popup, on ENC's own track); MealCalendar 32px and MusicPage 36px targets confirmed; LocationStore's notify() spreads a fresh snapshot with Date.now() stamps into an App-root subscription defeated further by inline closures at Dashboard's call site; MapHub is 5,491 lines, unmemoized, 64 useStates; capacitorAuthStorage.getItem has no memo layer; and today's ApplicationShell-BkjFetnr.js chunk contains a static from\"./ShipLogService-LX4Y7SY7.js\" (243KB) alongside the moot dynamic imports, with the MIXED_IMPORTS suppression still in vite.config.ts. Positive burn-down claims also verified (StalenessBanner mount receives stale/staleAgeMinutes/error at Dashboard.tsx:1109, probe-driven chip, critical-alerts gap self-acknowledged at AnchorWatchService.ts:41-45/:917). The single correction is a cross-lane duplicate: MOB-not-one-press was billed 0.25 in both navigation/IA and marine-safety-UX for the same residual, and the nav/IA copy carried the weaker evidence (it undercounted the path — MOB is tier-2 behind the Tactical category, making auditor 2's account authoritative); I restore 0.25 to navigation/IA and keep the charge once in marine-safety-UX. My journey walk (Glass → weather → chart → arm anchor watch) surfaced no unbilled defect clearing the deduction bar, and nothing billed here intrudes on the ENC layer's certified track. Adjusted group total: states 5.0 + navigation/IA 4.5 + accessibility 4.25 + marine-safety-UX 5.5 + performance 4.75 = 24.0/30.
