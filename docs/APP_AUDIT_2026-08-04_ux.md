# Thalassa UX Re-score — 2026-08-04 (HEAD 0c3f0507)

UX group only; prior UX 24.0 (2026-08-03 evening). Composite with latest group scores:
Engineering 25.5/40 + UI 18.75/30 + UX 24.25/30 = **68.5/100**.

| Dimension                                           | Score | Max |
| --------------------------------------------------- | ----- | --- |
| states: loading/empty/error/offline                 | 5     | 6   |
| navigation/IA                                       | 4.5   | 6   |
| accessibility                                       | 4.5   | 6   |
| marine-safety-UX                                    | 5.5   | 6   |
| UX — performance as experienced (jank/latency/boot) | 4.75  | 6   |

**UX TOTAL: 24.25/30**

### states: loading/empty/error/offline — 5/6 (pre-verify)

- **−0.5** The chart surface still has no cached-data-age surfacing and its weather-inspect popup fails silently: tapping a spot offline/on-failure drops the skeleton and paints the popup with no weather, no error message, and no retry, while the purpose-built CacheAgeBadge and DataFreshness components remain orphaned with zero consumers — a skipper reading GRIB/isobar layers offshore has no way to see how old the chart's weather data is. Unchanged from the evening baseline; no commit since 35e44f50 touched any of these files.
    - Evidence: components/map/useWeatherInspectPopup.tsx:245-252 (.catch(() => { inspectLoadingRef.current = false; view.loading = false; paint(); }) — 'Drop the skeleton rather than spinning forever', no error state, no retry); grep -rn 'CacheAgeBadge|DataFreshness' across components/ pages/ src/ hooks/ App.tsx → zero importers of components/ui/CacheAgeBadge.tsx or components/ui/DataFreshness.tsx (only hits are the unrelated NmeaStore DataFreshness type at components/nmea/useNmeaStore.tsx:11 and components/nmea/TheGlassPage.tsx:18)
- **−0.25** Offline detection is still split into two contradictory mechanisms on the same surfaces: the chart's offline BEHAVIOUR (offline notice card + OSM raster fallback) and the 'Saved locally' OfflineBadge on six vessel forms key off raw navigator.onLine via useOnlineStatus, while the same chart's ConnectivityChip and the Glass StalenessBanner read probe-driven uiStore.isOffline — and even the Glass's own rain-refresh timers gate on raw navigator.onLine two hundred lines above the probe-driven flag. On a boat LAN with a dead WAN uplink (the app's own documented normal case) the chip says offline while the fallback base layer never engages.
    - Evidence: components/map/MapHub.tsx:323 (const isOnline = useOnlineStatus()), :2883 (useOfflineBaseLayer(mapRef, mapReady, isOnline)), :4903 (offline card gated on !isOnline); hooks/useOnlineStatus.ts:10 (useState(navigator.onLine), no probe); components/ui/OfflineBadge.tsx:11 consumed by EquipmentList/MaintenanceHub/DocumentsHub/ChecklistsPage/InventoryList/DiaryComposeForm; components/Dashboard.tsx:333 and :384 (rain timers 'if (!navigator.onLine) return') vs Dashboard.tsx:149 (probe-driven useUIStore isOffline on the same page); doctrine at services/internetProbe.ts:5-21; probe-driven counter-examples components/map/ConnectivityChip.tsx:36-40, services/internetProbe.ts:107-110
- **−0.25** Partial-source failure is still silent end-to-end: a marine-source outage returns null by design through every catch, the offshore hero renders '--' with no distinction between 'source unreachable' and 'no data here', per-source provenance/age was deliberately deleted rather than fixed, and setError fires only on whole-report failure — one weather source down degrades the Glass invisibly while PredictWind/Windy-class competitors show model identity and run age per source.
    - Evidence: services/weather/api/marine.ts:196-226 (fetchMarine: every catch falls through, ':212 if (!base) return null', ':226 return null'); components/dashboard/HeroSlide.tsx:802-819 (eight '--' fallbacks for missing marine metrics); components/dashboard/StatusBadges.tsx:292-305 ('Removed 2026-04-23: Data Sources modal… provenance it displayed was frequently incorrect' — props underscore-ignored); services/WeatherOrchestrator.ts:875 (setError only in the whole-report catch)

### navigation/IA — 4.5/6 (pre-verify)

- **−0.75** Still no back-stack or history integration anywhere: previousView is a single slot, the implemented iOS edge-swipe-back is disabled at its only mount, zero pushState/popstate integration so browser back exits the web app with no shareable URL state (vs Windy's URL-first web UX), deep links remain boot-time one-way, and Settings still fakes one level of stack via a localStorage return-to key.
    - Evidence: App.tsx:816 (canSwipeBack={false} at the sole PageTransition mount; gesture exists at components/ui/PageTransition.tsx:120, default false at :38); stores/uiStore.ts:69 (set({currentView, previousView}) — single slot); grep -rn 'popstate|pushState' App.tsx hooks/ services/ components/ stores/ → zero hits; services/deepLink.ts:4-5 ('The SPA has no router — navigation is uiStore.setPage() in-memory state'); viewRegistry.tsx:183 (authScopedStorageKey('thalassa_settings_return_to'))
- **−0.25** The transition state machine remains half-repaired: TAB_PAGES correctly matches the shipped 5-tab IA, but VESSEL_CHILDREN still hand-lists only 9 views against 22 group:'vessel' registrations at HEAD — mob, guardian, radio, galley, crew, checklists, notices, avnav, glass, weatherWindow, skipperReference and gpx-import all animate as a flat tab crossfade instead of push/pop, even though the derived VESSEL_VIEWS set is now consumed by App.tsx for isVesselView while uiStore's direction logic still ignores it.
    - Evidence: stores/uiStore.ts:17 (TAB_PAGES = dashboard/map/voyage/details/vessel — verified fixed), :18-28 (VESSEL_CHILDREN = chat/compass/inventory/maintenance/polars/nmea/equipment/documents/diary only), :57-67 (falls to default 'tab' direction); viewRegistry.tsx: grep -c "group: 'vessel'" = 22, :401 exports VESSEL_VIEWS; App.tsx:36,94 imports/uses VESSEL_VIEWS but stores/uiStore.ts contains no reference to it
- **−0.25** Still no iOS universal links: the entitlements carry no associated-domains, so boat.thalassawx.app/plan and voyage-log links always open Safari and can never deep-link into the installed native app, despite the web middleware and deepLink service being built for exactly these URLs.
    - Evidence: ios/App/App/App.entitlements read in full at HEAD: only aps-environment, com.apple.developer.applesignin, com.apple.developer.weatherkit — no com.apple.developer.associated-domains; web routes exist at services/deepLink.ts:4-10 and middleware.ts
- **−0.25** Modal stacking still runs two parallel systems: 8 ad-hoc five-digit z-[10xxx] literals in the map/passage/planner ecosystem coexist with OverlayPortal's sanctioned tier scale (modal 1100 / nested 1200 / critical 2147483000) — the exact uncoordinated-z pattern that produced the documented night-scrim regression, unchanged in count since the evening score.
    - Evidence: grep -rn 'z-\[10[0-9][0-9][0-9]\]' components/ → 8 hits at HEAD: RoutePlanner.tsx:1119 (z-[10060]), ui/OnboardingOverlay.tsx:170 (z-[10000]), map/ChartDepthControls.tsx:203 (z-[10060]), map/MapHub.tsx:260 (z-[10050]), map/TraceReportModal.tsx:299 (z-[10050]), passage/TripLegPicker.tsx:153 (z-[10060]), passage/PassageSummaryCard.tsx:1032 (z-[10000]), passage/PlanOnWebHint.tsx:94 (z-[10070]); sanctioned scale at components/ui/OverlayPortal.tsx:13-31

### accessibility — 4.5/6 (pre-verify)

- **−0.75** Still no path to enlarge text: iOS Dynamic Type is inert on hardcoded px classes, no in-app text-size setting exists, and 403 sub-11px labels violate the app's own declared 11px WCAG floor — with the brand-new float-plan safety sheet shipping fresh 10px labels
    - Evidence: grep -rEoh 'text-\[(8|9|10|10.5)px\]' components/ pages/ src/ modules/ at HEAD 0c3f0507 = 325x text-[10px] + 73x text-[9px] + 5x text-[10.5px] (unchanged from the evening's 403 despite the label-token 11→12px move in c385eff3, which only touched theme.ts typography); styles/typeScale.ts:8-10 declares the '11px floor for all text (WCAG 1.4.4)'; the chart's own-ship GPS-age and SOG chips remain font-size: 9px (components/map/useVesselTracker.ts:87,109); grep for fontScale/textScale/fontSizeMultiplier/Dynamic Type across stores/ services/ components/ hooks/ App.tsx hits only the ENC popup (components/map/encPopup.ts:697 — ENC's own certified track); the NEW FloatPlanSheet ships its 'Raise the alarm if not heard from by' label and helper text at text-[10px] (components/vessel/FloatPlanSheet.tsx:139,153,239) — new safety surfaces still land under the floor
- **−0.25** Residual sub-44pt touch targets survive the burn-down on live surfaces, and the new float-plan sheet ships its primary share actions at ~37px against the app's 'not negotiable' 44pt marine doctrine
    - Evidence: components/chat/MealCalendar.tsx:793 recipe-picker close still w-8 h-8 (32px, no min override); components/music/MusicPage.tsx:2064 and :2088 transport prev/next still h-9 w-9 (36px) after the c25b662e redesign, beside h-11 w-11 play/pause at :2072/:2080; NEW: components/vessel/FloatPlanSheet.tsx:251-264 copy/share buttons are py-2.5 text-xs (~37px tall) and the collapsible preview summary is py-2 text-[10px] (:239) — on the float-plan safety surface; doctrine at components/NavButton.tsx:29 and theme.ts 'Every tappable control should clear 44pt'. Verified the named fixes did land and hold: StalenessBanner.tsx:215 and RadialHelmMenu.tsx:949,1089 min-h-[44px]; ConfirmDialog.tsx:125 min-h-[44px]; GalleyPage now on PageHeader (0c3f0507)
- **−0.25** Sub-AA contrast survives on the 3am drag-alarm screen: the bearing-to-anchor caption — direction-to-safety information — is still text-red-400/60 (~3.1:1), one line above the corrected instances
    - Evidence: components/anchor-watch/AnchorAlarmOverlay.tsx:101-104 renders '{bearing}° {cardinal} to anchor' as text-sm text-red-400/60 font-mono over the radial-gradient(#450a0a→#1c0505→#0a0202) alarm background — ~3.1:1 vs the 4.5:1 AA floor for 14px text; the fix pass demonstrably touched this file and stopped short again: :84 and :148 are text-red-200/90 with the fix comment ('was text-red-400/40 (~1.9:1…)') while :101 is untouched at HEAD 0c3f0507
- **−0.25** Bulk-stamped generic aria-labels override the visible text of semantically different controls for VoiceOver — option pickers announce every choice identically, so a screen-reader user cannot select an experience level or preference at all
    - Evidence: aria-label='Edit item details' appears 10x across 5 files, stamped onto controls that are not edit buttons: components/crew-finder/CrewProfileForm.tsx:369 (experience-level picker — every EXPERIENCE_LEVELS option announces 'Edit item details', masking its visible label since aria-label supersedes inner text), :437 (clear-date button), :573,:593,:613 (smoking/drinking/other preference option grids), plus components/vessel/MaintenanceHub.tsx:627, components/vessel/checklists/SwipeableItemCard.tsx:161, components/vessel/maintenance/ServiceLogSheet.tsx:123, components/diary/DiaryEntryView.tsx:131; same pattern: aria-label='Proceed to next step' x7 (onboarding steps), 'Export data' x4 (MaintenanceHub.tsx:572,595,923,955 — indistinguishable export variants), 'Delete this item' x4. The morning's billed instance of this exact pattern WAS fixed (AestheticsTab.tsx:104-105 now per-option aria-label + aria-pressed) but the stamp survives everywhere else; none of these carry aria-pressed state

### marine-safety-UX — 5.5/6 (pre-verify)

- **−0.25** MOB is still three taps plus an activation tap from the helm, not one: the radial entry remains in the tier-2 Tactical grid behind the category arc, and it navigates to a page rather than dropping the mark — short of the one-press bar ORCA and dedicated plotters set
    - Evidence: components/map/RadialHelmMenu.tsx:199-206 pushes the MOB item into the 'tactical' category items array (the in-code comment 'MOB first, always' at :196-198 governs ordering within that tier-2 grid); tier 1 renders category bubbles only; MapHub wires onOpenMob to setPage('mob') — navigation, not marking — and activation deliberately stays on MobPage (components/vessel/MobPage.tsx:274-277 handleActivate on the 224px button; RadialHelmMenu.tsx:53-55 documents the choice). Path: FAB → Tactical → MOB → activate. Unchanged from the evening's verified account at HEAD 0c3f0507; the surrounding affordances (chart datum marker, live chip with '· fix old' stale tag at useMobMarker.ts:118-121, Watch-armed activation) all verified real
- **−0.25** Alarm escalation still lacks iOS critical alerts — phone locked, mute switch on, and iOS having reclaimed the app leaves the 20x30s local-notification fallback silent; the code itself flags the entitlement as pending
    - Evidence: services/AnchorWatchService.ts:46 ('(future) a critical-alerts variant — pending Apple entitlement') and :902-910 (fallback ships interruptionLevel 'timeSensitive', which 'breaks through Focus' but not the ringer switch; 'Future: switch interruptionLevel to critical once Apple grants the com.apple.developer.usernotifications.critical-' — the mute-bypassing AlarmAudioPlugin path needs a live JS runtime, conceded 'unreliable' when backgrounded); ios/App/App/App.entitlements still carries no critical-alerts key at HEAD. Dedicated anchor-alarm competitors ship critical alerts; the layered design is honest but the last rung is still missing

### UX — performance as experienced (jank/latency/boot) — 4.75/6 (pre-verify)

- **−0.5** Underway, a periodic whole-app-shell re-render persists: App root subscribes to LocationStore, whose notify() always publishes a fresh {...state} snapshot with a new Date.now() timestamp, and Dashboard's React.memo is defeated by inline closures at its only call site — unchanged since the evening audit. Honest cadence note: the 25m movement gate makes the real-world rate ~6–10s at 5–8kn cruising speed (3s is the ceiling at higher speed), each fire also carrying a reverse-geocode network hit; the mechanism and cost are otherwise exactly as previously billed.
    - Evidence: App.tsx:402 (const locationStore = useLocationStore() at App root); stores/LocationStore.ts:54-63 (notify() builds const snapshot = { ...state } per emission — React can never bail) and :141-151 (setFromGPS always stamps timestamp: Date.now()); hooks/useLiveLocationName.ts:48-49 (POLL_INTERVAL_MS=3_000, MIN_MOVEMENT_M=25), :103-105 (25m gate), :114/:123 (LocationStore.setFromGPS per qualifying pass); App.tsx:927-932 (onOpenMap={() => {...}}, onTriggerUpgrade={() => setIsUpgradeOpen(true)} inline closures nullifying Dashboard.tsx React.memo). git log 35e44f50..HEAD touched none of these lines.
- **−0.25** MapHub is still an unmemoized monolith whose entire body re-executes on every internal setState — including its own LocationStore subscription firing a fresh snapshot on the same underway cadence while GL layers animate. Round-1 decomposition (78470910, 5,487→5,112 lines) was five pure moves and honestly changed nothing about the setState blast radius: the four extracted hooks (useMapFitRequest/useConsensusMatrix/usePinViewMode/useMapHubLayerVisibility) are still called inside MapHub, so their state still re-renders the full body; buildTacticalState is now a pure function but is invoked inline per render at the JSX site, handing a fresh 24-field object to an also-unmemoized RadialHelmMenu (per-call CPU is trivial — the monolith re-render surface is the cost, and it stands).
    - Evidence: components/map/MapHub.tsx:297 (export const MapHub: React.FC<MapHubProps> — no React.memo; wc -l = 5,112, useState = 54, useEffect = 31); MapHub.tsx:1579 (const location = useLocationStore() — fresh object every notify per stores/LocationStore.ts:54-63); MapHub.tsx:3493 (tacticalState={buildTacticalState({...})} inline per render); components/map/RadialHelmMenu.tsx:477 (unmemoized); commit 78470910 ('five pure moves… hook call positions/order audited' — extraction, not isolation).
- **−0.25** Boot bridge chatter unfixed: the Supabase auth storage adapter still has no in-memory read cache, so every getSession() reads through Preferences.get over the native bridge — the ~40 identical same-key round-trips in the device boot log remain — behind getCurrentUserId, which is explicitly documented for hot paths. Still a one-line write-through cache away.
    - Evidence: services/supabase.ts:43-53 (capacitorAuthStorage.getItem: awaits authStorageQueue then Preferences.get({ key }) on every call, no memo layer — byte-identical to the evening audit's cited code); services/supabase.ts:229-240 (getCurrentUserId — 'Use this in hot paths' — each call runs supabase.auth.getSession(), which reads through the adapter); git log 35e44f50..HEAD -- services/supabase.ts → no commits.
- **−0.25** Boot-critical bundle leak unfixed: ShipLogService (~243KB chunk at last verified build) is statically welded into the boot graph via SystemStatusButton's top-level import, nullifying App.tsx's deliberate dynamic-import deferral, while vite.config.ts still suppresses the exact MIXED_IMPORTS warning class that would surface the defeat.
    - Evidence: components/SystemStatusButton.tsx:15 (import { ShipLogService } from '../services/ShipLogService') with SystemStatusButton statically imported at App.tsx:24 and mounted in the shell at App.tsx:719/:1081; App.tsx:346 (void import('./services/ShipLogService') — the intended deferral, still moot); vite.config.ts:367-368 (onwarn returns on MIXED_IMPORTS / 'dynamic import will not move module'); no commit since 35e44f50 touched SystemStatusButton.tsx.

### Verifier adjustments

**Verdict:** CONFIRMED at 24.25/30 (states 5.0 + navigation/IA 4.5 + accessibility 4.5 + marine-safety-UX 5.5 + performance 4.75), up 0.25 from the evening's 24.0 — the sole mover is accessibility (+0.5 theme de-collapse credit, −0.25 new bulk aria-label finding), and every claim behind that movement verified real at HEAD. Adversarial re-check opened every load-bearing citation: (1) STATES — silent inspect .catch confirmed verbatim; MapHub navigator.onLine split confirmed at :323/:2883/:4903 vs probe-driven Dashboard.tsx:149 with raw navigator.onLine rain timers at :333/:384; marine.ts:196-228 null fallthrough and StatusBadges provenance-removal comment confirmed; HeroSlide has 13 '--' fallbacks. One evidence leg refuted (CacheAgeBadge deleted, see refuted) without changing the score. (2) NAV/IA — canSwipeBack={false} at App.tsx:816, zero pushState/popstate repo-wide, VESSEL_CHILDREN still 9 hand-listed vs 22 group:'vessel' registrations with VESSEL_VIEWS consumed by App.tsx but ignored by uiStore's direction logic, App.entitlements read in full (aps-environment/applesignin/weatherkit only — no associated-domains), exactly 8 z-[10xxx] literals. (3) ACCESSIBILITY — 403 sub-11px labels reproduced exactly (325×10px + 73×9px + 5×10.5px) against typeScale.ts's own declared 11px WCAG floor; FloatPlanSheet 10px labels (:139,:153,:239) and py-2.5 text-xs share buttons confirmed; AnchorAlarmOverlay:101 still text-red-400/60 with corrected lines above (:84) and below (:148); the NEW aria-label='Edit item details' finding is real and correctly weighted — 10 hits across 5 files, including CrewProfileForm.tsx:369 where every EXPERIENCE_LEVELS option carries the identical label, masking visible text from VoiceOver. (4) MARINE-SAFETY — RadialHelmMenu structure confirmed two-tier (tier 1 = category bubbles per the file header and categories/tier1Angles rendering; MOB is pushed into the 'tactical' category's items array at :199-206, and the tacticalState doc at :53-55 confirms it navigates rather than marks), so MOB remains FAB → Tactical → MOB → activate and the 0.25 stands, billed once (states auditor correctly declined to double-bill); critical-alerts entitlement confirmed absent from App.entitlements with the pending-entitlement comments at AnchorWatchService.ts:46 and :908-910. (5) PERFORMANCE — all four deductions byte-verified: App.tsx:402 root useLocationStore + :927-932 inline closures, LocationStore.notify() fresh {...state} snapshot + Date.now() stamp, useLiveLocationName 3s/25m cadence, MapHub 5,112 lines / 54 useState / unmemoized at :297 / self-subscribed at :1579 / inline buildTacticalState at :3493 with unmemoized RadialHelmMenu, supabase capacitorAuthStorage.getItem with no memo layer, SystemStatusButton.tsx:15 static ShipLogService import mounted at App.tsx:719/:1081 mooting the :346 dynamic deferral, vite.config.ts MIXED_IMPORTS suppression. De-dupe audit clean: MOB tier-2, offline-split, and FloatPlanSheet sizing each billed exactly once; no finding leaks onto the ENC certified track (encPopup correctly excluded; useVesselTracker's 9px chips are own-ship nav UI, not ENC rendering internals, and serve only as evidence within the already-billed text-size class). MealCalendar w-8 h-8 close (:793) and MusicPage h-9 w-9 transports (:2064) beside h-11 play/pause (:2072/:2080) confirmed. Cadence correction accepted (~6-10s underway shell re-render, 3s ceiling). Final UX: 24.25/30.
