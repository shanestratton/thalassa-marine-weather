# Thalassa Full-App Audit — 2026-08-03 (commit 6ea47741)

Protocol: 8 rubric finders + 3 adversarial group verifiers (11 agents, run wf_fd08f8fc-e24).
Baseline for comparison: 56.1/100 (2026-06-21). ENC layer internals excluded (own track, 86.15).

## Scorecard

| Dimension                                        | Score     | Max | Verifier delta             |
| ------------------------------------------------ | --------- | --- | -------------------------- |
| **ENGINEERING**                                  | **24.5**  |     |                            |
| build                                            | 6.5       | 8   | (raw 6.25, verifier +0.25) |
| test-health                                      | 6.25      | 8   |                            |
| architecture                                     | 3.5       | 8   | (raw 3.25, verifier +0.25) |
| DRY                                              | 1.75      | 6   |                            |
| consistency                                      | 2.75      | 5   | (raw 2.5, verifier +0.25)  |
| Code Quality — security                          | 3.75      | 5   |                            |
| **UI**                                           | **17.5**  |     |                            |
| design-system adherence                          | 3.5       | 8   |                            |
| theming                                          | 4         | 7   | (raw 3.75, verifier +0.25) |
| visual-consistency                               | 5.5       | 8   | (raw 4.75, verifier +0.75) |
| component-polish                                 | 4.5       | 7   |                            |
| **UX**                                           | **21.25** |     |                            |
| states: loading/empty/error/offline              | 4.5       | 6   |                            |
| navigation/IA                                    | 3.75      | 6   |                            |
| accessibility                                    | 4         | 6   |                            |
| marine-safety-UX                                 | 4.25      | 6   | (raw 4, verifier +0.25)    |
| UX — performance (experienced jank/latency/boot) | 4.75      | 6   |                            |

**TOTAL: 63.25 / 100**

## ENGINEERING — full deduction ledger

### build — 6.25/8 (pre-verify)

- **−0.75** CI is comprehensive but young and not reliably green: first runs 2026-07-28 were a wall of red (23F/0S; 07-29: 18F/2S), reliably green only since 07-30, and master still goes red regularly from real errors pushed directly with no pre-merge gate (~14% of completed runs since 07-30; e.g. an edge-function Deno type error left master red ~70 min on 08-02).
    - Evidence: gh run list --workflow=ci.yml (last 100 runs): 2026-07-28 {failure:23, cancelled:5}, 2026-07-29 {success:2, failure:18}, then 07-30→08-02 {success:30, failure:5}; failed run 30768650684 (2026-08-02T21:47Z) failed at step 'Edge function type check', next green run 30771194288 at 22:56 — .github/workflows/ci.yml:1-108 defines the (excellent) 12-step pipeline these runs execute
- **−0.5** Dependency-update automation is silently broken and vulnerabilities sit unpatched: dependabot's npm ecosystem targets a branch named 'main' that does not exist (repo default is master), so npm update PRs are never created — 59 packages outdated and 10 vulnerabilities (2 high) remain, including brace-expansion (high) whose fix is available via a plain non-breaking 'npm audit fix'; CI only fails on critical.
    - Evidence: .github/dependabot.yml:9 ('target-branch: main') vs 'gh api repos/{owner}/{repo} --jq .default*branch' → master and 'git branch -r' showing origin/dependabot/github_actions/* branches but zero dependabot/npm/\_ branches; 'npm outdated' → 59 rows; 'npm audit' → '10 vulnerabilities (6 low, 2 moderate, 2 high)', brace-expansion 'fix available via npm audit fix'; .github/workflows/ci.yml:40-44 gates critical only
- **−0.5** Three live map-rendering engines ship in one offline-first app: mapbox-gl (73 importers), leaflet (13 importers), and maplibre-gl kept alive for just two components — mapbox-gl and maplibre-gl are sibling GL engines, so the second is near-pure duplication weight despite the repo's own bundle-budget discipline.
    - Evidence: package.json dependencies list all three (mapbox-gl ^3.18.1, leaflet ^1.9.4, maplibre-gl 5.24.0 per npm outdated); grep: maplibre imported only by components/map/ThalassaMap.tsx and components/passage/SpatiotemporalMap.tsx, which are live-imported from components/map/MapHub.tsx, components/map/useMapInit.ts, components/passage/PassageCanvas.tsx; vite.config.ts:374-396 manualChunks splits vendor-mapbox and vendor-leaflet but maplibre rides along unsplit

### test-health — 6.25/8 (pre-verify)

- **−1** Absolute coverage is ~25% lines / ~21% branches against the repo's own stated 80% target — roughly three-quarters of instrumented product code has zero coverage, and large god-modules remain untested or near-untested (HeroSlide 1371 LOC / 0 test refs, DiaryPage 1553 / 0, RadialHelmMenu 1408 / 1 ref, ChartLockerService 2058 LOC / 1 test file).
    - Evidence: vitest.config.ts:44-52 — thresholds lines:25/functions:20.5/branches:20.5/statements:25 with comment 'raised to sit just below the verified full-suite baseline (25.33/20.97/21.00/25.29)' and 'TARGET: 80% across the board'; grep -rl over tests/: components/dashboard/HeroSlide.tsx 0 refs, components/DiaryPage.tsx 0 refs, components/map/RadialHelmMenu.tsx 1 ref, services/ChartLockerService.ts (2058 lines per wc -l) referenced by 1 test file
- **−0.25** The coverage ratchet is global-only — no per-module thresholds pin the safety-critical dirs (MobService, AnchorWatch\*, services/shiplog), so a coverage regression in life-safety code passes CI as long as the app-wide 25% floor holds.
    - Evidence: vitest.config.ts:43-52 — single global thresholds block, no per-glob threshold entries (vitest supports glob-keyed thresholds); coverage.include spans all of services/**, components/**, etc. so unrelated additions can mask safety-code regressions
- **−0.25** A documented race-class flake was left unresolved: tests/routing/segmentRoute.test.ts failed 1-in-8 full-suite stress runs under max parallel contention on 2026-06-21 and was explicitly parked as 'watching; not touching' — no subsequent commit to the file addresses it, so the green-master signal still carries a known unreliable test.
    - Evidence: docs/ROUTING_COLLAB.md:3158-3161 (reply 60): segmentRoute.test.ts 'a rare single failure under MAX parallel contention… did NOT reproduce across 8 full runs. Watching; not touching'; git log --oneline -3 -- tests/routing/segmentRoute.test.ts shows only behavior-change commits (a5c89db3, 7ab62e38, ee60af86), none a flake fix
- **−0.25** The full suite still cannot be run unmanaged on the development machine (OOM without --maxWorkers=2 per the audit's given constraint), and the config itself documents full-suite parallel contention breaking tests — local pre-push whole-suite verification is impractical, leaving CI as the only place the complete suite runs.
    - Evidence: vitest.config.ts:9-16 — comment: real-ENC repros 'can blow the 5s default under full-suite parallel CPU contention, surfacing as an intermittent STACK_TRACE_ERROR' (testTimeout raised to 20000 to compensate); package.json build script and .github/workflows/ci.yml:21-22 both require NODE_OPTIONS=--max-old-space-size=4096; full-suite --maxWorkers=2 OOM constraint confirmed as a given in the audit brief

### architecture — 3.25/8 (pre-verify)

- **−1.75** God-modules are severe and REGRESSING: the MapHub decomposition has gone backwards (+89% since the last audit), and the project violates its own declared 500-line component cap in 78 files.
    - Evidence: components/map/MapHub.tsx is 5476 lines today vs 2893 at the 2026-06-21 baseline commit (git show 11290b1b:components/map/MapHub.tsx | wc -l = 2893; 4984 by Jul 10) — with 163 React hook call sites and 111 imports in one component. CONTRIBUTING.md:28 declares 'Max 500 lines — extract sub-components'; 78 components exceed it, 25 exceed 1000 (components/VesselHub.tsx 2478, components/CrewManagement.tsx 2390, pages/LogPage.tsx 2264, components/music/MusicPage.tsx 2261, components/voice/BosunConsole.tsx 2153). The pieces extracted FROM MapHub are themselves god-modules: components/map/usePassagePlanner.ts 2575, components/map/useWeatherLayers.ts 2397, components/map/useMapInit.ts 1417. 27 services exceed 1000 lines (services/InshoreRouter.ts 3691, services/shiplog/OfflineQueue.ts 3094, services/DiaryService.ts 2847, services/routeTracer.ts 2568).
- **−1.25** 30 circular-dependency chains detected by madge across services/stores/utils/hooks (~24 outside the certified ENC track), including store<->service cycles that make the store layer bidirectionally coupled to business logic.
    - Evidence: npx madge --circular over services stores utils hooks (562 files) reports 30 cycles. Non-ENC examples: stores/settingsStore.ts > services/ShipLogService.ts > services/shiplog/CapturePipeline.ts > services/shiplog/EntrySave.ts > services/shiplog/LiveTrickle.ts > (back to settingsStore); stores/followRouteStore.ts > stores/settingsStore.ts > services/ShipLogService.ts > services/shiplog/EntryCrud.ts > services/shiplog/RoutesAndTracks.ts > services/shiplog/PassagePlanSave.ts > services/VoyageService.ts (cycle 8); services/vessel/LocalDatabase.ts > services/ShoppingListService.ts > services/PassageProvisionsService.ts > services/GalleyRecipeService.ts; services/inshoreRouterEngine.ts > services/engine/tierPipeline.ts > services/tier3/tier3Router.ts. Component layer adds CrewManagement.tsx > crew/ReadinessCardStack.tsx.
- **−1** Layering is muddled versus the declared UI->Hooks->Services flow: over half of all components import services directly, and the services and stores layers import each other in both directions.
    - Evidence: ARCHITECTURE.md system diagram (lines 40-44) declares 'UI --> Hooks --> Services'. In practice 173 of 336 component .tsx files import from services/ directly (grep "from '../services/" over components/ pages/). 14 service files import from stores/ while 10 of 12 stores import from services/ (grep counts), producing the settingsStore<->ShipLogService cycles above. stores/uiStore.ts:9 reaches into components/ (type-only, minor).
- **−0.75** State management runs four coexisting paradigms mid-migration: zustand stores, hand-rolled 'Zustand-like' pub/sub stores, deprecated React context bridges still consumed by 23 files, and store-like singletons living in services/.
    - Evidence: stores/ splits 8 zustand (authStore.ts, settingsStore.ts, uiStore.ts...) vs 4 hand-rolled — stores/LocationStore.ts:9 'Zustand-like API without the dependency', same pattern in WindStore.ts, PassageStore.ts, MapFitTargetStore.ts. context/SettingsContext.tsx:16 marks useSettings() '@deprecated Use useSettingsStore() directly' yet 23 files still call useSettings() (grep). Seven bridge contexts in context/ plus a stray second directory contexts/ holding only CrewCountContext.tsx. services/NmeaStore.ts is a subscriber-model store living in the services layer.

### DRY — 1.75/6 (pre-verify)

- **−2** The historical haversine sprawl is fully intact: 28 private haversine function definitions across the tree (43 files contain the inline formula), while the canonical utils/navigationCalculations.ts has exactly 2 production importers.
    - Evidence: grep for haversine definitions finds 28 private copies, e.g. components/map/useVesselTracker.ts:25, components/map/ThreatBanner.tsx:72, components/map/GhostShip.tsx:34, services/MobService.ts:70, services/InshoreRouter.ts:2098, services/AnchorWatchService.ts:152, services/WeatherRoutingService.ts:309, services/shiplog/CourseChangeDetector.ts:317, stores/followRouteStore.ts:66, utils/cpaCalculation.ts:181, services/passage/detectBends.ts:125+135 (two in one file). Canonical utils/navigationCalculations.ts:15 calculateDistance is imported by only utils/generateDemoLog.ts and services/voyageCompute.ts:42 (plus its test); it is not re-exported via utils/index.ts. Eight competing EXPORTED haversines exist: utils/navigationCalculations.ts:15, utils/math.ts:119 calculateDistanceKm, utils/gpsFollow.ts:32, src/geo.ts:15, services/isochrone/geodesy.ts:12, services/engine/geometry.ts:16, services/shiplog/GpsTrackBuffer.ts:41.
- **−0.75** utils/fetchWithRetry.ts is dead canonical code — zero production importers — while at least 12 services hand-roll their own retry/backoff loops.
    - Evidence: grep -rln fetchWithRetry over services/components/hooks/utils/workers/stores/pages matches only utils/fetchWithRetry.ts itself (definition at :31), and utils/index.ts does not re-export it. Meanwhile ad-hoc retry loops exist in services/PiCacheService.ts:1458 ('for (let attempt = 0; attempt < MAX_ATTEMPTS...'), services/ChartLockerService.ts, services/AnchorWatchSyncService.ts, services/CrewService.ts, services/EncImportService.ts, services/PiProvisionService.ts, services/SeamarkService.ts, services/shiplog/EntrySave.ts, services/voice/deepgramRecognizer.ts, services/weather/ResumableGribFetcher.ts, services/weather/shelter/coastlineSource.ts, services/weather/useGribDownload.ts.
- **−0.5** Five toast implementations still ship: one canonical global system plus four independents each duplicating timer/dismiss/animation/z-index scaffolding (improved from baseline — the app-controller path now routes through the canonical system).
    - Evidence: Canonical: components/Toast.tsx:51 global toast API + :262 useToast. Independents: components/PushToast.tsx:191 maintains its own parallel global singleton (let globalPushToast) with its own ToastItem stack, auto-dismiss and exit animation (:201-230); components/ui/UndoToast.tsx rolls its own timer + rAF progress bar (:18-49); components/dashboard/OffshoreBoundaryToast.tsx and components/map/PerfDowntierToast.tsx each implement their own show/auto-dismiss lifecycle. Credit: hooks/useAppController.ts:461 showToast now delegates to the canonical toast, so general-purpose duplication was consolidated.
- **−0.5** The metres-to-feet conversion has four separately exported canonical constants plus a canonical mToFt helper, and the raw literal 3.28084 is still inlined at 25+ call sites (drift risk contained only by the parity test on exported ones).
    - Evidence: Four exported/module constants all defining 3.28084: services/units.ts:19 FEET_PER_METRE, services/GlobalUnitService.ts:187 M_TO_FT, services/weather/api/marine.ts:72 M_TO_FT, services/weather/shelter/index.ts:17 FT_PER_M — alongside utils/units.ts:8 mToFt. Raw literal inlined at e.g. components/OnboardingWizard.tsx:361, components/AnchorWatchPage.tsx:102, components/DiaryPage.tsx:457, components/passage/PassageCanvas.tsx:285, services/weather/transformers.ts:237/242/260/305/320/397/544, services/weather/api/openmeteo.ts:352/442/495. (The drifted 3.281 constant from the 06-21 audit is fixed — WeatherInspectPopup.tsx:57 and VoyageResults.tsx:159 note the remediation.)
- **−0.5** Cache and micro-helper boilerplate is repeatedly hand-rolled: 22 files build their own Map-based caches, three services duplicate near-identical loadCache/saveCache localStorage pairs, and 7 private sleep/delay helpers exist.
    - Evidence: 22 files match 'cache = new Map' style TTL caches (grep), e.g. services/TideHeightService.ts, services/GebcoDepthService.ts, services/OsmRouteOverlayService.ts, services/ntmRouting.ts, services/InshoreRouter.ts. Hand-rolled persistence pairs: services/qldNotices.ts loadCache()/saveCache(), services/NoticeToMarinersService.ts loadCache()/saveCache(), services/MapOfflineService.ts loadLastAutoCache()/saveLastAutoCache(). 7 private sleep/delay/wait helper definitions across services/utils/workers (grep).

### consistency — 2.5/5 (pre-verify)

- **−0.75** File-naming convention is split roughly 2:1 in both services/ and stores/ — PascalCase and camelCase coexist with no rule, including a confusing side-by-side pair for the same subsystem.
    - Evidence: services/\*.ts top level: 101 PascalCase vs 55 camelCase files (ls-based count) — services/InshoreRouter.ts sits beside services/inshoreRouterEngine.ts, services/ShipLogService.ts beside services/routeTracer.ts, services/gpxService.ts, services/weatherRouter.ts. stores/: 4 PascalCase (LocationStore.ts, MapFitTargetStore.ts, PassageStore.ts, WindStore.ts) vs 8 camelCase (authStore.ts, settingsStore.ts, uiStore.ts...), where the casing split exactly tracks the hand-rolled vs zustand implementation split.
- **−0.75** File organization is inconsistent: the pages/ directory holds exactly one page while a dozen other \*Page.tsx components live under components/; duplicate context/ and contexts/ directories; a vestigial src/ directory; and a React hook living in services/.
    - Evidence: pages/ contains only LogPage.tsx + log/; meanwhile components/AnchorWatchPage.tsx, components/ChatPage.tsx, components/DiaryPage.tsx, components/GuardianPage.tsx, components/LonelyHeartsPage.tsx, components/music/MusicPage.tsx, components/vessel/AvNavPage.tsx (and 6 more vessel/\*Page.tsx) are pages living in components/. context/ (8 files) coexists with contexts/ (1 file: CrewCountContext.tsx). src/ holds 7 stragglers including src/geo.ts (a competing exported haversine) and src/windField.ts, while the rest of the app lives at repo root. services/weather/useGribDownload.ts is a React hook in the services layer.
- **−0.5** Declared standards and docs have drifted from practice: CONTRIBUTING.md rules are ignored at scale and ARCHITECTURE.md misstates the codebase by ~6x and still diagrams a deleted feature.
    - Evidence: CONTRIBUTING.md:28 'Max 500 lines' vs 78 violating components; CONTRIBUTING.md:35 'Avoid as any' vs 65 occurrences in non-test source (grep over services/components/stores/hooks/utils). ARCHITECTURE.md:8 says '67 components' (actual: 420 files in components/), :10 says '65 Services' (actual: 385), and the component-hierarchy diagram still shows 'Marketplace' under Chat — a feature deleted 2026-07-29 (commit bc065281). Positives kept: 0 bare eslint-disable, 40/40 sampled services carry header doc comments, conventional-commit discipline holds in git log.
- **−0.5** Error-handling and async idioms vary by module with no shared convention: throw vs return-null-in-catch vs bespoke result objects, and legacy .then() chains persist alongside async/await.
    - Evidence: 26 catch sites in services/ resolve errors to 'return null' (grep) while other services throw or invent one-off result shapes — services/VesselFleetService.ts:151 defines FleetMutationResult<T> ({value, queued}) used nowhere else as a pattern; services/units.ts and callers instead throw; hooks/useAppController.ts:461 infers error-ness by string-matching msg.toLowerCase().includes('error') to pick toast severity. 45 service files still use .then() chains vs 218 using await (grep counts), with both styles mixed in the weather API layer.

### Code Quality — security — 3.75/5 (pre-verify)

- **−0.5** No schema validation at fetch boundaries: zod is still a declared dependency that is never imported anywhere in the codebase; upstream JSON is trusted via raw type casts, including routing/depth payloads arriving over unauthenticated plaintext boat-LAN HTTP.
    - Evidence: package.json:94 declares "zod": "^3.22.4" but grep for zod imports across all .ts/.tsx/.mjs returns zero hits (baseline finding still true). 38 raw '(await res.json()) as T' casts in services/, e.g. services/OsmRouteOverlayService.ts:159 casts Pi routing overlay to Partial<OsmRouteOverlay> with only nullish-defaulting, services/PiCacheService.ts:760, services/InshoreRouter.ts:2579, services/MapboxDirectionsService.ts:119. Mitigation exists but is ad-hoc (e.g. services/localNotices.ts:54-56 Number.isFinite filter; services/supabase.ts:329-349 waypoint bounds checks), not systematic.
- **−0.25** iOS CSP network controls are largely decorative: CapacitorHttp enabled:true patches fetch/XHR to native HTTP (bypassing connect-src entirely), and the meta CSP itself carries bare http: and data: in connect-src plus script-src 'unsafe-inline'; SECURITY.md documents the unsafe-inline and native-http trade-offs but not the CapacitorHttp/CSP bypass.
    - Evidence: capacitor.config.ts:23-25 'CapacitorHttp: { enabled: true // Patch fetch/XHR to use native HTTP — bypasses CORS }'; index.html meta CSP line 'connect-src 'self' data: http: …' and "script-src 'self' 'unsafe-inline'"; SECURITY.md:32-37 accepted-trade-offs table omits the native-patch bypass. Weighted low: 'unsafe-eval' claim verified true (absent from both index.html and vercel.json), XSS sink surface is small and escaped (components/map/useAisStreamLayer.ts:356-415 escapes every field via escapeAisPopupHtml on unknown-typed inputs; only 6 innerHTML assignments repo-wide), and ATS restricts plaintext to local network (ios/App/App/Info.plist NSAppTransportSecurity = { NSAllowsLocalNetworking = true } only, no NSAllowsArbitraryLoads).
- **−0.25** Boat-LAN data integrity: routing water/reef/coastline overlays and pi-cache ENC payloads are fetched over unauthenticated plaintext HTTP from an mDNS name with no payload signing or manifest verification — a spoofed calypso.local on shared marina Wi-Fi can inject poisoned routing/depth data into a navigation app.
    - Evidence: services/OsmRouteOverlayService.ts:150-180 fetches the Pi overlay over plain fetch and merges it straight into router inputs with no authenticity check; ios/App/App/Info.plist NSAllowsLocalNetworking=true permits the plaintext local transport; SECURITY.md:37 documents 'connect-src http: (native only)' for the boat-LAN Pi as an accepted trade-off but no integrity control exists client-side. Low likelihood (proximity attack, single prod user) but navigational-safety severity.
- **−0.25** Supabase session including the refresh token persists in Capacitor Preferences (iOS UserDefaults plist), not the Keychain — readable from unencrypted device backups or a jailbroken device; Keychain would satisfy the documented eviction rationale equally well.
    - Evidence: services/supabase.ts:43-89 capacitorAuthStorage proxies to @capacitor/preferences (UserDefaults per its own comment at lines 14-16), wired as the auth storage at services/supabase.ts:205 with persistSession:true and autoRefreshToken:true; no Keychain/secure-storage plugin appears in package.json. The adapter itself is carefully built (ordered mutations, dual-store purge on removeItem at lines 77-88), so only the storage-class choice is deducted.

### Verifier adjustments (engineering)

- **+0.25** [build] Partial refutation of the 0.5 'three live map-rendering engines' deduction: maplibre-gl is never imported anywhere in source — the only repo-wide matches are two glyph-URL strings (demotiles.maplibre.org), and the cited 'importers' ThalassaMap.tsx / SpatiotemporalMap.tsx actually import mapbox-gl via react-map-gl/mapbox. Vite does not bundle un-imported dependencies, so 'maplibre rides along unsplit' as duplication weight is factually wrong. The corrected finding — two live engines (mapbox 73 importers + leaflet 13) plus one dead maplibre-gl dependency in package.json — still merits 0.25, so restore only half.
    - Evidence: grep -rn maplibre over all .ts/.tsx/.mjs/.js (excl. node_modules/dist/public/worktrees) returns only components/map/ThalassaMap.tsx:100 and components/passage/SpatiotemporalMap.tsx:95, both glyph URL strings; ThalassaMap.tsx:5-7 imports 'react-map-gl/mapbox' and 'mapbox-gl'; package.json:81 still declares maplibre-gl ^5.24.0 (dead dep)
- **+0.25** [architecture] Double-billing across the auditor's own deductions 2 and 3: the settingsStore<->ShipLogService bidirectional store/service coupling is billed in the 1.25-point circular-dependency deduction (madge cycles 2, 3, 8, 11, 12 all route through it) and again in the 1.0-point layering deduction, whose evidence explicitly says the 14-service-import-stores / 10-store-import-services counts are 'producing the settingsStore<->ShipLogService cycles above'. The layering deduction's independent leg (173/336 components importing services directly, contra ARCHITECTURE.md's declared UI->Hooks->Services flow) stands; restore the overlapping store<->service leg.
    - Evidence: Auditor deduction 2 evidence and deduction 3 evidence cite the same settingsStore.ts > ShipLogService.ts coupling; madge run reproduced (cycles 2/3/8/11/12 of 30 all traverse stores/settingsStore.ts > services/ShipLogService.ts)
- **+0.25** [consistency] Double-billing across dimensions: the consistency 'declared standards drifted from practice' deduction (0.5) re-bills the CONTRIBUTING.md:28 500-line cap violations ('78 violating components') that the architecture dimension already billed at full weight in its 1.75-point god-module deduction, which cites the identical rule and violation count as primary evidence. Same root cause (oversized components vs the declared cap), one consequence, billed twice. The consistency deduction's independent legs — ARCHITECTURE.md misstating component/service counts by ~6x (67 vs ~420, 65 vs ~385, verified), the deleted Marketplace still diagrammed, and 'as any' occurrences vs CONTRIBUTING.md:35 — are genuine and retain the remaining 0.25.
    - Evidence: CONTRIBUTING.md:28 'Max 500 lines — extract sub-components' verified; find components -name '\*.tsx' | xargs wc -l shows ~76 files >500 lines (24 >1000) — cited as evidence in BOTH the architecture god-module deduction and the consistency standards-drift deduction; ARCHITECTURE.md:8-10 stale counts independently verified
- Refuted: Build: 'Three live map-rendering engines ship in one offline-first app… maplibre imported only by ThalassaMap.tsx and SpatiotemporalMap.tsx' — REFUTED on its core premise. Neither file imports maplibre-gl; both import mapbox-gl (ThalassaMap.tsx:5-7 via react-map-gl/mapbox) and merely reference a demotiles.maplibre.org glyph URL string (ThalassaMap.tsx:100, SpatiotemporalMap.tsx:95). A repo-wide grep finds zero maplibre-gl imports, so it contributes zero bundle weight; the true finding is a dead package.json dependency (package.json:81) alongside two genuinely live engines (mapbox-gl + leaflet). Half the deduction restored, half retained for the real dual-engine + dead-dep state.
- Missed (netted into adjustments): maplibre-gl ^5.24.0 is a fully dead dependency (declared at package.json:81, zero imports repo-wide) — a cleaner, stronger finding than the refuted triple-engine claim; it joins zod (package.json:94, also never imported, already billed in the security lane) as a second declared-but-unused dependency, suggesting no depcheck/knip step exists in the otherwise thorough CI pipeline. Covered by the 0.25 retained within the build deduction rather than billed as a new line item. | Minor count correction, no score impact: components exceeding the 500-line cap measure ~76 (24 >1000) versus the auditor's 78/25 — within counting-method tolerance and immaterial to the deduction size.

**Group verdict:** The Engineering group's audit work is unusually solid: of roughly two dozen concrete evidence claims I re-executed (MapHub at 5476 lines vs 2893 at baseline commit 11290b1b, madge reproducing exactly 30 circular chains, dependabot targeting a nonexistent 'main' branch with zero dependabot/npm/\* remote branches, npm audit's 10 vulnerabilities incl. 2 high, zod and fetchWithRetry both dead with 0 importers, navigationCalculations at exactly 2 production importers, the CI run-list failure cadence, vitest thresholds 25/20.5/20.5/25, CapacitorHttp enabled:true, Supabase auth on Capacitor Preferences, the parked segmentRoute flake in ROUTING_COLLAB.md reply 60), all but one verified precisely. The one genuine miss is the build auditor's 'three live map engines' claim — maplibre-gl is never imported anywhere and ships no bundle weight; it is a dead dependency, so half that deduction is restored. Two modest double-billings were found and de-duped conservatively: the settingsStore<->ShipLogService coupling billed in both the cycles and layering deductions within architecture, and the CONTRIBUTING.md 500-line-cap violations billed at full weight in architecture's god-module deduction and again in consistency's standards-drift deduction. No material missed deductions surfaced in the credited areas — the claims of health I spot-checked (CI pipeline content, MobService semantics-grade tests, the coverage ratchet) held up. Net adjustment: +0.75 across build (+0.25), architecture (+0.25), and consistency (+0.25), leaving the group's harsh-but-evidenced picture intact: excellent small-scale hygiene and a genuinely strong CI/security trajectory, undermined by regressing god-modules, intact haversine/retry duplication sprawl, and a four-paradigm state-management migration.

## UI — full deduction ledger

### design-system adherence — 3.5/8 (pre-verify)

- **−1.25** The majority neutral family in components (gray-\*) is not in the design system at all — slate/stone are the only tokenized neutrals — making off-token styling the norm, not the exception, and it grew since baseline
    - Evidence: grep -rEo "\b(text|bg|border|from|via|to|ring|divide|placeholder|shadow)-(slate|gray|stone|zinc|neutral)-[0-9]+" components/ --include="\*.tsx" → 1681 gray vs 903 slate (baseline gray was 1565), spread across 165 files including core surfaces (components/passage/ResourceCalculator.tsx 52 uses, components/vessel/AvNavPage.tsx 50, components/map/MapHub.tsx 35, components/dashboard/WeatherCharts.tsx 37); theme.ts:123-141 (offshoreColors) and theme.ts:197-214 (onshoreColors) define only slate/stone text tokens
- **−1** Arbitrary font-size bypass grew ~26% since baseline while the semantic type-scale utilities built to replace it have ~7% adoption
    - Evidence: grep -rEoh "text-\[[0-9]+px\]" components/ --include="\*.tsx" | wc -l → 1898 (baseline ~1504), including 1379× text-[11px]; the sanctioned replacements .text-micro/.text-label (index.css:183-189, comment at 180-182: 'Use these instead of text-[10px] / text-[11px] / text-[13px]') have only 102 uses in components/
- **−0.75** 355 font sizes below the app's own self-declared 11px WCAG floor, 121 of them paired with muted colors — illegible-in-sunlight territory for a marine app
    - Evidence: index.css:61 comment: '11px is the WCAG-safe floor'; grep counts: 291× text-[10px] + 64× text-[9px] in components/; 121 instances combine 9-10px with gray/slate-500 or white/30-40, e.g. components/map/MapHub.tsx:4290 'text-[9px] leading-snug text-gray-500' and components/dashboard/GlassTutorial.tsx:309 'text-[9px] font-mono text-white/40'
- **−0.5** Type-scale tokens remain degenerate and contradict their own documentation: micro==label==11px in CSS vars, and theme.ts documents an 8-tier scale (label=12px) that its own tokens don't implement
    - Evidence: index.css:60-62 (--text-micro: 11px; --text-label: 11px — identical); theme.ts:270-279 comment declares 'label = 12px' but theme.ts:285-289 implement label, labelSm AND caption all as text-[11px] — three tokens, one size
- **−0.5** Palette-discipline violations on UI surfaces: MusicPage runs a bespoke off-system palette (custom hex backgrounds, cyan brand accents, off-scale radii/shadows) in direct violation of theme.ts's documented brand-palette reservation, and hex colors are hand-passed as icon props on VesselHub
    - Evidence: components/music/MusicPage.tsx:659 bg-[#050b12], :700 bg-cyan-300 text-[#04131d] shadow-[0_8px_22px_rgba(...)], :904 rounded-[1.35rem] bg-[#07131f]/90, :1039 rounded-[1.35rem] — vs theme.ts:29-33 ('Teal…and orange… NEVER mix these into UI surfaces') and radii tokens theme.ts:314-319; components/VesselHub.tsx:586-663 icon color="#cbd5e1", statusColor="#94a3b8", '#ef4444' hex literals in JSX props; 60 hex arbitrary-value classNames across components/
- **−0.5** Overlay/modal fragmentation — three coexisting patterns — and the token system's direct adoption is ~7% of component files, so coherence rests on convention rather than shared primitives
    - Evidence: ModalSheet (components/ui/ModalSheet.tsx) imported by 14 files; t.modal token used by 1 file; 27 files hand-roll 'fixed inset-0 z-[' overlays (grep -rln "fixed inset-0 z-\[" components/); no shared Button/Card component exists — theme.ts:374-383 class-string tokens only, with 20 static `import { t }` importers + 3 useThemeStore consumers out of 330 component files

### theming — 3.75/7 (pre-verify)

- **−1** Onshore mode is a background-only warm for ~95% of the UI: zero gray-\* override rules, zero accent override rules in CSS, and the intended sky-to-amber accent identity only manifests in the 3 components that consume the theme dynamically
    - Evidence: grep "theme-onshore._gray" index.css → 0 rules vs 1681 gray-_ utility uses in components/; the 42 .theme-onshore rules (index.css:219-327) cover only bg/text/border-slate-\*; no .theme-onshore rule touches sky/amber/purple accents, so onshore accent shift exists only for useThemeStore consumers — exactly 3 component files (components/dashboard/StatusBadges.tsx, components/dashboard/HeroSlide.tsx, components/chat/ChatHeader.tsx) — while 20 files import static t which is permanently offshore (theme.ts:424-437 'They always get offshore')
- **−0.75** Baseline onshore accent collapse persists unchanged: accent.sky is byte-identical to accent.amber and accent.purple to accent.red, destroying colour-as-information (a purple 'special' badge renders as danger-red onshore) on the dynamic path the codebase says new code should use
    - Evidence: theme.ts:216-221 (onshore sky: bg-amber-500/10, text-amber-400…) vs theme.ts:223-228 (onshore amber: same family, only bgActive/borderActive opacity differ); theme.ts:244-249 (onshore purple: bg-red-500/10, text-red-400) vs theme.ts:237-242 (onshore red: same family); ThemeContext.tsx:5 directs new code to useThemeStore, which serves these collapsed tokens
- **−0.75** Light mode (.display-light) has systematic holes: five high-frequency low-alpha surface/border/text utilities have no light override, rendering white-on-white-invisible cards, borders and text; the chart hub is entirely light-mode-unaware
    - Evidence: index.css .display-light block covers 99 distinct utilities but grep confirms NO rules for bg-white/[0.02] (84 uses in components/), bg-white/[0.06] (93), bg-white/[0.08] (41), border-white/[0.04] (49), or text-gray-200 (34) — all near-invisible on the light bg-slate-200 shell (App.tsx:425); grep isLight|effectiveMode|display-light in components/map/MapHub.tsx → 0 hits; light is reachable as the daytime half of displayMode 'auto' (hooks/useAppController.ts:853)
- **−0.5** No dusk tier and no S-52 palette switching for the chart: night vision is a single red scrim overlaid on unchanged day colors, and auto mode can never select night — it must be chosen manually
    - Evidence: App.tsx:1313-1319 (the only night treatment: fixed rgba(69,10,10,0.25) scrim) plus App.tsx:421-423 (text-red-600 container); no DAY/DUSK/NIGHT colour-table exists anywhere (grep dusk/night across services/ and components/map returns only sun-time computation and comments); hooks/useAppController.ts:853 'effectiveMode = isNight ? dark : light' — auto never yields night; Navionics/C-MAP-class competitors ship day/dusk/night chart tables
- **−0.25** Dead theming mechanism shipped in production CSS: the .theme-onshore --tw-slate-\* custom-property block is consumed by nothing (Tailwind v3 compiles colors inline, as the file itself admits 80 lines later) and even its own list omits slate-400, the most-used text shade
    - Evidence: index.css:135-169 sets --tw-slate-950…--tw-slate-50; index.css:216-218 admits 'Tailwind v3 compiles colors inline (not using CSS custom props), we use a targeted override strategy' — the real overrides are the separate explicit rules at index.css:219+; grep 'var(--tw-slate' across index.css and dist CSS → 0 consumers; line 155 has the stone-400 comment but no --tw-slate-400 assignment; block verified present in dist/assets/useFocusTrap-D6ljxaLB.css

### visual-consistency — 4.75/8 (pre-verify)

- **−0.75** Header treatment drift across top-level surfaces: three-plus distinct header styles despite a 22-page PageHeader standard
    - Evidence: components/ui/PageHeader.tsx:63 defines the standard (text-xl font-extrabold uppercase, adopted by 22 pages incl. LogPage/CrewManagement); components/SettingsModal.tsx:474 and :654 use a gradient-clip 'text-2xl font-black text-transparent bg-clip-text' header + 'Control Center' mono subtitle; components/vessel/GalleyPage.tsx:240-256 uses a bespoke 'text-base font-black' header with hand-rolled 36px back chevron; components/VesselHub.tsx:579 uses 'text-lg font-black tracking-wide'
- **−0.75** Empty-state visual language is split: ad-hoc emoji+grey-text blocks persist on shipped surfaces alongside the sanctioned EmptyState component
    - Evidence: components/ui/EmptyState.tsx:4-5 states it 'Replaces ad-hoc emoji + grey text patterns' and is adopted by 13 surfaces, yet GalleyPage.tsx:313-320 ('🍽️ No Active Meals') and :488-495 ('📖 No Saved Recipes'), components/crew/CrewRoster.tsx:174-181 ('👥 No crew yet'), and components/vessel/CastOffPanel.tsx:572-577 ('🗺️ No draft voyages yet') still use the ad-hoc pattern
- **−0.75** The app's own documented 11px accessibility type floor is violated 386 times with sub-11px text
    - Evidence: styles/typeScale.ts:8-10 declares 'ACCESSIBILITY: 11px floor for all text (WCAG 1.4.4)' and theme.ts:269-291 bottoms its 8-tier scale at 11px caption; grep finds 386 occurrences of text-[10px]/text-[9px]/fontSize:10 across components+pages, incl. components/map/MapHub.tsx (29 uses), components/VesselHub.tsx (10), pages/LogPage.tsx (8), and RadialHelmMenu.tsx:882,1000 item/category labels
- **−0.5** Two competing typography token systems each claim single-source-of-truth with conflicting tier values
    - Evidence: styles/typeScale.ts:2 ('Thalassa Type Scale — Single Source of Truth': body=13px, caption=12px; used by Toast.tsx and 4 passage/_ files) vs theme.ts:269-291 ('8-Tier type scale (standardised)': body=14px/text-sm, caption=11px; used by t._ consumers) — same tier names, different values, two consumer populations
- **−0.5** Icon language is mixed on shipped surfaces: emoji-as-icon vs the categorized SVG icon system
    - Evidence: components/vessel/GalleyPage.tsx:282 ('🍳 Active Meals' tab), :299 ('📖 Saved Recipes' tab), :341 ('📦' ingredient bullets), :420 ('🛒'); pages/LogPage.tsx carries 9 emoji; meanwhile components/icons/\* provides a categorized SVG set and components/PushToast.tsx:49-51 comments it was explicitly 'migrated from emoji' to properly-styled SVGs — the migration never reached Galley/Log/CrewRoster

### component-polish — 4.5/7 (pre-verify)

- **−0.75** Six orphaned UI primitives remain in components/ui with zero app importers, one still barrel-exported
    - Evidence: CacheAgeBadge.tsx, Pressable.tsx, FirstRunHint.tsx have zero references anywhere outside their own files; GestureTutorial.tsx and OnboardingTooltips.tsx are referenced only by tests (tests/TutorialOverlaysAccessibility.test.tsx:54, tests/OnboardingLocationOverlaysAccessibility.test.tsx:49); ListSkeleton is exported from components/ui/index.ts:15 yet imported only by tests/ListSkeleton.test.tsx. (Baseline items verified: ConnectivityBanner deleted, StalenessBanner remounted at Dashboard.tsx:1106, RetryCard mounted at ForecastSheet.tsx:244)
- **−0.75** Sub-44pt touch targets on wet-finger controls violate the app's own 'not negotiable' 44pt marine rule
    - Evidence: BackButton.tsx:10-13 and theme.ts:338-352 declare 44pt non-negotiable, yet: GalleyPage.tsx:244 back chevron w-9 h-9 (36px); SettingsModal.tsx:639 mobile back p-2 + w-5 icon (~36px); components/diary/DiaryEntryView.tsx:113-116 same pattern; components/vessel/CastOffPanel.tsx:685,694 crew-count steppers w-9 h-9; pages/LogPage.tsx:1461 expand-live-map h-9 w-9
- **−0.5** Literal '${t.border.default}' inside double-quoted className strings — the border token never applies and a junk class ships in the DOM on two default-surface components
    - Evidence: components/ForecastSheet.tsx:143 <div className="bg-slate-900/95 ${t.border.default} rounded-[2rem] ..."> (plain string, not a template literal) and components/dashboard/Advice.tsx:100 <Card className="bg-slate-900 p-0 overflow-hidden ${t.border.default} ..."> — Advice renders on the Glass dashboard, ForecastSheet on the map
- **−0.25** Fake staged loading theater in ForecastSheet cycles six fabricated progress messages every 300ms, looping the full sequence every 1.8s
    - Evidence: components/ForecastSheet.tsx:17-24 ('Contacting Satellite Grid...', 'Triangulating Wind Vectors...' etc.) with the interval at :73-76 stepping modulo the list every 300ms — implausible staged progress that visibly repeats on any fetch over ~2s
- **−0.25** Plain-text lazy fallback for Voyage Log contradicts the app's own documented no-spinner/skeleton discipline
    - Evidence: components/Dashboard.tsx:1142-1146 Suspense fallback renders bare text 'Loading Log…' for LogPage, while pages/LogPage.tsx:1611-1615 documents the house rule ('skeleton cards, NOT... never a page-wide spinner') and App.tsx:801 uses SkeletonDashboard for the equivalent Glass boot

### Verifier adjustments (ui)

- **+0.75** [visual-consistency] Double-billing: the sub-11px type-floor violation is deducted 0.75 in design-system (auditor 1: '355 font sizes below the 11px WCAG floor', index.css:61) and again 0.75 here (auditor 2: '386 violations of the 11px floor', styles/typeScale.ts:8). Same defect, same self-declared-floor evidence, same instances (auditor 2's 386 is just components+pages scope vs auditor 1's 355 components-only; I measured 377+6 across both). Billed once at the root in design-system; restored here.
    - Evidence: grep -rEoh 'text-\[(9|10)px\]' components/ pages/ → 377 (superset of the 355 counted in components/ alone: 291× text-[10px] + 64× text-[9px]); both deductions cite the same floor declarations (index.css:61 '11px is the WCAG-safe floor' / styles/typeScale.ts:8 'ACCESSIBILITY: 11px floor') and overlapping exemplars (MapHub, VesselHub)
- **+0.25** [theming] Partial double-billing of the gray-_ root cause: the 1.0 onshore deduction leads with 'zero gray-_ override rules' — but gray-\* being outside the token/override system is exactly the 1.25 design-system deduction. If gray were the tokenized slate, the 42 existing .theme-onshore slate→stone rules would catch most of it. The independent, non-gray portion of the finding (no accent override rules, 20 files pinned to offshore via static t, only 3 dynamic consumers — all verified) supports 0.75, not 1.0.
    - Evidence: index.css:219+ .theme-onshore rules cover bg/text/border-slate-* only (verified: explicit rgb overrides for bg-slate-950/900 etc.); grep 'theme-onshore.*gray' index.css → 0 — a direct consequence of the untokenized-gray finding already billed 1.25 in design-system; the accent-hole and 3-dynamic-consumer evidence (components/chat/ChatHeader.tsx:58 useTheme(), StatusBadges.tsx:2 + HeroSlide.tsx:41 useEnvironment, theme.ts:424-437 static offshore pinning) stands independently and keeps 0.75 billed

**Group verdict:** Both UI auditors did rigorous, reproducible work — every headline count re-ran to the exact figure (1681 gray vs 903 slate in components/, 1898 text-[Npx] vs 102 semantic replacements, 0 .theme-onshore gray rules, 139 .display-light rules with confirmed zero coverage of bg-white/[0.02|0.06|0.08], border-white/[0.04] and text-gray-200, 27 hand-rolled fixed-inset overlays, 1 t.modal consumer) and every spot-checked file:line landed: the onshore accent collapse (sky family byte-identical to amber, purple to red) is verbatim in theme.ts, the literal '${t.border.default}' inside double-quoted classNames ships in ForecastSheet.tsx:143 and Advice.tsx:100, the fake 300ms six-step loading theater, the 36px back/stepper controls against BackButton's 'not negotiable' 44pt doctrine, the dead --tw-slate-\* block (real overrides hardcode rgb values, confirming zero var() consumers), the emoji empty states EmptyState.tsx was built to replace, and all six orphaned primitives (only labeling nit: the 3 dynamic-theme consumers use useEnvironment/useTheme via ThemeContext, not useThemeStore directly — substance identical, exactly 3). No deduction was stale or fabricated; nothing user-invisible was billed — MapHub, GalleyPage, Dashboard, ForecastSheet are all default-path surfaces. The only corrections are cross-dimension double-billing: the sub-11px WCAG-floor violation was deducted 0.75 twice (design-system and visual-consistency) on the same instances and same self-declared floor, restored once in visual-consistency; and the onshore-theming deduction leans partly on the untokenized-gray root cause already billed 1.25 in design-system, restored 0.25 with the independent accent-hole/static-pinning evidence keeping 0.75. Adjusted group totals: design-system 3.5/8, theming 4.0/7, visual-consistency 5.5/8, component-polish 4.5/7 — UI group 17.5/30. The honest picture: foundations (PageHeader on 25 surfaces, tested night-scrim ceiling, shipped light mode, 44pt BackButton, disciplined spacing) are genuinely strong, but the two structural baseline findings — gray-as-majority-neutral outside the token system and the arbitrary-font-size bypass — both grew since June, and onshore/light theming remain shallow because of them.

## UX — full deduction ledger

### states: loading/empty/error/offline — 4.5/6 (pre-verify)

- **−0.5** StalenessBanner's loud 'error' and 'offline-cache' severities are unreachable on its only mount — the stale/staleAgeMinutes/error props are never passed, so all-upstreams-failed-while-online surfaces nothing until the 60-minute age tier, and a sibling comment falsely claims the banner covers these states
    - Evidence: components/Dashboard.tsx:1106-1112 passes only generatedAt/locationType/isOffline/onRefresh/isSyncing (comment at Dashboard.tsx:133-135 admits the freshness/error signals were 'removed'); components/dashboard/StalenessBanner.tsx:50-51 gates the two red tiers on the unwired error/stale props; components/dashboard/StatusBadges.tsx:178-181 claims 'stale/error states already surface in the StalenessBanner directly above this row'
- **−0.5** Data-age and fetch-failure surfacing is confined to the Glass — the chart's weather inspect popup silently drops its skeleton and paints without weather on failure (no message, no retry), and the purpose-built CacheAgeBadge/DataFreshness components have zero consumers, so cached-data age is invisible on the OBS/chart surface offline
    - Evidence: components/map/useWeatherInspectPopup.tsx:245-252 (catch: 'Drop the skeleton rather than spinning forever' — view.loading=false, paint(), no error state); components/ui/CacheAgeBadge.tsx and components/ui/DataFreshness.tsx have 0 importers outside components/ui/ (grep across components/, pages/, src/)
- **−0.5** Offline detection is inconsistent with the app's own doctrine: the chart ConnectivityChip, VesselHub, and BosunConsole key off raw navigator.onLine, so on a boat LAN with dead WAN uplink (the exact case internetProbe exists for) the chart chip renders nothing (implying online) while the header shows the amber offline chip
    - Evidence: components/map/ConnectivityChip.tsx:34-36 (detect() uses navigator.onLine, never uiStore.isOffline); components/VesselHub.tsx:181; components/voice/BosunConsole.tsx:254; contradicted doctrine at services/internetProbe.ts:4-14 and stores/uiStore.ts:74-80

### navigation/IA — 3.75/6 (pre-verify)

- **−0.75** No back-stack or history integration anywhere: still no router by design, previousView is a single slot, iOS edge-swipe back is implemented in PageTransition but disabled at its only mount, there is no Capacitor backButton listener, browser back exits the web app with no shareable URL state (vs Windy's URL-first web UX), and Settings needs a localStorage return-to hack to fake one level of stack
    - Evidence: App.tsx:806 (canSwipeBack={false} at the sole PageTransition mount); stores/uiStore.ts:28,48-64 (single previousView, no pushState/popstate); hooks/useAppBootstrap.ts:244-256 (only appStateChange listener — no backButton anywhere in repo); viewRegistry.tsx:182-197 ('thalassa_settings_return_to' localStorage workaround); services/deepLink.ts:4-5 ('The SPA has no router')
- **−0.5** The transition state machine has drifted from the shipped 5-tab IA: TAB_PAGES still models retired 'chat' as a tab while 'voyage' and 'details' (now bottom tabs) are absent, so Glass-to-Plan tab switches animate as push/pop drill-downs; VESSEL_CHILDREN omits 12+ current vessel sub-pages (mob, guardian, radio, galley, checklists, crew, notices, avnav, glass, weatherWindow...), so those drill-ins animate as flat tab crossfades
    - Evidence: stores/uiStore.ts:12 (TAB_PAGES = dashboard/map/chat/vessel), :13-23 (VESSEL_CHILDREN missing the newer vessel-group views in viewRegistry.tsx:365-395), :24 ('voyage' in OVERLAY_PAGES → 'push' on every Plan tab press from App.tsx:1243 setPage('voyage'))
- **−0.25** anchor_alarm push-notification taps route to the chart ('map'), which renders no anchor-watch UI at all — the alarm surface with the swing circle and acknowledge control is the 'compass' page, where the app's own SystemStatusButton 'View' action correctly points
    - Evidence: App.tsx:600-602 (case 'anchor_alarm': setPage('map')); no AnchorWatch/SwingCircle usage anywhere in components/map/ (grep); alarm UI at components/AnchorWatchPage.tsx:533,1183 registered as 'compass' (viewRegistry.tsx:261-266); App.tsx:711 (onNavigateAnchor → setPage('compass'))
- **−0.25** MOB is three taps from the helm: its sole entry point is a card inside the Vessel hub (Vessel tab → MOB card → red activate button), with no affordance on the chart, the Glass, the SystemStatusButton, or the RadialHelmMenu — where competitors (ORCA, dedicated plotters) keep MOB one tap from the primary nav surface
    - Evidence: components/VesselHub.tsx:921-926 is the only onNavigate('mob') in the codebase (grep of components/, pages/, hooks/, App.tsx); components/vessel/MobPage.tsx:4-10 (activation requires the button tap on arrival); no MOB entry in components/SystemStatusButton.tsx or components/map/RadialHelmMenu.tsx item lists
- **−0.25** No iOS universal links: the app entitlements carry no associated-domains, so boat.thalassawx.app/plan and voyage-log links always open Safari and can never deep-link into the installed native app; native URL handling exists only for the OAuth callback
    - Evidence: ios/App/App/App.entitlements (aps-environment, applesignin, weatherkit only — no com.apple.developer.associated-domains); the only appUrlOpen listener is components/settings/CalypsoIntegrationsTab.tsx:148 (OAuth redirect)
- **−0.25** Modal stacking runs two parallel systems: the OverlayPortal tier scale (modal/nested/critical, with a ceiling test) covers 41 files, but the map/passage overlay ecosystem still uses ad-hoc five-digit z-index literals outside the scale — the exact pattern that produced the documented night-scrim regression
    - Evidence: components/ui/OverlayPortal.tsx:13-31 (the sanctioned scale, whose own comment concedes 'several map/planning tools legitimately use five-digit z-index bands'); ad-hoc values at components/map/ChartDepthControls.tsx:203 (z-[10060]), components/passage/TripLegPicker.tsx:153, components/passage/PlanOnWebHint.tsx:94 (z-[10070]), components/map/TraceReportModal.tsx:299, components/map/MapHub.tsx:257, components/passage/PassageSummaryCard.tsx:1032; regression history at App.tsx:1298-1312

### accessibility — 4/6 (pre-verify)

- **−0.75** No OS text-size integration and pervasive hardcoded px micro-type that bypasses the app's own fluid rem scaling
    - Evidence: index.css:955-957 sets a fluid rem root (clamp(13px,4vw,17px)) but Tailwind arbitrary px classes ignore it: 291 occurrences of text-[10px] and 64 of text-[8px]/text-[9px] across components/ (e.g. VesselHub.tsx:882 text-[9px] status labels, RadialHelmMenu.tsx:1000 text-[10px] category labels, StalenessBanner.tsx:182 text-[11px]). No text-size setting exists anywhere in components/settings/ (AestheticsTab.tsx offers only displayMode/orientation), and iOS Dynamic Type has no effect on px text in the Capacitor WKWebView — a low-vision skipper cannot enlarge 9-10px data labels at all.
- **−0.5** Onshore theme still collapses sky accent into amber and remaps purple to red, destroying info-vs-warning colour semantics (unfixed baseline finding)
    - Evidence: theme.ts:216-229 — onshoreColors.accent.sky and accent.amber are byte-identical (both text-amber-400 / bg-amber-500/10), so informational and warning accents are indistinguishable ashore; theme.ts:244-250 maps accent.purple to text-red-400/bg-red-500/10, rendering non-danger accents in the danger colour. Applied globally via ThemeProvider's theme-${environment} root class (context/ThemeContext.tsx:27-35) plus the index.css:135+ .theme-onshore overrides.
- **−0.25** All four display-mode buttons carry the identical generic aria-label, overriding their visible labels for VoiceOver
    - Evidence: components/settings/AestheticsTab.tsx:104 — aria-label="Save theme selection" is hardcoded inside DISPLAY_MODES.map(), so Auto/Light/Dark/Night all announce as 'Save theme selection' with no aria-pressed/selected state; a VoiceOver user cannot tell which mode they are choosing or which is active.
- **−0.25** Sub-44pt touch targets on secondary but consequential controls
    - Evidence: components/map/RadialHelmMenu.tsx:1071 — tier-1 'Clear All' pill is px-3 py-1.5 text-[10px] (~28-30px tall) and it clears every active layer including AIS in one tap; RadialHelmMenu.tsx:931 grid clear is py-2 (~32px); components/dashboard/StalenessBanner.tsx:212 Retry button is px-2 py-1 text-[11px] (~26px tall). All well under the 44pt minimum NavButton.tsx:5 documents as the app's own standard for 'vessel movement/pitching safety'.
- **−0.25** Low-contrast fine print on safety surfaces, worst on the alarm screen itself
    - Evidence: components/anchor-watch/AnchorAlarmOverlay.tsx:146 — 'Monitoring continues after silencing' rendered text-red-400/40 over the near-black #1c0505 alarm gradient (AnchorAlarmOverlay.tsx:33) computes to roughly 1.9:1, far below WCAG 4.5:1, for a safety-relevant instruction read at 3am; similar /40-/50 alpha captions at AnchorAlarmOverlay.tsx:84 (text-red-400/50 last-known-position line).

### marine-safety-UX — 4/6 (pre-verify)

- **−0.5** MOB has no affordance on the chart surface; landscape helm mode buries it four taps deep
    - Evidence: grep of components/map/MapHub.tsx and components/map/RadialHelmMenu.tsx finds zero MOB references — the only phone entry point is the Vessel-hub tile (components/VesselHub.tsx:921-940), so from the chart it is OBS→Vessel→MOB→Mark (3 taps portrait). In mobile landscape App.tsx:1152 hides the bottom nav entirely, adding the App.tsx:1133-1150 hamburger first (4 taps) — in the one scenario where MobPage.tsx:9 itself says 'every second matters'. Apple Watch trigger (services/native/watchBridgeListeners.ts:11-13) mitigates for watch owners only; best-in-class plotters keep MOB one action from the chart.
- **−0.25** An active MOB fix is never plotted on the chart — helm gets numbers only, no spatial context
    - Evidence: services/MobService.ts has no consumer under components/map/ (grep -rln MobService matches only vessel/RadioConsolePage.tsx, vessel/MobPage.tsx, BgGeoManager.ts and the watch bridge). MobPage.tsx:349-370 shows bearing/distance text, but the chartplotter never renders the MOB point relative to hazards, route or drift — Navionics/ORCA drop a MOB waypoint on the chart instantly.
- **−0.5** Own-ship marker freezes silently when GPS fixes stop — stale position is indistinguishable from live, and the chart has no GPS status indicator
    - Evidence: components/map/useVesselTracker.ts:281-295 and components/map/useLocationDot.ts:14-31 update the marker only when a fix arrives; neither has any age timer, dimming, or 'position lost' state, so the dot just stops moving. The one GPS-status surface, SystemStatusButton, lives in the header (App.tsx:709) which App.tsx:459 explicitly hides on the map view (showHeader = !['map','warnings']). Anchor watch got a GPS watchdog (AnchorWatchService.ts:492-494) but the browse/underway chart did not.
- **−0.5** Anchor-alarm notification tap routes to the chart, which has no alarm UI or silence control — the full-screen alarm overlay is page-local
    - Evidence: App.tsx:600-602 — case 'anchor_alarm': setPage('map'). AnchorAlarmOverlay is only rendered from AnchorWatchPage.tsx:532-533, and the shore-phone alarm effect (AnchorWatchPage.tsx:188-217) only runs while that page is mounted; SystemStatusButton (the only other alarm surface) is hidden on 'map' per App.tsx:459. A skipper or shore watcher tapping the push lands on a chart with a max-volume alarm sounding and no visible acknowledge/silence control, needing Vessel→Anchor navigation mid-emergency.
- **−0.25** StalenessBanner's fetch-failure and offline-cache severity tiers are unwired on the Glass page — only age and no-network warnings can fire
    - Evidence: components/Dashboard.tsx:1106-1112 mounts StalenessBanner with generatedAt/locationType/isOffline/onRefresh but omits the stale and error props, so pickSeverity (StalenessBanner.tsx:50-51) can never return 'error' or 'offline'; Dashboard.tsx:133-135 confirms 'Freshness/error signals previously fed the StalenessBanner — removed'. An outright pipeline failure on recent-ish cache shows nothing until the 60-minute age tier catches up.

### UX — performance (experienced jank/latency/boot) — 4.75/6 (pre-verify)

- **−0.5** Underway Glass tick re-renders the whole shell: App root subscribes to LocationStore, which re-stamps a fresh snapshot object every 3s while moving, and Dashboard's own React.memo is defeated by inline closures at its only call site — so the 1157-line Dashboard re-renders every pass exactly when the user is underway.
    - Evidence: App.tsx:401 (`const locationStore = useLocationStore()` at App root); hooks/useLiveLocationName.ts:53 (POLL_INTERVAL_MS = 3_000) and :122/:131 (LocationStore.setFromGPS called each geocode pass while moved >25m); stores/LocationStore.ts:55-64 + 139-151 (notify() always publishes a NEW `{...state}` snapshot with a fresh `timestamp: Date.now()`, so React can never bail); components/Dashboard.tsx:109 (`React.memo` added for perf) nullified by App.tsx:917-922 (`onOpenMap={() => {...}}` and `onTriggerUpgrade={() => setIsUpgradeOpen(true)}` inline closures recreated every App render).
- **−0.25** MapHub remains an unmemoized 5476-line monolith with 63 useState + 39 useEffect (~330 hook calls): every one of its internal setState calls — including isochrone routing progress events and its own LocationStore subscription — re-executes the entire component body while the GL layers are animating.
    - Evidence: components/map/MapHub.tsx:309 (`export const MapHub: React.FC<MapHubProps>` — no React.memo), wc -l = 5476, grep counts: 63 useState / 39 useEffect / 332 `use[A-Z]` hook calls; MapHub.tsx:1594 (`const location = useLocationStore()` — new object every notify); components/map/usePassageRouterEvents.ts:68 (`setIsoProgress({...})` fires per routing progress event into MapHub state). MapHub decomposition acknowledged in-progress (MEMORY: project_maphub_decomposition); the leg-grading jank batch already proved this component's render path produced user-visible stalls.
- **−0.25** Boot bridge chatter: ~40 identical Preferences.get round-trips for the same auth key at boot (today's device log), because the Supabase storage adapter has no in-memory read cache and getCurrentUserId — explicitly documented for hot paths — goes storage-adapter → native bridge on every getSession() call.
    - Evidence: services/supabase.ts:44-60 (capacitorAuthStorage.getItem hits `Preferences.get({key})` on every call, no memo layer); services/supabase.ts:227-239 (getCurrentUserId: 'Use this in hot paths' — each call runs supabase.auth.getSession(), which reads through the adapter); boot log (lane brief, verified device log today) shows ~40 same-key reads. A one-line write-through cache in the adapter would eliminate the bridge round-trips.
- **−0.25** Boot-critical bundle leak: ShipLogService (243KB chunk) and tz (73KB) are statically reachable from the ApplicationShell boot graph via SystemStatusButton, nullifying App.tsx's deliberate dynamic import of ShipLogService — and vite.config.ts suppresses the exact MIXED_IMPORTS warning class that would surface such deferral defeats.
    - Evidence: components/SystemStatusButton.tsx:15 (`import { ShipLogService } from '../services/ShipLogService'`) with SystemStatusButton statically imported at App.tsx:24; App.tsx:345 (`void import('./services/ShipLogService')` — the intended deferral, now moot); dist/assets/ApplicationShell-BdFLx1LM.js contains `from"./ShipLogService-Cr9l6Bao.js"` and `from"./tz-Du620eZc.js"` (243KB + 73KB, ls dist/assets today); vite.config.ts onwarn: `if (warning.code === 'MIXED_IMPORTS' || warning.message?.includes('dynamic import will not move module')) return;`.

### Verifier adjustments (ux)

- **+0.25** [marine-safety-UX] Duplicate billing: the StalenessBanner unwired error/stale props (0.25 here) is the exact same defect — same mount, same missing props, same lines — already deducted 0.5 under auditor 1's 'states' dimension. Within one 30-point group the same wiring gap cannot be billed twice; the states dimension is the correct home.
    - Evidence: components/Dashboard.tsx:1106-1112 (single mount, props confirmed missing) and components/dashboard/StalenessBanner.tsx:43-61 are the identical citations in both auditors' deductions; Dashboard.tsx:133-135 comment confirms once.
- **+0.25** [marine-safety-UX] Anchor-alarm-push deduction (0.5) is both partially refuted and double-billed. Refuted part: the claim that SystemStatusButton is 'hidden on map per App.tsx:459' is false — App.tsx:459 hides only the header; a second SystemStatusButton is mounted on the map view itself at App.tsx:1071 (comment at App.tsx:1050: 'Bosun mic … + System status ℹ — paired top-right on map view'), visible whenever anchor watch is active, with onNavigateAnchor={() => setPage('compass')} — so a responder landing on the chart is one tap from the alarm page, not stranded 'needing Vessel→Anchor navigation mid-emergency'. Double-billed part: the mis-route itself (anchor_alarm → 'map' instead of 'compass', App.tsx:600-602) is already deducted 0.25 in auditor 1's navigation/IA. Reducing 0.5 → 0.25 leaves the genuine residue (push should deep-link straight to compass; the page-local alarm overlay at AnchorWatchPage.tsx:532-533 is confirmed) fairly priced.
    - Evidence: App.tsx:1051-1072 (SystemStatusButton mounted inside the map view's top-right overlay cluster with onNavigateAnchor → 'compass'); App.tsx:459 hides only the header; App.tsx:600-602 mis-route confirmed; duplicate at auditor 1 nav/IA deduction 3.
- **-0.25** [marine-safety-UX] Missed finding: an ARMED anchor watch has zero presence on the chart surface — no anchor point, no swing circle, nothing. Grep of components/map/ for AnchorWatch/anchorwatch/swing returns zero matches; RadialHelmMenu's 'Anchor' item (RadialHelmMenu.tsx:244) is the anchorages-guide overlay, not anchor watch. The swing viz is page-local to AnchorWatchPage ('compass'). This compounds the mis-route both auditors billed: the anchor_alarm push (App.tsx:600-602) deposits the responder on a chart that displays nothing about the anchor or the drag. Navionics/ORCA/dedicated plotters draw the anchor and swing radius on the chart; at the stated bar this is a concrete gap distinct from the routing defect.
    - Evidence: grep -rni 'anchorwatch|anchor watch|swing' components/map/MapHub.tsx components/map/RadialHelmMenu.tsx → no matches; components/map/RadialHelmMenu.tsx:244 'Anchor' is the anchorage layer toggle (ChartAnchorIcon, admiralty anchor icon at :1146,:1338); anchor-watch viz confined to components/AnchorWatchPage.tsx (registered as 'compass', viewRegistry.tsx:261).
- Refuted: Auditor 2 (marine-safety-UX): 'SystemStatusButton (the only other alarm surface) is hidden on map per App.tsx:459' — false; App.tsx:459 hides the header only, and a dedicated map-view mount exists at App.tsx:1071 inside the chart's top-right overlay cluster (comment App.tsx:1050), with a one-tap onNavigateAnchor path to the compass/alarm page. The same wrong premise also props up the GPS-staleness deduction ('the one GPS-status surface … hidden on the map') — that deduction's core claim (marker freezes with no age indication, useVesselTracker.ts:281-295 / useLocationDot.ts:14-31 confirmed to have no staleness handling, and the status button hides entirely when no system is active) survives, so its points stand, but the cited evidence needed correction. | Auditor 2 (marine-safety-UX): Stale
- Missed (netted into adjustments): Armed anchor watch is invisible on the chart: no anchor point or swing-circle rendering exists anywhere under components/map/ (grep for AnchorWatch/swing = zero matches; RadialHelmMenu.tsx:244 'Anchor' is the anchorages overlay, not anchor watch), even though the anchor_alarm push routes responders to that very chart (App.tsx:600-602). Billed -0.25 on marine-safety-UX.

**Group verdict:** The UX group's work is high quality and almost entirely confirmed at commit 6ea47741: I re-verified every load-bearing citation and the recently-changed hotspots. StalenessBanner's error/offline tiers are genuinely dead at their only mount (Dashboard.tsx:1106-1112 passes no stale/error props; the :133-135 comment admits the signals were removed; pickSeverity at StalenessBanner.tsx:43-61 can never return the red tiers); MOB's sole entry really is VesselHub.tsx:925 with zero matches in MapHub/RadialHelmMenu/SystemStatusButton; anchor_alarm pushes really route to 'map' (App.tsx:600-602) while the alarm overlay is page-local to 'compass'; canSwipeBack={false} at App.tsx:806; uiStore's TAB_PAGES still contains retired 'chat' while the live tab bar is dashboard/map/voyage/details/vessel (App.tsx:1243,1268), so Plan and Log tab presses animate as push drill-ins; the entitlements file has no associated-domains; theme.ts:216-229/244-250 still collapse sky→amber and purple→red; AestheticsTab.tsx:104 hardcodes one aria-label across all four mode buttons; and the perf lane's four deductions all check out precisely (SystemStatusButton's static ShipLogService import at App.tsx:24 defeating the :345 deferral with MIXED_IMPORTS suppressed in vite.config.ts:364-368; the LocationStore fresh-snapshot notify + 3s underway poll + inline closures at App.tsx:917-922 defeating Dashboard's memo; MapHub at 5,476 lines/63 useState/39 useEffect unmemoized; the uncached Preferences-backed auth storage at supabase.ts:44-60 behind the hot-path getCurrentUserId). Three corrections in the marine-safety dimension: the claim that SystemStatusButton is hidden on the map is wrong (second mount at App.tsx:1071 with a one-tap anchor shortcut), the anchor-push and StalenessBanner findings were each billed in two dimensions of the same group, and — cutting the other way — nobody billed that an armed anchor watch renders nothing on the chart the alarm push sends you to. Net effect: marine-safety-UX 4.0 → 4.25; all other dimensions stand as scored, moving the group from 21.0 to 21.25/30 — an honest reflection of a product whose offline/alarm/performance engineering is near the bar while its chart surface still lacks the safety affordances (MOB, anchor, GPS-staleness) that Navionics-class competitors put one tap or one glance away.
