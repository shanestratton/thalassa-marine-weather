# Release Evidence Checklist — audit items 18–22

Written 2026-09-05 for the public-beta candidate, from the external audit of
`919b8cb1` and what was verified against the repo and production that day.
This is the device / archive / App Store Connect work that only a signed
physical run can produce. Tick boxes as evidence lands; put dates beside them.

Decisions already made (do not re-open here): **Watch is cut** from this beta
(2026-09-05). **Apple Music is live** and stays live. **In-app deletion is on**.

---

## 18 · Native release evidence

There is no XCTest/XCUITest target, and `ship:beta` runs no native tests.
The signed archives and Apple validation/upload receipts below supersede the
original unsigned 2026-08-06 archive. Packaging does not replace the outstanding
physical-device matrix.

### Archive

**104 candidate supersession (2026-09-07):** the b36dcc57 packaging and main-D7cZs-UC.js references below are preliminary evidence, not the final archive fingerprint. CI subsequently exposed a diary-video cancellation race in the independently deployed Pi cache. The follow-up fix serializes local file operations, aborts active requests and prevents late responses from recreating a cancelled clip; all 223 Pi tests pass locally, including five new race regressions. The main app suite also passed (9,261 tests, five existing skips). Final source ea4ba2e4 passed [CI run 34092747990](https://github.com/shanestratton/thalassa-marine-weather/actions/runs/34092747990), including all four jobs, and was rebuilt/synced with all 132 source/configuration and 140 artifact contracts passing. Local browser checks passed in Chromium (40) and mobile Safari (39), with seven existing profile/hosted-only skips. No yacht deployment was performed; the subsequently authorised signed archive and upload are recorded below. Do not rebuild or reuse 104 after its upload.

- [x] Xcode 26.6 selected (2026-09-06; `xcode-select` had been on the 27.0 beta).
- [x] Build number bumped 101 → 102 on all four configurations, commit 0606365b (2026-09-06).
- [x] `npm run ship:beta` green under Node 24 (Homebrew node@24): 132 release + 140 artifact contracts; bundle 13.15/18 MB, JS 9.71/9.90 MB — 98% of the JS budget, watch it (2026-09-06).
- [x] Archived 2026-09-06 08:58 in Xcode 26.6: 1.2.0 (102), `com.thalassa.weather`, product `Thalassa Marine Weather.app` (icon label stays Thalassa), bundle main-DhSzDRmW.js, 0 source maps, dSYMs present. Archive signature is Apple Development as normal; the Distribution re-sign happens at export.
- [x] Organizer → Validate App passed 2026-09-06 ≈09:15 — "Thalassa Marine Weather 1.2.0 (102) validated. Your app successfully passed all validation checks." First attempt hit Xcode's stale app-record cache (it created the App Store record itself, then could not see it); a relaunch fixed it. App Store record: `com.thalassa.weather`, SKU `com.thalassa.weather`, primary language English (Australia), created by Xcode at 09:04.
- [x] Archive verified: no Watch folder, no PlugIns; background modes = audio, location, fetch (2026-09-06).
- [x] Uploaded 2026-09-06 ≈09:20 — "Thalassa Marine Weather 1.2.0 (102) uploaded". ITSAppUsesNonExemptEncryption=false, so no export-compliance prompt. App Store Connect now holds two records: **Thalassa Marine Weather** (`com.thalassa.weather`, this one) and the older **Thalassa** (`com.thalassa.weather-2025`, never had a build — it is what made the name "Thalassa" look taken).
- [x] Build number bumped 102 → 103 on all four configurations, commit c9fbd246 (2026-09-06).
- [x] `npm run ship:beta` green under Node 24 (24.19.0) on 231e58d3, 2026-09-07 12:31: 132 release + 140 artifact contracts; bundle 13.17/18 MB, JS 9.73/9.90 MB (98%); iOS bundle main-BkgZEbDP.js, byte-identical to `dist`. 103 carries everything committed to that point — the matrix fixes below **and** the Pi-as-primary section that follows; nothing was held back for 104.
- [x] Archived 2026-09-07 12:43 in Xcode 26.6: 1.2.0 (103), `com.thalassa.weather`, product `Thalassa Marine Weather.app`. Verified from the shell: `public/` byte-identical to the gated `ios/App/App/public` (bundle main-BkgZEbDP.js), 0 source maps, no Watch folder, no PlugIns, dSYMs present, background modes audio / location / fetch, ITSAppUsesNonExemptEncryption=false. Signed Apple Development as normal; the Distribution re-sign happens at export.
- [x] Organizer → Validate App passed 2026-09-07 ≈12:55 — "Thalassa Marine Weather 1.2.0 (103) validated. Your app successfully passed all validation checks." No stale-record retry needed this time.
- [x] Uploaded 2026-09-07 ≈13:00 — "Thalassa Marine Weather 1.2.0 (103) uploaded"; no export-compliance prompt. Processing → internal group Skipper (tester INSTALLED since 102, so the build should appear in the TestFlight app on its own). **Beta Skippers stays untouched and the public link stays OFF** until the matrix passes.

- [x] Build number bumped 103 → 104 on all four configurations, commit 566aa1a4 (the other session, 2026-09-07 16:31).
- [x] `npm run ship:beta` green under Node 24 (24.19.0) on b36dcc57, 2026-09-07 ≈16:55: 132 release + 140 artifact contracts; bundle 13.20/18 MB, JS 9.76/9.90 MB — **98.6% of the JS budget, 140 KB of headroom; the next feature trips it and the budget will need a deliberate decision**; iOS bundle main-D7cZs-UC.js, byte-identical to `dist`. 104 carries: Pi-first instruments (8a6a2ec3), the invite-only Instrument Panel (b6b6ef33), the weather's cloud rung (76bfface), the Ship's Log that follows the boat (e336bd05), the one-row Glass strip (d72303f6), "the Pi" wording (16ed590f), the permissions-cache tolerance (bdba223e), and the other session's public-page instrument faces and ship's-bell work.
- [x] Archived **1.2.0 (104)** in Xcode 26.6 on 2026-09-07 at 17:15 AEST (scheme App, generic iOS device): `~/Library/Developer/Xcode/Archives/2026-09-07/Thalassa-1.2.0-104-ea4ba2e4.xcarchive`. Source **ea4ba2e4**, bundle **main-DLa0BecT.js**, identifier `com.thalassa.weather`. All 413 archived `public/` files byte-match the gated native copy; zero source maps, no Watch or PlugIns, all 23 dSYM UUIDs match their binaries. Strict/deep signature verification passed with normal host trust-service access. Apple Development archive signing is expected before distribution re-signing. Shane's description is saved in the archive with the corrected final bundle/commit identifiers.
- [x] Apple validation passed for **1.2.0 (104)** on 2026-09-07 at approximately 17:22 AEST: Xcode's `method=validation`, `destination=upload` workflow returned `Validated App` / `EXPORT SUCCEEDED`. Automatic version/build-number management was disabled, preserving 104.
- [x] Uploaded **1.2.0 (104)** on 2026-09-07 at **17:24:30 AEST**: Xcode reported `Upload succeeded` / `Uploaded App` / `EXPORT SUCCEEDED`, and Apple confirmed the uploaded package is processing. This receipt confirms upload, not completed processing or tester availability. **No Beta Skippers, public-link or tester-group settings were changed.**
- [x] 104 processed and reached the internal **Skipper** group: installed over 103 on Shane's phone (Shane, 2026-09-08 morning: "104 is on my phone right now"). External beta access unchanged.

### Build 105 final candidate (2026-09-08)

- [x] Exact source **153141e4** passed [CI run 34165267304](https://github.com/shanestratton/thalassa-marine-weather/actions/runs/34165267304): all four jobs green, including the full coverage suite, dependency/lint/format/type checks, Deno tests, production packaging, Lighthouse and CI browser tests. [CodeQL run 34165267291](https://github.com/shanestratton/thalassa-marine-weather/actions/runs/34165267291) also passed. Final result verified after 08:20 AEST, before the release-documentation-only commit.
- [x] Rebuilt and Capacitor-synced from **153141e4** under Node 24 with `VITE_APP_BUILD=105`: **132 source/configuration + 140 artifact contracts** passed, including production routes, secret scans and bundle budgets. Bundle **main-DeeB8j8m.js**, total 13.21/18 MB, JavaScript 9.77/9.90 MB. All 410 `dist` files byte-match the native copy; 19 Capacitor plugins synced. No app source or parked vessel-claim work was changed.
- [x] Local production-browser matrix: **79 passed, 7 existing profile/hosted-only skips** across Chromium and mobile Safari (2.6 minutes). The full app unit suite and shared-reader tests are recorded in the 105 follow-up test gates below.
- [x] Signed archive completed **08:07:33 AEST**, Xcode **26.6 (17F113)**, scheme App / Release / generic iOS device: `/Users/shanestratton/Library/Developer/Xcode/Archives/2026-09-08/Thalassa-1.2.0-105-153141e4.xcarchive`. Verified **1.2.0 (105)**, `com.thalassa.weather`, iOS 17 minimum; all **412 archived public files** byte-match the gated native copy, **23 binaries have matching dSYMs**, zero source maps, no Watch or PlugIns, background modes audio/location/fetch and `ITSAppUsesNonExemptEncryption=false`. Strict/deep code-signature verification passed. Apple Development archive signing is normal before distribution re-signing.
- [x] Apple validation passed **approximately 08:10 AEST**: `method=validation`, `destination=upload` returned **Validated App / EXPORT SUCCEEDED**. `manageAppVersionAndBuildNumber=false` preserves 105. This is validation, not a distribution upload or TestFlight processing receipt.
- [x] Distribution export passed **approximately 08:11 AEST**: `/Users/shanestratton/Documents/Temporary Projects/Thalassa Releases/1.2.0-105-153141e4/Thalassa Marine Weather.ipa`, SHA-256 **99ed027e9a4973127c3773d49e494ca7ae7e03f42daf637a63f19f3e4ff0cbbe**. Verified Cloud Managed Apple Distribution signing, strict/deep signature, `get-task-allow=false`, production APNs and `beta-reports-active=true`; exported app remains 1.2.0 (105), with matching web files and dSYMs. Draft What to Test notes sit alongside the IPA. No distribution upload, tester-group changes, public-link changes or yacht deployment was performed.
- [x] **Uploaded 1.2.0 (105) at 08:27:47 AEST**, following Shane's explicit upload approval. The same validated archive/source **153141e4** was sent through Xcode with automatic version/build management disabled. Apple returned **Uploaded package is processing / Upload succeeded / Uploaded App / EXPORT SUCCEEDED**. Local receipt: `/private/tmp/thalassa-build105.RZa2NL/upload.log`. This confirms upload, not completed processing, internal-group availability or installation. App Store Connect's browser session requires sign-in before those can be checked. No tester-group, Beta Skippers, public-link or yacht changes were made. **Do not rebuild or reuse 105 after this upload.**
- [ ] Verify processing completion, availability in the internal **Skipper** group and upgrade from 104 on the phone. The proposed test notes were clarified to separate B joining/stopping from B replacing the followed route; both devices should be on 105 with a freshly authored passage, the clock should be on Ship's position, and fuel cards are explicitly Not connected pending sender/feed integration. These notes have not yet been submitted to App Store Connect.

### TestFlight (2026-09-06)

- [x] Build 102 processed — _Ready to Submit_, expires in 90 days.
- [x] Internal group **Skipper** (Shane) with 102 — no review. Tester sat at NOT_INVITED (no email, TestFlight app empty; remove/re-add did not help) until an invitation was created through App Store Connect's own API from the logged-in session (HTTP 201) → state INSTALLED, 2026-09-06.
- [x] External group **Beta Skippers** with 102 submitted to Beta App Review 2026-09-06 10:03 AEST (_Waiting for Review_). Its tester shows "No Builds Available" / NOT_INVITED — expected until Apple approves; auto-notify is on, so the invite sends itself. **Public link OFF** until the device matrix below passes. Funnel: Founding Skippers approval emails carry the link.
- [x] Test Information: contact, OTP review notes, privacy URL; age rating 13+ (UGC + messaging, Terms say not for under-13s).
- [x] Build 103 processed 2026-09-07 ≈13:10 (about ten minutes after upload) — _Ready to Submit_, expires in 90 days, in internal group **Skipper** only; Beta Skippers still holds 102 (_Waiting for Review_). No processing email arrived for 102 or 103: every notification on Shane's App Store Connect user was off ("App Status Reports" = No Countries or Regions). Fixed 2026-09-07 ≈13:30 at Shane's request: App Status Reports and Agreements set to World, the weekly analytics / developer response / App Store featuring emails ticked, saved and verified after a reload; mail goes to captain@serene-summer.com. Financial Reports, Payments and Tax Invoices remain unset (the auto-mode classifier refused those three; they only matter once there is a paid product). Until a 104 email proves the setting, the signal is still the TestFlight app on the phone or the iOS Builds page.

### Bugs found during the matrix (2026-09-06)

Process: reproduce → find the cause in code or production → fix with a test that would have caught it → ship (edge function deploy, or the next build) → re-check on the phone. Recorded here so the evidence trail shows what beta found.

- [x] **Public Voyage Log shows "the video is still making its way ashore" for a clip that landed.** Photos through, video not. Cause: `diary-video` went private on 2026-09-04 (20260904120000) but `voyage-log` still passed the row's public-bucket URL through; production answers 400 "Bucket not found" and the page reads any non-OK as "not ashore yet". Fix: `publicVideo()` signs the clip for the entry's owner exactly like photos. Ships with `supabase functions deploy voyage-log`; no app build. Re-test: Check again on the public page → the clip plays.
- [x] **102 upgrade deleted the local vessel database.** See the upgrade-path item below; fix c95b5ade, ships in build 103.
- [x] **The weather followed the phone off the boat.** Driving from Scarborough dragged the Glass and Obs weather along; the GPS follower, the boot path and the orchestrator's "Current Location" all read the phone. Now `services/weatherPosition.ts`: bus → Pi (u-blox inside) → the boat's held last fix → phone last. The Glass status strip says which ("Boat GPS · live", "Boat's last fix · 3h ago · tap to change", "Phone GPS"), and a centred boat-or-phone modal appears once per hold when the phone is ≥ 2 NM from her fix; dismissing it keeps the boat. Ships in build 103. Re-test: leave the boat quiet, drive off → the Glass holds Scarborough and the modal offers the phone.
- [x] **Tide station name and graph did not follow the position.** Reported 2026-09-06 14:30: the station arrived only with the 30 NM forecast refetch while the label renamed at 0.5 NM. Fixed 8908491b: a tide-only refresh every 3 NM (`TIDE_REFRESH_NM`), 24 h cached. Ships in 103.
- [x] **Apple Music had no off switch on its page.** Only the floating bar's X stopped it. Fixed 8908491b: a STOP chip beside SPEAKER (stop = pause + clear the queue). Ships in 103.
- [x] **Radio Console transcript ran off the bottom.** Re-laid out 2026-09-06: no page scroll; the transcript owns the middle box and scrolls inside itself; LAT/LON and SOG/COG/UTC sit under it; the channel line (VHF Ch 16 / DSC Ch 70; HF DSC 8414.5 / 6312 / 4207.5 kHz, voice 8291 / 6215 / 4125 kHz) and the three call buttons are pinned 8 px above the tab bar and never move. Ships in 103. Re-test: Distress mode with MOB nature on the phone — everything visible without scrolling.
- [x] **Ship's Log inconsistencies** (2026-09-06 14:23 screenshot) — fixed same day, ships in 103 (+ `supabase db push` for 20260906150000): day rows and the Today group now read the gated total the card uses; day's runs only for passages ≥ 24 h, labelled by dates sailed; LIVE only while recording; recorded speed = receiver SOG (or a gate-confirmed hop), so jitter no longer reads 7.4 kts; per-leg distance kept to 4 dp; a track whose whole footprint fits in a 150 m box prunes at stop and at load (the boat on the hard). Original finding: 0.1 nm on the card vs 2.6 NM in the day rows (gated cumulative vs raw per-leg sum, plus 0.01 NM rounding that inflates jitter legs); "Day 1 05 Sep" on a same-day track (noon-to-noon windows labelled by window start, panel shown for any track crossing noon); LIVE badge means "dated today", not tracking; 7.4 kts max is jitter speed (the accrual fix gated distance only); tiles read zero because the track is classed on land (by design); 0.1 NM sits above the 0.05 empty-track prune. Fix list awaiting Shane's call.

- [x] **Skipper Device card was hard to read.** Re-laid 2026-09-06: vessel name on top, the GPS order beneath (Boat GPS › This device, or just This device), the button says "Press to make this the Primary Device" / "Release — this is not the Primary Device", a faint emerald/cyan edge; same 120 px footprint. Ships in 103.

### Matrix on 103 (2026-09-07)

- [x] **Upgrade check 102 → 103 passed** (in-place update from the TestFlight app, ≈13:05; Shane: "upgrade is great"). The check 102 failed on 2026-09-06 now passes.
- [x] **The Glass status strip grew a second line ("PHONE GPS").** The 2026-09-06 receiver line sat under the badge row; Shane: "we have no spare real estate to add lines to the page … if you want to put just phone or vessel in between that is fine. but it needs to go back to how it was." Fix (same day): the strip is one row again — badge · forecast age with a single word, PHONE or VESSEL · model pill; while a held fix is in force the middle is a button that re-opens the boat-or-phone question; the full description ("Boat's last fix · 3h ago") lives in the accessible name only. Tests: `tests/StatusBadgesOneRow.test.tsx` (render: three children, no sibling line, tap re-opens), and the source contract in `tests/BoatWeatherFollow.test.ts` now forbids the full-width line. Ships in 104. Re-test: the Glass footer reads `INSHORE · 1m ago · PHONE · ICON` on one line.
- [x] **Skipper Device card reads right ("Primary: the Pi", claim button gone), but the banner said "CALYPSO PUBLISHES THE BOAT · PHONES STAND DOWN" and overflowed the card.** calypso is the Pi's hostname, not the boat, and every other boat's Pi has another name. Fix 16ed590f: "the Pi" in every user-facing string that carried the hostname (the banner, which now wraps inside its fixed height; the NMEA Gateway row; the gateway page badge; the Instrument Panel's Remote diagnosis). Ships in 104.
- [x] **The Ship's Log recorded the phone's drive — 13.3 NM, 1 h 40 m, 1063 entries, "— → Newport" — while the boat sat on the hard** (Shane 15:16: "it should be vessel primary gps, vessel secondary gps, and lastly phone gps"). Cause: the 2026-09-05 rule made the phone wait three minutes for a DEAD boat GPS, but from a phone at Newport her GPS merely looked dead (no socket, no LAN) while the Pi published her every five seconds. Fix e336bd05: while tracking, the manager asks for her every 10 s — the Pi direct, then her cloud row — and a fix under a minute old is the boat: it is recorded as the track (below the bus, above the phone), it keeps the vessel GPS "alive" so the phone is refused, and a phone more than 300 m from her fix of the last ten minutes is refused as not aboard even once she goes quiet. Log entries take the same order. The Log page says "Phone GPS held — her GPS is alive through the cloud · phone 1.6 NM from her. The log follows the boat." Ships in 104. Re-test: tracking on at home with the Pi publishing → the day's voyage is her dot at Scarborough (pruned as empty at stop), the amber line shows, and the car is nowhere in it. The 13.3 NM voyage already recorded can be deleted from its card's menu.
- [ ] Observation, not yet a fix: the Glass read PHONE at Newport while the Pi was publishing. Two reasons — the phone had not seen the boat since the hold landed (nothing held yet), and the weather position chain does not read the Pi's cloud snapshot. Proposed order for 104: bus → Pi on the LAN → Pi's cloud row → held last fix → phone. Listed under 104.

### Matrix on 104 (2026-09-08)

- [x] **A trip is headed for its first hop, not its destination.** Passage Planning's saved routes: a Newport → Whitsundays passage read "Newport - Mackay". `groupTracesByTrip` named the trip after leg 1; fixed 287cea44 to first origin – final destination (the rollup's rule): "Newport - Whitsundays (2 legs)". Ships in 105.
- [x] **The NMEA Gateway flashed "NMEA error" / "Connection failed" at home while the Pi was publishing** (Shane: "if we have a pi at the vessel, then nothing should try to connect"). Cause: the policy's fallback treated "the LAN cannot see the Pi" as "the Pi is silent" and opened the saved gateway socket after a minute. Fix 287cea44: the policy asks the cloud (every 30 s while the LAN is silent) before any socket — a Pi publishing anywhere is alive — and closes a fallback socket once she is seen in the cloud; AvNav's network scan and Smart Polars go through the policy too; the gateway page rolls its direct-connection controls up behind "Gateway settings — only if the Pi is down" and shows no failure while a Pi is paired. Ships in 105. Re-test: at home with the Pi publishing, the gateway card reads "Away · via the Pi" and stays quiet for as long as you care to watch.

- [x] **"I do not like the messages that are popping up about what gps we are using."** The PHONE/VESSEL word on the Glass strip and the amber "Phone GPS held" sentence on the Log page are gone (13dea7b1). In their place one glyph in the page header beside the ℹ button: a little boat or a phone with a GPS dot — emerald live, sky through the cloud, amber for her held last fix (tap it to choose boat or phone). The words survive only as accessibility labels. Ships in 105.
- [x] **The cast-off "Following a route?" sheet showed two of the passage's three legs.** Two causes closed in 453135a6: the there-and-back fold treated a homeward leg as the reverse of the outbound one and folded the last leg into its own return (trip legs are never folded now); and a leg saved in Route Tracer but never mirrored into the log had no row at all (it now appears under its passage, disabled, "Not in the log yet — open it in Route Tracer and save it"). Ships in 105. Re-test: slide Start Tracking → the Newport – Whitsundays passage lists all three legs; if the third reads "Not in the log yet", open it in Route Tracer and save it once.

- [x] **Skipper Device card still confusing with the Pi primary: "Primary: the Pi" on the top line, then "This device" on the next.** Fix f879b712: the GPS-order row shows only when no Pi is primary (empty row, same height, so the 120 px card never moves); the pill reads "The Pi is the Primary Device". Ships in 105.

- [x] **Settings → Advanced: the Calypso and Calypso's Knowledge sections** removed (27b30549; "we no longer have calypso any more"). Ships in 105.
- [x] **ℹ System Status: the "Last flight · suspended kill" block** removed; **the GPS glyph moved in** as a Position row (boat/phone with its fix dot, the sentence beside it) instead of a header chip; the ℹ button is now always present so the row can always be found (27b30549). Ships in 105.
- [x] **The chart's layer menu folds itself after 8 s without a touch** (27b30549); a touch inside restarts the clock. Ships in 105.

### Also in 103 — the Pi as primary device (agreed 2026-09-06 evening, built that night and 2026-09-07)

Planned and written up as 104; the 103 bundle was rebuilt after each of these landed, so they ship in 103 and the matrix below covers them too.

- [x] **The Pi is the primary device when she's aboard** (built 2026-09-06 evening, ships in 103). The Pi posts a live snapshot of the whole bus every 5 s to `telemetry-relay` (pairing credential, skipper's internet policy, five-minute backoff) → one row per skipper in `vessel_telemetry`. While that row is under a minute old the Skipper Device card reads "Primary: the Pi" and replaces the claim button with "calypso publishes the boat · phones stand down"; the public page's live marker prefers the Pi's snapshot when it is fresher than the phone's last track point. Ship's log track recording stays on the phone for now. **Deploy:** `supabase db push` (20260906170000), `supabase functions deploy telemetry-relay voyage-log`, then on the Pi `cd ~/thalassa-marine-weather && git pull && cd pi-cache && ./redeploy.sh`. Verify: the row appears in `vessel_telemetry`; `THALASSA_TELEMETRY_PUBLISH=0` in pi.env switches it off.
- [x] **Crew see the Instrument Panel anywhere, no VPN** (built 2026-09-06 evening, ships in 103). Shane's order for the phone: a) the gateway socket — boat LAN, or the same LAN over Tailscale — always wins; b) the cloud row, read every 5 s (60 s on a satellite link) while the panel or the Skipper card is open and fed into the same instrument store, the header saying "Remote · calypso reported 6 s ago"; c) nothing older than a minute is shown. RLS: the skipper, `boat_members` and accepted `vessel_crew` read; only the relay writes. Re-test: Marta's phone, invited as crew, opens the Instrument Panel off the boat.
- [x] **The Pi keeps its internet gate open across restarts when the operator says so** (built 2026-09-07). `THALASSA_PI_WAN_UPLINK=ordinary` in the Pi's `.env` declares a 4G/ordinary uplink and the diary + telemetry relays no longer stand down after a reboot until a phone reconnects; `satellite` pins the gate shut; undeclared keeps the fail-closed default. The app's own policy pushes still apply. Serene Summer's `.env` carries the line. Verified 2026-09-07: the Pi was redeployed at 07:13 and again at 12:24; each boot banner reads "WAN uplink: ordinary (declared) — internet gate stays open across restarts" and the publisher logged `sent` one second later with no phone on the LAN.
- [x] **NMEA Gateway card and page say how the boat is being read** (built 2026-09-07, ships in 103): "Connected · instruments & AIS" aboard, "Away · reading her via calypso" when the cloud feeds the panel, "connect when aboard" otherwise. The socket remains the best source aboard; nothing connects to the Pi directly for instruments any more.
- [x] **Ship's clock keeps the boat's time** (39c090dd, ships in 103): the zone picker defaults to "Ship's position", following the weather's boat position; the phone only while none has been seen.
- [x] **A second phone on the same account could not see, and could overwrite, the route the first phone was following** (Shane 2026-09-08: "this is where we probably need the skippers device to be god"; "fix it the way that you recommend"). Design chosen from a judge panel: author-stamped follow hydration, no leases, no refusal triggers. Built 0d46f902 (foundation) + f21eaf4c (UI), ships in 105. `voyage_plan_links` rows now carry `device_id`/`device_name`/server `updated_at`; `voyages` carries `recording_device_id/_name` (stamped by the device whose Cast Off activated the row). Every device reads the account's active voyage + link: the Log page shows an "Under way on another device" card — who records, which route is published, who set it — with "Record here too" and "Change the route". Any replace of another device's route lands in a centred confirm naming that device and the time ("Replace" / "Keep theirs"); a stop only clears a link this device set; a queued link that would overwrite the other phone's is dropped on reconnect and reported inline. Settings → Voyage Log shows "Set by <device> · HH:MM". End Voyage, the cleanup sheet and the Vessel hero name the recording device (cautions, not blocks). Also fixed on the way: releasing the Skipper Device claim wrote `undefined`, which JSON drops, so the release never reached the cloud — it writes `null` now; CrewService minted its own device id under the same key — one id now; a route fetched onto a second device arrives with its trip chain and planned mirror. "Skipper is god" ships DARK behind `settings.routeAuthority` (`'confirm'` default | `'skipper'`) — switching it on is Shane's call. **Deploy:** `supabase db push` (20260908150000) — until applied, link writes fall back to the unstamped shape and reads still hydrate. **Two-device smoke (105):** (1) phone A casts off Newport → Whitsundays with Show on the Public Page ticked; (2) phone B, same account, opens the Log page → card reads "Recording on <A's name> · Following Newport - Whitsundays … · set by <A's name>" and the route line is on B's chart; (3) B taps Change the route, picks a day sail → confirm names A and the time → Keep theirs → nothing changes on the public page, B's chart keeps the day sail, inline note says so; (4) repeat with Replace → public page swaps, A's Settings → Voyage Log shows "Set by <B's name>"; (5) B taps Record here too → B tracks the same voyage, the card disappears, only the claim holder publishes; (6) B stops its recording → A's route stays on the public page; (7) A ends the voyage → B's card clears within a minute; (8) Vessel → Skipper Device → Release on A → B's card shows nobody holds the claim within a minute (the null-release fix).
- [x] **The weather follows the punter; the boat is a saved location** (Shane 2026-09-08: "the weather should always be the punters location, BUT in the saved locations, there should be one that has the vessel name as a special saved location … implement whatever you think is the best way"). Supersedes the boat-first default of 76bfface. 'Current Location' now follows a FOLLOW TARGET, per account on the device: the phone by default (her receivers only as a fallback for a phone that cannot answer); the boat when her row — named after the vessel, pinned above Current Location in the ★ menu with a boat glyph and a BOAT tag — is picked (bus → Pi → cloud row → her held fix with its age; the phone only when no boat has ever answered on this device). Picking Current Location puts it back on the phone. Nothing asks any more: the boat-or-phone dialog survives only as the ℹ panel's door while a held fix shows, and its answer throws the same switch. No fix from her yet → an inline note under her row (no toast), the choice stands, and the follower moves the weather to her when she reports. Glass page touched with Shane's word (the ★ menu row + the resolver); the header strip is unchanged. Re-test on 105: at home with the Pi publishing, the Glass opens on Newport's weather with a phone glyph in ℹ; ★ → Serene Summer → Scarborough's weather, boat glyph; ★ → Current Location → Newport again.

### Next build (104) — what is actually left

- [x] **104 build preparation** (2026-09-07): build number 103 → 104 on all four configurations, marketing version remains 1.2.0, source commit 566aa1a4. Initial `ship:beta` completed under Node 24.19.0 at ≈16:36 with `VITE_APP_BUILD=104`: 132 preflight + 140 artifact contracts, production routes, bundle budgets, secret scan and Capacitor sync all passed. Bundle total 13.20/18 MB, JS 9.76/9.90 MB; copied iOS web assets match `dist`. The public-status tests now exercise the Barometer selector and actual sharing/expiry withdrawal. The nested sign-in test now checks the intended email-field autofocus and verifies both Close and Escape restore the outer action. No production focus behavior was changed or test quarantined. These are packaging/test checks, not a signed archive or device evidence; rerun validation against the final source commit before archiving.
- [x] Archive, validate and upload **104** after its final checks pass — completed 2026-09-07; see the final archive/upload receipt above. Processing and internal-group availability still require verification; Beta Skippers and its public link were not changed.
- [ ] The Pi records the Ship's Log track itself (today the phone still records; the Pi only publishes the live snapshot).
- [ ] Verify a claimed phone that leaves the boat while tracking cannot publish the skipper's position as the boat's.
- [ ] Realtime subscription on `vessel_telemetry` instead of the 5 s poll (the table is already in the publication).
- [x] **The Glass status strip is one row again** (2026-09-07; see Matrix on 103).
- [x] ~~The weather position chain reads the Pi's cloud snapshot before the held fix and the phone~~ **Superseded 2026-09-08 (see below): the phone is the default and the boat's order applies only when her ★ row is picked.** Originally: the weather position chain reads the Pi's cloud snapshot before the held fix and the phone (bus → Pi on the LAN → Pi's cloud row → held → phone) — from the PHONE-at-Newport observation on 103. Built 76bfface: `cloudFix()` is a weather-only rung; Anchor Watch and the Ship's Log never take it. Re-test: ashore with the Pi publishing, the Glass footer reads `… · VESSEL` and the weather is Scarborough's, not Newport's.
- [x] **Instrument Panel is invite-only** (Shane's call 2026-09-07 ≈13:20: "both now"; built b6b6ef33). A "Share instrument panel" toggle on the crew invite, off by default, stored on the crew row; the `vessel_telemetry` read policy checks it; the skipper can flip it per crew member afterwards; a crew member without it sees "ask the skipper to share the instruments", not a blank panel. The public page is untouched. It is the 'instruments' register ("Share Instrument Panel", first on the invite modal and the crew access form) → `vessel_crew.permissions.can_view_instruments`; migration 20260907140000 rewrites `vessel_telemetry_crew_reads` and drops the boat_members grant. **Deploy:** `supabase db push` — DONE 2026-09-07 ≈15:00 (Shane; `supabase migration list --linked` shows remote 20260907140000). From here an accepted crew member reads the live snapshot only with the share ticked — on 103 a crew phone reads nothing until 104 gives the skipper the switch. Re-test: invite Marta with the share unticked → her panel reads "Not shared"; tick it under Crew → her panel shows the boat within seconds, no reinstall.
- [x] **Pi-first instruments on the phone** (same call; built 8a6a2ec3). Order: the Pi over the boat LAN (pinned HTTPS lane), then the Pi through the cloud, then the direct Signal K / YDWG-02 socket only when no Pi is paired or the Pi has been silent for a minute (with hysteresis). The Pi feed carries what the socket carries today — GPS, AIS targets, depth, wind, the lot — because the panel, the chart's AIS layer, Anchor Watch, the Radio Console and the Ship's Log all read the one store. The direct socket stays first-class for boats without a Pi. Why: the YDWG-02 has three TCP slots and the Pi already holds two. Built as `GET /api/telemetry` on the Pi (snapshot + AIS off Signal K), `PiTelemetryService` (2 s LAN poll), `InstrumentSourcePolicy` (socket only after 60 s of Pi silence, closed again 30 s after the Pi returns, one switch per 2 min, manual Connect respected), `NmeaStore.isBoatFeed()` (the LAN lane counts as the boat's GPS; the cloud row never does). **Deploy:** Pi redeploy — DONE 2026-09-07 14:55:52 (Shane): banner "WAN uplink: ordinary (declared)", publisher `sent` 2 s after boot, and `GET /api/telemetry` on the Pi's loopback answers `available: true` with the whole bus (Scarborough fix, TWS 9.1 kt, 1017 hPa) and 17 AIS targets. Serene Summer appears in that list under her MMSI because her own transponder is heard back by her receiver — the same as the gateway's own-ship AIVDO today; the anchor radar and the AIS guard drop the MMSI in the vessel profile, so keep 503101240 set there. Re-test on 104: aboard on the boat Wi-Fi with Tailscale off → NMEA Gateway row "Aboard · reading her via the Pi", panel header "Live · Pi", AIS targets on the chart, no socket opened (the NMEA page shows the Pi-paired note); Pi off for a minute → the socket opens by itself; Pi back → within about two minutes the socket closes again.
- [x] **105 build preparation** (2026-09-08 ≈07:50): build number 104 → 105 on all four configurations, marketing version 1.2.0, source commit 23149108. `ship:beta` under Node 24.19.0 with `VITE_APP_BUILD=105`: 132 preflight + 140 artifact contracts, production routes, bundle 13.21/18 MB (JS 9.77/9.90 MB), secret scans and Capacitor sync (19 plugins) all passed; iOS web assets match `dist`. Full Vitest run earlier on the route-authority commits: 9,347 passed, 2 red — `FollowSteersCheckedGeometry` (a stale source pin from 453135a6, repaired in 10c97a3c) and `PublicVesselClock` (pins `publicInstrumentTimeZone(cloud, boatI…` in the voyage-log edge function, which 9509aca0 from the other session changed today — theirs to settle). Packaging checks only; Shane archives, validates and uploads.
- [x] **106 build preparation** (2026-09-09 ≈08:50): build number 105 → 106 on all four configurations, marketing version 1.2.0, source commit 7bcc62e6 (+ this bump). Two red lights on the way, both fixed in the bump commit: (1) `check:beta` failed its "AISHub fails closed" gate because that gate read the share card's fail-closed copy off the NMEA Gateway page, and 41e1c58f moved the card to Settings → Preferences — the gate now reads `components/settings/FleetSharingSection.tsx` and asserts GeneralTab mounts it (same copy, same rule); (2) CI on 7bcc62e6 was red in both `npm audit --audit-level=high` steps on freshly published advisories, none in our code — root lockfile patch bumps (`@xmldom/xmldom` 0.8.13 → 0.8.15 under @capacitor/cli's plist, `js-yaml` 4.3.1 → 4.3.2 under eslint, `fflate` 0.8.2 → 0.8.3, `@humanfs/*`), and `sharp` pinned to 0.35.4 by an override in `cloudflare-worker/package.json` (miniflare's transitive; wrangler unchanged at 4.118.0; worker `tsc --noEmit` clean). Both audits and both `npm ls --all` now exit 0. `ship:beta` under Node 24.19.0 with `VITE_APP_BUILD=106`: 132 preflight + 140 artifact contracts, production routes, bundle 13.27/18 MB (JS 9.82/9.90 MB), secret scans and Capacitor sync (19 plugins) all passed; bundle `main-DlzjODoM.js`; iOS web assets match `dist`. The CI steps that sat behind the failed audit were run locally with CI-identical commands: `check-deps`, `check:beta:source`, `lint` (incl. migration audit), `format:check`, `tsc --noEmit`, `deno fmt --check`, `deno check */index.ts`, `deno test` (106 passed) — all green. Full unit suite: CI on 36bc112d was red on two more pins that had sat behind the audit step — `BetaPrivacyContract` read the AIS consent copy off the NMEA Gateway page (moved to Preferences in 41e1c58f; test repointed) and `DisplayModeCoverage` found `text-amber-200/85` (crew roster) and `text-violet-200/60`, `/70` (float-plan invitee chips) without daylight rules (rules added in index.css) — fixed in 30b413e4. **Final source 30b413e4** passed [CI run 34289100675](https://github.com/shanestratton/thalassa-marine-weather/actions/runs/34289100675): all four jobs green including the full coverage suite; [CodeQL run 34289100656](https://github.com/shanestratton/thalassa-marine-weather/actions/runs/34289100656) passed. Rebuilt and Capacitor-synced from 30b413e4 with `VITE_APP_BUILD=106`: 132 + 140 contracts, bundle **main-1tCVx7dC.js**, 13.27/18 MB (JS 9.82/9.90 MB), 19 plugins, iOS web assets match `dist`. Packaging checks only; Shane archives, validates and uploads from 30b413e4.
- [x] **105 follow-up test gates** (2026-09-08): repaired the stale `PublicVesselClock` source pin after the clock lookup moved into the shared public instrument reader. The contract now checks both full and lightweight callers; two additional server behavior tests verify boat-position priority, private coordinates, stale-GPS rejection and the full response's already-public position fallback. `FollowSteersCheckedGeometry` also passes with its existing `10c97a3c` repair. Full Vitest under Node 24: **9,360 passed, 3 expected failures, 5 skipped; 1,067 files passed, 4 skipped**, no unexpected failures (232.44 s). Shared reader Deno tests: **6 passed**. TypeScript, focused lint and formatting checks passed. Tests/documentation only: no app or Pi behavior changes, and the prepared 105 bundle and Capacitor sync were not regenerated.
- [x] **Vessel claim, invite-only access and Release / Undo** (Shane 2026-09-08: "the only way a person can get access to the same vessel as someone who has already claimed it, is via the invite part. however we also need a way for a punter to release a vessel in case it has been sold, or they were just doing a delivery" → "fix it the way that you recommend"). Built 7fceaec9 by the resumed implementation workflow (12 agents; every slice adversarially verified, all pass or pass-with-fixes; 92 affected test files green; tsc clean; build + cap copy done). **For 106, not 105.** A hull is claimed by its 9-digit MMSI on the active `boats` row — hard refusal only at create/onboarding (`MMSI_CLAIMED` names the boat, never the owner; escapes "Enter crew code" / "Save without MMSI"), advisory on profile edits (amber "claimed by them" line; tri-state `mmsiClaimed`, null until the server says). `boats.owner_id` never moves: Release archives under the same owner, drops the hull's crew, unpairs only the Pi matched to the hull, clears telemetry, darkens the public page, frees the MMSI; the last boat also drops `vessel_crew`, pending codes and the skipper identity. Undo for 30 days. Bootstrap refuses to resurrect a released hull (`VESSEL_RELEASED`). Crew land on the live hull. Invite modal gains a role picker (Deckhand / Navigator / Co-skipper / Punter) and "Create a crew code". OfflineQueue re-sends a chunk refused for a released boat once with `boat_id` null. **Deploy — DONE by Shane 2026-09-08 ≈12:20:** read-only `supabase migration list --linked` shows remote **20260908170000** with no pending local migrations, and `supabase functions list` shows diary-relay **v14 ACTIVE**, updated 2026-09-08 12:22 AEST (after the push, as required). Original order for the record: (1) run the duplicate-MMSI query from the header of `supabase/migrations/20260908170000_vessel_claim_and_release.sql` — where two ACTIVE boats share an MMSI the oldest keeps the claim and the others lose it with only a NOTICE; (2) `supabase db push` (drops and recreates the five RETURNS TABLE fleet functions — a `--dry-run` or branch database first is prudent; nothing here ran against a real Postgres); (3) `supabase functions deploy diary-relay` — AFTER the push, or every pair call answers 503 until the column exists. Until the push, Release/Undo surface "function not found" as ordinary errors. **Decisions — APPROVED by Shane 2026-09-08 ≈11:05 ("they are all excellent ideas"); all shore-side, none at sea:** hard MMSI refusal at create/onboarding; Release refused offline or while this device is tracking; the bootstrap VESSEL_RELEASED guard (a skipper who genuinely buys another boat of the same name uses Add vessel or Undo); typed-name confirm for 'sold' only; email invites now carry the role preset (a default deckhand invite grants Ship's Stores + galley READ, a co-skipper invite grants Stores WRITE, without the tick). **Known limits, flagged by the verifiers:** releasing any hull forgets this phone's Pi pairing even when the surviving boat's Pi is the paired one; a multi-boat owner's Pi relay row keeps the boat_id stamped at first pairing; the crew code cannot carry Instrument Panel / Equipment / R&M / Documents; a released-boat 42501 on the live trickle stops the second device's public trail until stop (ephemeral); a legacy public config with `boat_id` NULL is not darkened by Release. **Two-account smoke for 106:** A claims Serene Summer with MMSI 503101240; B onboards with the same MMSI → banner, "Enter crew code" works; A invites B as co-skipper → B sees identity, panel, registers; A releases (sold) with typed name → result dialog counts; A's second phone opens → empty fleet with "You released…", no re-bootstrap; A taps Undo → boat back, releases again; B claims fresh; B's phone pairs the Pi, telemetry lands under B; A still opens old voyages and diary; A's public page dark, B's is serene-summer-2.
- [x] **105 route-author migration verified live** (2026-09-08): read-only `supabase migration list --linked` confirms remote **20260908150000** (voyage_plan_links author stamp + voyages recording device), with no pending local migrations. No database writes were performed during release preparation; two-device behavior still requires the physical smoke above.
- [ ] Whatever else the 103 matrix finds.
- [x] **Float plan crew from the invites** (Shane 2026-09-08: "can we scrape the names of crew from the invites. with the option of adding crew and deleting crew"). Built 37c7a8a2, for 106. The persons roster pre-fills when the sheet opens: the skipper first (byline row or onboarding name), then every ACCEPTED crew member named from the boat's byline table (prefix first "nickname" last, email local part as fallback) with the role mapped (co-skipper → First mate, navigator → Navigator, deckhand → Deckhand, punter → Guest). Pending invitees appear as "Add <name>" chips marked "invited, not yet accepted" — never assumed aboard. Delete per row and "+ Add person" as before; "Refresh from crew" re-reads names/roles and keeps typed rows and age/medical notes. People-aboard follows a crew-seeded roster until the stepper is touched. A failed read leaves the roster empty and editable (today's behaviour). Verified adversarially (one major fixed: refresh used to wipe typed age/medical). Smoke for 106: open the Float Plan with one accepted crew and one pending invite → skipper named, mate listed with role, invitee as a chip.
- [x] **Obs page: a thin blue line from the boat to the skipper's house** (Shane 2026-09-09, looking at the boat's weather from home). The vessel marker's wake trail joined every point the ownship arbiter painted, so a momentary Pi-lane dropout followed by a phone fix drew a chord from Scarborough marina to Newport and back. Fixed 1a5d217e: the trail and the anchor swing envelope belong to one receiver — the boat taking over from the phone clears what the phone drew; once the boat has spoken the phone moves the arrow but never draws her wake or widens her swing; a boat with no instruments keeps her phone-drawn wake. The chart's centre was already the Glass location box (initialCenter + the weatherCoords follow effect); the chord made it look as though the Obs page followed the phone. Re-test on 106: at home with the Pi publishing, open Obs → the arrow sits on the berth, no line to the house.
- [x] **NMEA Gateway page: position card vs ℹ panel disagreed; Pi card was YDWG-specific; "Share what you hear" moved** (Shane 2026-09-09). Fixed 41e1c58f: the position-source resolver knows the Pi lane ("On-board GPS · Live via the Pi"; her cloud row named as hers, "this phone steers by its own GPS"); the Pi card reads "Your Pi is paired, so this phone reads the boat's instruments through it. Nothing on this page connects on its own — the gateway settings are only for when the Pi is down."; the AIS crowd-feed consent card is now Settings → Preferences → "Share what you hear" (components/settings/FleetSharingSection.tsx), consent rules unchanged. Shane's direction: Preferences becomes the home for most toggles. Re-test on 106: at home with the Pi publishing, NMEA Gateway → Position source reads "On-board GPS · Live via the Pi", matching the ℹ boat glyph; Settings → Preferences shows the share toggle.
- [x] **Guardian: arm slider docked at the foot, alert feed grows and shrinks** (Shane 2026-09-09: "move the slide to arm vessel to the bottom of the screen (exactly 8px above the top of the menu bar) and make sure that the alert feed grows and shrinks to fit"). Built ce894b18, for 106. The slider left the scrolling column for a dock under it; the page's bottom padding is the tab bar + 8px so the dock sits exactly 8px above the menu bar. Hero, quick actions and nearby boats keep their size (the nearby list caps at ~13rem and scrolls itself); the alert feed is the flex region with a 120px floor and scrolls inside itself; on a very short screen the column scrolls as a whole. Re-test on 106: Guardian page → the slider is the last thing above the tab bar with an 8px gap; with 0 and 20 alerts the feed fills the space between the nearby list and the slider.
- [x] **Scuttlebutt: the Admin card arrived a beat after the channels and moved the row under the thumb** (Shane 2026-09-09: "i end up in the Crew list channel"). Built 6ed9bdc8, for 106. Channels painted from cache before the roles loaded; the channels view now waits for both (bounded by the existing 8 s init race; a failed init still shows the list), so the crown card and the channels land in one paint. Re-test on 106 as an admin: open Scuttlebutt → the skeleton, then the Admin card and channels appear together; tapping General straight away opens General.
- [x] **Obs: a green flag at Lady Musgrave for a route not plugged in; the passage under way is now opt-in on the chart** (Shane 2026-09-09). The flag was the destination of the route the phone was FOLLOWING ("Coral Sea" = the end of leg 1 of Newport → Whitsundays); a follow persisted for a week with no way on the chart to stop it. Built 838fbd89: the flag and its chip are tappable → centred "Following Newport → Coral Sea … Stop following / Keep following"; a day-old follow with no recording behind it is dropped at boot. Then Shane: "i would prefer not to have the current route showing on the obs page by default … the punter can add it in when he wants from the layer fab … they persist when you try to turn them off." Built 99501c4a: Active Voyage Mode used to force the active voyage's route, track and the followed flag onto the chart and re-apply them every 60 s and on every routes change, so a picker clear undid itself. One device-remembered switch now sits behind all three — OFF by default, the layer FAB's new "Passage" entry (shown while a voyage is active or a route is followed) turns it on; turning it off clears what it drew; a picker clear turns it off too, so it sticks. Routes/Tracks pickers unchanged. For 106. Re-test: with a passage active, open Obs → no route, track or flag; layer FAB → Passage → all three appear; Routes picker → Clear selection → the route stays gone; FAB → Passage off → chart clean.
- [x] **Settings: Aesthetics folded into Preferences** (Shane 2026-09-09: "move the entire aesthetics page to a section inside the preference page"). Built 5408b82c, for 106: Display Mode, Visual Preferences (Always On, Dim while always on) and Display Orientation render inside Settings → Preferences after Units; the standalone tab and its Appearance group are gone. Re-test: Settings → Preferences shows the three sections; no Aesthetics row in the menu.
- [x] **Vessel page: Diary and Scuttlebutt tiles parked under the fixed deck** (Shane 2026-09-09: "if you scroll upwards … get stuck under the 4 cards above them"). Built c03e03e3, for 106. The inner scroll port reserved the tab bar a second time on top of the root's reservation, giving a short page ~100px of dead scroll room; the port now keeps a small pb-4 and contains its own overscroll. Re-test: Vessel page with nothing expanded → flick up → the tiles come back under the four watch tiles with no gap; with Boat Binder expanded the last row still clears the tab bar.
- [x] **Vessel profile: a name, age and rank per person aboard, flowing into the Float Plan** (Shane 2026-09-09). Built ac245980, for 106. Under "Crew Aboard (incl. Skipper)" the Vessel tab renders exactly that many rows — name, age, rank (the Float Plan's roles; Skipper first) — saved on the vessel profile (`crewRoster`). The Float Plan seeds its persons roster from those rows first (named ones, capped at the count, ages carried, count follows), then offers only pending invitees as chips; "Refresh from crew" steps aside for a profile roster. Re-test: Vessel → Crew Aboard 2 → type two names with ranks → Cast Off → Float Plan opens with both rows and "2 aboard"; clear the names → the crew-list prefill returns.
- [x] **Vessel profile: the per-person rows stack** (Shane 2026-09-09: "just stack them claude. never enough space :)"). Built 62d93cc1, for 106. Each person aboard is a small card with name, then age and rank on the row beneath, instead of three fields squeezed across one line. Re-test: Vessel → Crew Aboard 3 → three stacked cards, every field fully visible on an iPhone-width screen.
- [x] **Instrument Panel: Bells page gone, Sea temp page added, helm dial flipped astern** (Shane 2026-09-09). Built 3d1645b6, for 106. (1) "get rid of the bells page" — the Bells section left the snap scroll; the clock stays and still strikes the half hours; Ship's bells on/off, Test and Clock zone are now Settings → Preferences → Ship's clock (`components/settings/ShipClockSection.tsx`, same two localStorage keys the panel reads, an event keeps an open panel in step). Rail: Clock · [Watch] · Wind · Barometer · Position · Speed · Depth · Sea temp · Heading · Helm. (2) "add a sea water temp page, with all of the beautiful trimmings" — a Sea temp section after Depth reads `state.waterTemp` ($--MTW / the Pi's water temperature) in the skipper's °C/°F, with a hull-sensor live/stale line, a Warming/Cooling/Steady pill over the 15-min record (six samples and ≥0.2 °C before it will say anything), Low/High/Change cards, the axis sparkline, and an honest "No water temperature on the bus" state. (3) "flip the helm dial … the rudder is at the back of the vessel" — the rudder arc, needle and readout now hang from the bottom of the dial (amidships at 6 o'clock), port still to port. Re-test on 106: Instrument Panel → no Bells page; Preferences → Ship's clock → flip bells on and Test → hear the bell, then open the panel and the clock's zone follows a change made in Preferences without relaunch; with the boat's MTW on the bus the Sea temp page shows the reading and a chart after a few minutes, with no water temp it says so; Helm → the needle sweeps across the bottom of the dial.

### Physical-device matrix (Distribution-signed build, from TestFlight)

iPhone:

- [ ] Fresh install → email OTP sign-in → onboarding shows once and only once.
- [ ] Sign out → sign in again on the same install → **no** onboarding (item 14
      fix: a returning account must never be walked through onboarding).
- [ ] Airplane mode at cold boot with a signed-in account → no onboarding, no
      wizard; dashboard shows the empty-state card. Back online → nothing pops.
- [x] Upgrade path: install the previous TestFlight build, create data, upgrade
      in place → vessel records, diary entries, ship log intact. **Item 10**: the
      vessel database moved out of Documents — after upgrade, open Files → On My
      iPhone → Thalassa: no `vessel_*.json` (the S-63 fingerprint and the
      enc-cells / offline*map_v1 / osm-overlay folders are expected there).
      \_102 FAILED this on 2026-09-06 (checks 1 and 2 passed): Capacitor's
      Filesystem resolves `Directory.Data` to Documents on iOS, so the 102 move
      found every file "already at the destination", skipped the copy and
      deleted the only copy; the sync then re-pulled the server's records, and
      anything edited offline since the last sync was lost. Fixed in
      LocalDatabase (`Directory.Library` + a same-folder guard that refuses to
      delete) for build 103 — re-run this check on the 102 → 103 upgrade.* **PASSED 2026-09-07 ≈13:05 on the in-place 102 → 103 update.**
- [ ] 103 first look, straight after the upgrade check: Account shows 1.2.0 (103); the Ship's Log no longer lists the on-the-hard 0.1 NM track (150 m footprint prune at load); Vessel → Skipper Device reads "Primary: the Pi" while calypso is publishing; the Glass status strip names its position source ("Boat GPS · live", or "Boat's last fix · … · tap to change").
- [ ] Anchor Watch controlled ashore test (audible check, arm, lock screen,
      background, acknowledge) — do not rely on it as the only alarm.
- [ ] MOB tile: "OVERBOARD" fully legible at Display Zoom → Larger Text.
- [ ] Apple Music: connect on device (MusicKit permission prompt appears),
      play a playlist from the helm, ask Bosun to skip a track. Confirms the
      developer token is issued by `musickit-token` (see 22 · MusicKit).

iPad:

- [ ] Install + launch; tablet split view (long-press The Glass) at ≥1024 pt.
- [ ] Rotation; keyboard does not cover sheet inputs.

Pi / NMEA (aboard):

- [ ] Pair over the pinned HTTPS lane; diary relay; chart sync; AvNav.
- [ ] Wind overlay + passage planner fetch wind through the Pi (GRIB cache).
- [x] ~~JWT reconciliation, Pi half~~ — no longer needed (2026-09-06). The
      dashboard's own toggle text recommends verify_jwt **OFF** with auth in
      the function; all four Pi-called functions guard themselves and stay
      off. Nothing to check aboard.

Cloud telemetry (off the boat, no VPN):

- [x] Tailscale OFF on the phone, away from the boat LAN: the Instrument Panel reads the Pi through the cloud — **PASSED 2026-09-07 ≈13:30 on 103** (Shane: "the nmea instruments work with tailnet off, whoo hoo"). On 103 the row and header still name the hostname (calypso); 104 says "the Pi". Still to try: Tailscale back ON aboard → the gateway socket wins and the header returns to it.
- [ ] Marta's phone, invited as crew, opens the Instrument Panel off the boat and sees the same — no VPN, no configuration.
- [ ] Ship's clock on "Ship's position" shows the boat's zone and stays there when the phone's zone is changed (Settings → General → Date & Time, Set Automatically off).

Offline / reconnect:

- [ ] Airplane mode mid-passage: chart, instruments, ship log keep working;
      entries queue. Reconnect → queue drains, no duplicates.
- [ ] Satellite Mode on (Account → Satellite Mode): confirm in Safari Web
      Inspector network tab that **no** GRIB / radar / AIS / vessel-metadata /
      offline-download / diary media requests are made for 5 minutes of normal
      use; forecast JSON still refreshes.

Authentication and deletion (disposable Apple ID + disposable account):

- [ ] Native Sign in with Apple: fresh authorisation, retained-token
      registration, `/auth/revoke` on sign-out.
- [ ] Typed-confirmation account deletion from Settings → Account.
- [ ] Interrupt the deletion (kill the app mid-way) → relaunch → it resumes and
      completes; the account is locked meanwhile.
- [ ] After deletion: cannot sign back in as that user; Storage has no objects
      under the user id (photos, audio, **diary-video** — added to the inventory
      2026-09-05); local caches cleared or the app says uninstall clears them.
- [ ] Other users' rows survive: a voyage where the deleted user was weather
      master still exists with `weather_master_id` null (item 3 fix).

---

## 19 · OVERBOARD wrap — done in code (`161d6060`)

- [ ] Visual check on device at Display Zoom → Larger Text (covered above).

---

## 20 · Background push contract

Facts: `Info.plist` declares `remote-notification` in `UIBackgroundModes`, and
both `send-push` and `send-anchor-alarm` set `content-available: 1`, but
`AppDelegate.swift` has no `didReceiveRemoteNotification:fetchCompletionHandler:`
— nothing ever runs in the background on a push. The code change removes the
unused declaration and flag rather than adding a handler nobody needs (an
alert push does not need `content-available` to be delivered).

- [ ] After the code change ships: a real anchor alarm push still arrives on a
      locked phone as a Time Sensitive alert and routes to the Anchor screen on
      tap.
- [x] Both Functions redeployed (`send-push`, `send-anchor-alarm`) on
      2026-09-06 — `verify_jwt` pinned `false` in `supabase/config.toml`.

---

## 21 · Production diagnostics

Facts: Sentry (`@sentry/react`) tags releases `thalassa@<VITE_APP_VERSION>` only;
production builds have `sourcemap: false`; there is no native Sentry SDK, so
iOS crash dSYMs are not collected anywhere but Apple.

Code side (in the repo): release becomes `thalassa@<version>+<build>` with the
commit SHA and platform as tags, and production emits **hidden** source maps.

You:

- [ ] Add `SENTRY_AUTH_TOKEN` (org: Thalassa, project: thalassa-web) as a CI
      **secret**, and `SENTRY_ORG` / `SENTRY_PROJECT` as vars, so the upload
      step runs. Without the token the build still succeeds and simply skips
      the upload.
- [ ] Set `VITE_APP_BUILD` in CI vars to the same number as
      `CURRENT_PROJECT_VERSION` for each archive (or wire it from the pbxproj).
- [ ] dSYMs: in Xcode Organizer → the uploaded archive → **Download Debug
      Symbols**, then either upload to Sentry
      (`sentry-cli debug-files upload --org … --project … <archive>.xcarchive/dSYMs`)
      or accept that native crashes are read only in App Store Connect → Crashes.
      Decide once and record it here.
- [ ] Confirm one deliberate test error from a TestFlight build appears in
      Sentry with the right release, build, commit and `platform: ios` tag.

---

## 22 · App Store compliance

### Privacy labels (App Store Connect → App Privacy)

Reconcile against what the app actually collects. Ground truth per data type:

| Data type                       | Collected?                                    | Linked to user                            | Used for tracking | Source of truth                   |
| ------------------------------- | --------------------------------------------- | ----------------------------------------- | ----------------- | --------------------------------- |
| Precise location                | Yes                                           | Yes (voyage/ship log, anchor, float plan) | No                | `services/shiplog`, `GpsService`  |
| Coarse location                 | Yes (0.25° wx cell, anonymous)                | No                                        | No                | `services/weather/wxPublished.ts` |
| Email address                   | Yes (sign-in)                                 | Yes                                       | No                | Supabase Auth                     |
| Phone number                    | Last 4 digits + fingerprint only (Crew List)  | Yes                                       | No                | Terms §Crew List; Twilio Verify   |
| Photos / videos                 | Yes (diary, recipes, profile)                 | Yes                                       | No                | `DiaryService`, Storage buckets   |
| Audio                           | Yes (diary voice, Bosun)                      | Yes                                       | No                | Deepgram token proxy              |
| User content (chat, diary text) | Yes                                           | Yes                                       | No                | `chat_messages`, `diary_entries`  |
| Crash data / diagnostics        | Yes (Sentry, PII stripped)                    | No                                        | No                | `services/sentry.ts`              |
| Product interaction / analytics | No                                            | —                                         | —                 | none shipped                      |
| Advertising / tracking          | No                                            | —                                         | No                | none                              |
| Apple Music library             | Processed on device; **not** sent to Thalassa | —                                         | No                | `AppleMusicPlugin.swift`          |

- [x] App Privacy questionnaire completed in App Store Connect from the table above (2026-09-06).
- [x] Privacy Policy URL set to https://www.thalassawx.app/terms (v2.9) on the App Privacy page (2026-09-06).

### UGC moderation and support procedures (Guideline 1.2)

- [ ] Report a message: works from the message menu and lands in the report
      table (`reportMessage` in `ContentModerationService`).
- [ ] Block a user: available and hides their messages for the blocker.
- [ ] Moderation pipeline: after the item-5 change ships, a message from a
      throwaway account containing an obvious slur is **never seen** by a
      second device (fail-closed pending → rejected), and a normal message
      appears on the second device within ~2 s.
- [ ] Support contact published in the App Store listing and in-app
      (privacy@thalassawx.com is in the Terms; add a support address if
      different).
- [ ] **Reviewer sign-in for App Store review.** Sign-in is email one-time code (no password), so the App Review form's mandatory username/password cannot be filled truthfully. Before submission: a review-only mailbox whose codes you relay via the review notes, or a documented test account. (TestFlight beta review 2026-09-06 worked with "Sign-in required" unticked + notes explaining OTP.)
- [x] TestFlight Test Information saved 2026-09-06: contact, review notes (OTP explained), privacy URL; standard Apple EULA (License Agreement left blank).
- [ ] Written moderation procedure (who reviews reports, within what time)
      exists somewhere you can point Apple at if asked. One paragraph is enough.

### Rights

- [x] AIS redistribution: AISHub written permission (Desimir, 2026-09-02) recorded live in the `public_ais_enabled` column comment (20260905103000 applied 2026-09-05). Shane keeps the email.
- [ ] Charts: S-63 permits per cell; o-charts licence; OpenSeaMap attribution
      shown in the map attribution control.
- [ ] Weather: CC-BY-4.0 attribution for ECMWF/DWD/UKMO/JMA/Météo-France/NOAA
      wherever forecasts are shown or shared; Rainbow.ai, Stormglass,
      WeatherKit per their terms (WeatherKit attribution link required by
      Apple — check it is visible where WeatherKit data appears).
- [ ] Bathymetry: GEBCO attribution.
- [ ] Notices to mariners: AMSA / NGA / UKHO / LINZ source lines shown.

### MusicKit

- [ ] Developer portal → Identifiers → `com.thalassa.weather` → App Services →
      **MusicKit** enabled. (Not an `.entitlements` key; it is an App ID
      service plus `NSAppleMusicUsageDescription`, which is present.)
- [x] Production secrets: `MUSICKIT_ENABLED=true` set and `musickit-token` v15
      deployed in that order on 2026-09-05 (verified 2026-09-06). Apple Music
      stayed up.
- [ ] Verified on a real device (see 18 · iPhone).

### Build number

- [x] `CURRENT_PROJECT_VERSION` = 102 on every configuration (0606365b), uploaded as 1.2.0 (102). `VITE_APP_BUILD = 102` in CI vars still to set (§21).
- [x] Bumped to 103 on every configuration, 2026-09-06 afternoon, for the matrix fixes (vessel database, boat-first weather, tides, STOP, Radio Console, Ship's Log, Skipper card, In irons / wing and wing). `VITE_APP_BUILD = 103` in CI vars still to set.
- [x] Uploaded as 1.2.0 (103) on 2026-09-07 ≈13:00. As packaged, the 103 bundle also carries the Pi-as-primary section (§18). 103 can never be reused; next bump is 104.
- [x] Bumped to 105 on every configuration, 2026-09-08 ≈07:50, source commit 23149108 (+ this bump). `VITE_APP_BUILD = 105` in CI vars still to set.
- [x] Uploaded as 1.2.0 (105) on 2026-09-08 08:27 AEST. 105 can never be reused; next bump is 106.
- [x] Bumped to 106 on every configuration, 2026-09-09 ≈08:50 (36bc112d); archive source is **30b413e4** (bump + gate/audit fixes + two test pins, CI green). `VITE_APP_BUILD = 106` in CI vars still to set.
