# Thalassa UI + Engineering Re-score — 2026-08-03 late night (HEAD 1781864a)

Post haversine-consolidation + safety-hook tests + UI round 2. Same harness, fresh finders.
Prior: Engineering 24.0 (evening), UI 20.5 (night). CI GREEN on the consolidation confirmed by auditors.

| Dimension               | Score     | Max | Note           |
| ----------------------- | --------- | --- | -------------- |
| **ENGINEERING**         | **25.5**  |     |                |
| build                   | 6         | 8   |                |
| test-health             | 6         | 8   |                |
| architecture            | 4         | 8   |                |
| DRY                     | 3.25      | 6   |                |
| consistency             | 2.75      | 5   |                |
| security                | 3.5       | 5   |                |
| **UI**                  | **18.75** |     |                |
| design-system adherence | 3.5       | 8   | raw 3, +0.5    |
| theming                 | 3.75      | 7   | raw 3.5, +0.25 |
| visual-consistency      | 5.75      | 8   |                |
| component-polish        | 5.75      | 7   |                |

## ENGINEERING — deduction ledger

### build — 6/8 (pre-verify)

- **−0.75** Dependabot pipeline is saturated and 100% red: 13 open PRs, every sampled PR failing checks, zero merged or triaged; the npm cap of 10 is full and actions PRs sit since June.
    - Evidence: gh pr list → PRs #8-#20 all OPEN (#8 since 2026-06-22, #9/#10 since 2026-07-20); gh pr checks on #20/#19/#12/#11/#10/#8 → 'check' job fail on all six plus Lighthouse Audit fail on all six; PR #11 check log (run 30789072236): workers/ais-ingest/index.ts(21,23) error TS7016 missing @types/ws — genuine breakage nobody is fixing; .github/dependabot.yml:14 open-pull-requests-limit: 10 (PRs 11-20 = at cap, new updates now blocked)
- **−0.5** Nine prod dependencies are dead-declared with zero references repo-wide (beyond the removed maplibre-gl/zod), plus three @types packages misplaced in prod dependencies.
    - Evidence: package.json:52 @google/generative-ai (services/geminiService.ts:45 comment: 'Replaces all direct @google/generative-ai SDK usage' — proxy replaced the SDK), :82 ngraph.graph, :83 ngraph.path, :92 uplot, :75 kdbush, :71 class-variance-authority, :60 @turf/bezier-spline, :65 @turf/point-grid, :62 @turf/buffer — grep -rF over all \*.ts/tsx/js/mjs/cjs excluding node_modules/dist/ios-public returns 0 hits for each; @types/leaflet:67, @types/mapbox-gl:68, @types/rbush:69 declared in dependencies not devDependencies
- **−0.25** Nine known vulnerabilities still standing (6 low, 2 moderate, 1 high 'tmp' symlink write), all in the @lhci/cli dev chain, unchanged since the evening audit.
    - Evidence: npm audit at HEAD 1781864a → '9 vulnerabilities (6 low, 2 moderate, 1 high)'; high = tmp GHSA-52f5-9888-hmc6 via @lhci/cli→inquirer→external-editor; only fix offered is breaking downgrade @lhci/cli@0.1.0; CI only warns at high (ci.yml:39-40)
- **−0.25** legacy-peer-deps=true still pinned repo-wide and mirrored in all three installing workflows, masking peer-dependency resolution on every install.
    - Evidence: .npmrc:1 legacy-peer-deps=true; .github/workflows/ci.yml:33, lighthouse.yml:31, preview-smoke.yml:36 all run npm ci --legacy-peer-deps
- **−0.25** A release-candidate package still ships in prod dependencies of a life-safety navigation app.
    - Evidence: package.json:47 "@codetrix-studio/capacitor-google-auth": "^3.4.0-rc.4" — RC pin in dependencies, unchanged since prior audits

### test-health — 6/8 (pre-verify)

- **−0.75** The coverage ratchet is 19-25 points stale: measured full-suite coverage is ~44-45% while the floors sit at 25/20.5/20.5/25 with a config comment claiming they sit 'just below the verified baseline'.
    - Evidence: HEAD CI run 30791179481 coverage step: 'All files | 43.76 | 38 | 44.13 | 45.49' (prior run 30789257756: 43.77/38.01/44.1/45.5) vs vitest.config.ts:44-52 thresholds lines:25 functions:20.5 branches:20.5 statements:25 and comment 'raised to sit just below the verified full-suite baseline (25.33/20.97/21.00/25.29)'; git log -- vitest.config.ts → last touched 63e5ecaa 2026-07-23, so the gate now permits a ~45% relative coverage collapse without failing, against the file's own 'Raise as test coverage improves' policy
- **−0.5** Per-glob coverage thresholds remain absent for safety-critical directories — one global floor lets anchor-watch/MOB/shiplog code regress while aggregate stays green.
    - Evidence: vitest.config.ts:44-52 is the only thresholds block (global, no per-glob keys); no targeted floor exists for services/AnchorWatchService.ts, services/MobService.ts, or services/shiplog/\*\* despite the repo's own life-safety framing of these modules
- **−0.25** The documented 1-in-8 segmentRoute contention flake is still parked with no fix commit — master's green signal still carries a known-unreliable test.
    - Evidence: git log --oneline -3 -- tests/routing/segmentRoute.test.ts → a5c89db3, 7ab62e38, ee60af86 (all behavior changes, none since the evening audit, none a flake fix); docs/ROUTING_COLLAB.md reply 60 'rare single failure under MAX parallel contention… Watching; not touching' stands verbatim per docs/APP_AUDIT_2026-08-03_evening.md:50-51
- **−0.25** ~6.9k LOC of product code sits outside the coverage include globs entirely — unmeasured and unthresholded.
    - Evidence: vitest.config.ts:31-43 coverage.include lists services/hooks/components/utils/context/modules/managers/stores/data only; src/ = 2,409 LOC (incl. src/geo.ts navigation math and the voyage-log SPA) and pages/ = 4,458 LOC (LogPage stack) are absent from the globs; workers/ais-ingest excluded from this deduction as it carries its own vitest.config.ts and parser/db/watchdog test files
- **−0.25** The full suite remains runnable only in CI — the local inner loop cannot verify a change against the whole suite (OOM), so full verification is a ~12-minute remote round trip.
    - Evidence: Standing constraint (MEMORY: 'NEVER run the full vitest suite' locally, build/lint need heap prefix); CI full-suite runs take 11m46s (run 30789257756) / ~12m (run 30791179481); no sharded or memory-bounded local runner has been added

### architecture — 4/8 (pre-verify)

- **−1.75** God-module sprawl unimproved and slightly worse: MapHub grew to 5487 lines (was 5476 at prior audit), 26 components/pages >=1000 lines, 27 service files >=1000 lines, 78 components exceed the repo's own 500-line cap
    - Evidence: components/map/MapHub.tsx (5487 lines, wc -l at HEAD 1781864a); services/InshoreRouter.ts:3691, services/shiplog/OfflineQueue.ts:3094, services/DiaryService.ts:2869; components/VesselHub.tsx (2460), components/CrewManagement.tsx (2390), pages/LogPage.tsx (2264), components/music/MusicPage.tsx (2261), components/voice/BosunConsole.tsx (2156); cap at CONTRIBUTING.md:28
- **−0.75** 27 circular dependency chains (down from 30 — modest real improvement), including cross-domain cycles through the local database and the shiplog web
    - Evidence: madge --circular over 1036 files: 'Found 27 circular dependencies'; e.g. services/vessel/LocalDatabase.ts > services/ShoppingListService.ts > services/PassageProvisionsService.ts > services/GalleyRecipeService.ts; services/VoyageService.ts > services/ShipLogService.ts > services/shiplog/EntryCrud.ts > services/shiplog/RoutesAndTracks.ts > services/shiplog/PassagePlanSave.ts; components/CrewManagement.tsx > components/crew/ReadinessCardStack.tsx
- **−0.75** State management runs three live paradigms mid-migration: 8 Zustand stores, 4 custom pub/sub singletons, a real 1074-line WeatherContext still unmigrated, 18 services with hand-rolled listener sets, and 48 files still consuming via deprecated context bridges (vs 68 direct)
    - Evidence: context/WeatherContext.tsx (1074 lines, not a bridge — imports services/GpsService, stores/LocationStore directly); stores/LocationStore.ts:11 ('Zustand-like API without the dependency'), stores/PassageStore.ts:4, stores/WindStore.ts, stores/MapFitTargetStore.ts:2; grep: 48 files import context/ bridges marked '@deprecated' (context/AuthContext.tsx:11); 18 files in services|stores with 'listeners: Set' pattern
- **−0.75** File-organization drift plus genuine upward layering leaks: pages/ holds one page while 17 \*Page.tsx live under components/; context/ AND contexts/ both exist; two-file managers/ and modules/ straggler dirs; a hook lives in services/ and imports a component
    - Evidence: find: pages/ = LogPage.tsx only vs 17 \*Page.tsx under components/ (components/AnchorWatchPage.tsx, components/vessel/MobPage.tsx, ...); context/ (8 files) vs contexts/CrewCountContext.tsx; managers/ = 2 files, modules/ = 2 files; services/weather/useGribDownload.ts:13 imports '../../components/map/ThalassaMap'; context/WeatherContext.tsx:12 imports '../components/Toast'

### DRY — 3.25/6 (pre-verify)

- **−0.75** utils/fetchWithRetry.ts is still dead — its only consumer is its own test — while ~14 files hand-roll retry/backoff loops
    - Evidence: grep -rln fetchWithRetry (excl. node_modules/dist/ios): only tests/fetchWithRetry.test.ts and utils/fetchWithRetry.ts:31 itself; hand-rolled retry/attempt loops in services/ChartLockerService.ts, services/EncImportService.ts, services/CrewService.ts, services/AnchorWatchSyncService.ts, services/PiProvisionService.ts, services/SeamarkService.ts, services/shiplog/EntrySave.ts, services/voice/deepgramRecognizer.ts, services/weather/shelter/coastlineSource.ts, components/map/CurrentParticleLayer.ts, components/map/WaveParticleLayer.ts, components/map/WindParticleLayer.ts, services/weather/ResumableGribFetcher.ts, services/PiCacheService.ts
- **−0.75** 31 files carry hand-rolled TTL-cache boilerplate with no shared cache primitive in utils/
    - Evidence: grep -rlE 'cacheTTL|CACHE_TTL|ttlMs|TTL_MS' services utils = 31 files; no createCache/TtlCache/memoize export exists anywhere in utils/ or services/ (grep returned empty)
- **−0.5** Five independent toast implementations coexist; the three specialized ones share nothing with the global event-based Toast system
    - Evidence: components/Toast.tsx:2 (global event singleton), components/PushToast.tsx:2 (separate slide-down system), components/ui/UndoToast.tsx, components/dashboard/OffshoreBoundaryToast.tsx, components/map/PerfDowntierToast.tsx — import blocks of the last three contain no reference to Toast.tsx
- **−0.5** Haversine residuals outside the documented deferral categories: 9 inline great-circle sites in 3 files are neither migrated nor comment-documented, including 5 in usePassagePlanner — a file the consolidation commit touched yet left with zero navigationCalculations imports
    - Evidence: components/map/usePassagePlanner.ts:306-312 (inline destination point), :373-380 (inline haversine), :475-485, :853-858 (inline bearings); grep -c navigationCalculations = 0 in that file while git show 1781864a --stat lists it (10 lines changed); services/routing/TideAwareAnnotator.ts:93-111 (private haversineM + bearing, no deferral comment); services/routing/landBackstop.ts:85-90 (inline dist closure, no comment) — none are engine hot-loops or planar approximations
- **−0.25** The routing engine's ~25 hot-loop great-circle copies remain — deferred with a documented reason (golden-route re-baselining, own session), so discounted but not free
    - Evidence: git log 1781864a: 'DEFERRED on purpose: the routing engine's 25 hot-loop copies (golden-route re-baselining, own session)'; live sites e.g. services/InshoreRouter.ts:2094-2098 ('Local to the marker-pairing logic — the engine has its own copy'), services/engine/geometry.ts:16-22, services/isochrone/geodesy.ts, services/WeatherRoutingService.ts, services/cycloneAvoidance.ts

### consistency — 2.75/5 (pre-verify)

- **−0.75** ARCHITECTURE.md is ~5x wrong and 4.5 months stale: claims 67 components (actual 322), 31 hooks (actual 47), 26 edge functions (actual 47), and still diagrams the Marketplace deleted 2026-07-29
    - Evidence: ARCHITECTURE.md:8 ('67 components'), :9 ('31 Custom Hooks'), :10 ('65 Services' vs 155 top-level/385 total service files), :20 ('26 Edge Functions' vs 47 in supabase/functions), :119 ('Chat --> Marketplace'); git log -1 -- ARCHITECTURE.md = 108aabe2 2026-03-20
- **−0.5** CONTRIBUTING.md contradicts the codebase it governs: a 500-line component cap that 78 components violate (MapHub at 11x) and a 'Dark theme only' rule contradicted by 156 display-light CSS rules
    - Evidence: CONTRIBUTING.md:28 ('Max 500 lines'), :42 ('Dark theme only — all backgrounds should be slate-900/950'); find/wc: 78 components/pages files >=500 lines, components/map/MapHub.tsx = 5487; grep -c 'display-light' index.css = 156
- **−0.5** Naming-convention split across the largest directory: top-level services are 55 camelCase vs 100 PascalCase, and duplicate context/ vs contexts/ directories persist
    - Evidence: ls services/\*.ts (non-test): 55 files start lowercase (routeTracer.ts, inshoreRouterEngine.ts, geminiService.ts) vs 100 uppercase (InshoreRouter.ts, DiaryService.ts, ShipLogService.ts); context/ (8 files) coexists with contexts/ (CrewCountContext.tsx)
- **−0.5** Error-message normalization is scattered: 137 inline 'instanceof Error ?' sites while the shared getErrorMessage helper is used by only 7 files
    - Evidence: grep -rn 'instanceof Error ?' across components/pages/services/hooks/stores/utils (excl. tests) = 137 hits; grep -rl getErrorMessage (excl. tests) = 7 files; helper exported from utils/createLogger.ts (imported at stores/settingsStore.ts:19)

### security — 3.5/5 (pre-verify)

- **−0.5** pi-cache boat-LAN service is fully unauthenticated on 0.0.0.0: an open passthrough proxy accepts any URL with attacker-chosen TTL (LAN attacker can cache-poison weather/tide responses the app then trusts, or SSRF into the LAN/tailnet), and config/chart-install endpoints are equally open
    - Evidence: pi-cache/src/server.ts:273-289 (GET /api/passthrough — 'The app sends any URL here', no host allowlist, ttl from query), pi-cache/src/server.ts:201 (POST /api/configure unauthenticated), pi-cache/src/routes/enc.ts:933 (POST /install-from-url unauthenticated), pi-cache/src/server.ts:345 (listen on '0.0.0.0'); no Authorization handling anywhere in pi-cache/src. Tempered: sealed-appliance private-LAN model, Tailscale encrypts transit, Signal K ecosystem norm is identical — hence 0.5 not more.
- **−0.5** No systematic input validation at fetch boundaries: zod's removal was a dead-dep cleanup (it was never imported), and the actual posture is bare 'as' casts on response JSON with only ad-hoc per-site guards; the central inputValidation util claims five form surfaces but has exactly one consumer
    - Evidence: 34 occurrences of '(await res.json()) as T' across services/utils/hooks/components (e.g. services/OsmRouteOverlayService.ts:159, services/InshoreRouter.ts:2579, services/DiaryRelayTransport.ts:151); zod removal commit f15c5519 says 'zero imports repo-wide... never-adopted validation intention'; utils/inputValidation.ts:4 claims 'onboarding, vessel details, crew finder, marketplace, and diary forms' but grep finds a single consumer (components/OnboardingWizard.tsx). Partial credit kept: several sites do post-cast runtime normalization (services/localNotices.ts:54-57 Number.isFinite filter; services/MaritimeIntelService.ts:128-129 normaliseMaritimeArticles on unknown) and server-side bounds exist (supabase/migrations/20260724091000_chat_payload_bounds.sql).
- **−0.25** Supabase session including the refresh token persists to iOS UserDefaults (Capacitor Preferences), not Keychain — unencrypted plist, backup-extractable; the storage swap is documented solely as an availability fix with no confidentiality consideration
    - Evidence: services/supabase.ts:196-206 (storage: capacitorAuthStorage with persistSession: true) and services/supabase.ts:10-16 (comment: chosen because localStorage 'is evictable on iOS WKWebView' — UserDefaults named explicitly, Keychain never weighed).
- **−0.25** Residual client-hardening gaps: CSP keeps 'unsafe-inline' script-src plus bare 'http:' and 'data:' in connect-src (any cleartext host reachable, where 'http://\*.local' + explicit tailnet hosts could scope it), and the check-client-secrets gate never runs in CI — the CI build invokes vite directly, bypassing the npm-script chain that carries the check locally
    - Evidence: index.html:23 (script-src 'self' 'unsafe-inline'), index.html:27 (connect-src 'self' data: http: ...); .github/workflows/ci.yml:74 runs 'npx vite build' while the secret scan lives only in package.json:8 'build' and :14 'ship' scripts; scripts/check-client-secrets.mjs:21-41 is additionally a name-denylist (variable names, not values), so a literal pasted key or a new name passes.

### Verifier adjustments

**Group verdict:** ENGINEERING 25.5/40 CONFIRMED — all three auditors' scores stand unmodified (build 6.0/8, test-health 6.0/8, architecture 4.0/8, DRY 3.25/6, consistency 2.75/5, security 3.5/5; deduction sums reconcile exactly in every dimension). Every load-bearing count re-executed at HEAD 1781864a and verified: CI run 30791179481 completed SUCCESS (gh run list; CodeQL green); MapHub.tsx 5487 lines and all nine other god-module counts exact (wc -l); madge circulars exactly 27 with worktrees excluded (an unexcluded run shows 108 — the auditor's invocation was the correct one); npm audit 9 vulns (6 low/2 moderate/1 high) exact; 13 open all-red dependabot PRs confirmed including PR #11's failing check; coverage 43.76/38/44.13/45.49 read directly from the CI log vs vitest.config.ts:49-52 floors of 25/20.5/20.5/25 under a comment claiming they hug the baseline — the ratchet-staleness deduction is the lane's most defensible finding; 8 of 9 dead-declared prod deps have literally zero hits and @google/generative-ai's 2 hits are both comments; fetchWithRetry, 31 TTL files, 5 toast systems, 137 instanceof-Error sites, ARCHITECTURE.md staleness (322 actual components vs claimed 67, 47 edge functions vs 26, Marketplace ghost at line 119, untouched since 2026-03-20), and all security cites (pi-cache unauthenticated passthrough on 0.0.0.0 at server.ts:273-289/345, supabase UserDefaults session storage at services/supabase.ts:194-207, single inputValidation consumer, CSP unsafe-inline + bare http:/data: with unsafe-eval verifiably gone) reproduced verbatim. SPECIAL CHECK — haversine deferral framing ACCEPTED: the routing engine's deferral is genuinely documented (commit message names golden-route re-baselining as the reason) and genuinely golden-tested (tests/inshoreRouter.golden.test.ts + tests/inshoreRouter.scorecard-baseline.test.ts exist; InshoreRouter.ts:2096 carries an explicit 'the engine has its own copy' comment referencing golden-pinned positions), so DRY's discounted 0.25 charge is fair; however landBackstop.ts and TideAwareAnnotator.ts carry NO deferral comments and usePassagePlanner.ts (touched by the commit) retains undocumented inline great-circle math — the 0.5 residual charge is correct and the build auditor's 'documented deferred' framing of those two files is refuted as a characterization (score-neutral). No cross-dimension double-billing found: god-module size (architecture) vs doc contradiction (consistency), and dead-dep vs validation-posture (build vs security) bill distinct failures. Zero score adjustments; Engineering total 25.5/40, up from 24.0 same-day, with the gains (haversine consolidation, safety-hook suites, dead-dep removals) all verified real rather than claimed.

## UI — deduction ledger

### design-system adherence — 3/8 (pre-verify)

- **−1.5** Arbitrary type sprawl unchanged: 2005 text-[Npx] occurrences vs ~114 semantic consumers (12 t.typography.\*, 80 .text-label, 22 .text-micro) — a 95:5 ratio; 1420 of them are text-[11px] hand-duplicating the caption tier the token system already defines.
    - Evidence: theme.ts:275-300 (8-tier scale), index.css:150-159 (.text-micro/.text-label utilities); grep counts at HEAD 1781864a: text-[11px] x1420, text-[10px] x325, text-[9px] x73, text-[12px] x88 across components/ pages/ modules/ src/; only 12 t.typography.\* usages app-wide
- **−1.25** Component tokens exist but the app does not run on them: t.button has zero consumers vs 1055 raw <button> elements; theme.ts imported by 20 of 346 tsx files (5.8%); useTheme reaches 1 component; themeStore->ThemeContext reaches 3 — the dynamic dual-theme token object is near-dead code.
    - Evidence: theme.ts:383-392 (button tokens), theme.ts:439-441 (stale '56 files' claim, actual 20); grep 't.button' = 0 matches in components/pages/modules/src; components/chat/ChatHeader.tsx (sole useTheme consumer); stores/themeStore.ts imported only by context/ThemeContext.tsx
- **−0.75** No design tokens registered with Tailwind — theme.extend is empty, so the 8-tier fontSize scale is unenforceable by config or lint, and even theme.ts must emit arbitrary text-[12px]/text-[11px] values; 3 of the 5 CSS type vars (--text-body/--text-title/--text-hero) have zero consumers.
    - Evidence: tailwind.config.js:31-33 (theme: { extend: {} }); theme.ts:294-298 (token defined via arbitrary values); index.css:67-72 (vars declared), grep 'var(--text-body|--text-title|--text-hero)' = 0 matches
- **−0.5** Three contradictory typography sources of truth ship simultaneously: theme.ts (caption=11px, label=12px), styles/typeScale.ts (caption=12, body=13, plus hardcoded color #64748b in LABEL_STYLE), and index.css vars (--text-body:14px) — each file claims to be the single source.
    - Evidence: styles/typeScale.ts:2 ('Single Source of Truth'), :33-41 (SIZE caption=12/body=13), :49-57 (LABEL_STYLE hardcodes '#64748b'); theme.ts:275-300 (caption=11px); index.css:57-70 ('5 semantic sizes', --text-body:14px); typeScale has 5 consumers incl. components/Toast.tsx
- **−0.5** 403 type occurrences sit below the codebase's own documented 11px WCAG 1.4.4 floor (325 text-[10px], 73 text-[9px], 5 text-[10.5px]) and none fall under the documented SVG-fixed-viewBox exception — all are regular DOM text.
    - Evidence: styles/typeScale.ts:8-11 (11px floor + SVG-only exception); components/VesselHub.tsx:873,899,928,949 (text-[9px] on <p>/<span>); grep '<text[^>]\*text-\[(9|10)px\]' = 0 SVG matches
- **−0.5** 32 arbitrary-hex Tailwind utilities across 18 files, unchanged since the night audit — off-palette one-off colors (bg-[#1a1d2e] x8, bg-[#0a0e14] x7, chat gradient hexes) that no token or theme rule can reach.
    - Evidence: components/crew-finder/CrewBrowseBoard.tsx:207,211,223,227 (bg-[#1a1d2e]); components/passage/TripOverviewSheet.tsx:238, components/passage/LegPickerDropdown.tsx:736,807, components/vessel/CastOffPanel.tsx:248 (bg-[#0a0e14]); components/chat/ChatComposer.tsx:66 (from/via-[#050a18]); full 32-line locus list reproduced by grep at HEAD

### theming — 3.5/7 (pre-verify)

- **−1** gray-500-on-slate AA contrast failure at scale, unchanged since night: 404 text-gray-500 + 104 text-slate-500 render at ~3.4-3.7:1 on slate-900/950 (AA needs 4.5:1), with 257 lines co-locating text-gray-500 with 9-11px type; the onshore override maps it to stone-500-on-stone-900 at ~3.6:1, faithfully reproducing the failure in both palettes.
    - Evidence: usage counts at HEAD: text-gray-500 x404, text-slate-500 x104; components/CrewManagement.tsx:1959 (text-[10px] text-gray-500 — sub-floor size plus sub-AA contrast); index.css:355-358 (.theme-onshore .text-gray-500 -> rgb(120 113 108) stone-500)
- **−1** display-light still has ~150 occurrences of dark-wash/light-text holes after the 6-hole (53-use) patch: text-white/90 x25, /55 x12, /45 x12, /75 x6, /65 x4, /10 x5; text-slate-200 x21, text-slate-100 x7, text-gray-100 x6; bg-black/70 x27 and /80 x17 uncovered while /60 and /90 are — near-white text survives onto lightened surfaces.
    - Evidence: index.css:422-700 display-light block (grep '.display-light .text-white\\/90|/75|/65|/55|/45|text-slate-200|text-slate-100|text-gray-100|bg-black\\/70|bg-black\\/80' = 0 rules each); concrete break: components/PlanSignOutButton.tsx:55 pairs bg-slate-900/80 (lightened by index.css:459) with text-slate-200 (no override) — unreadable in light mode; components/VesselHub.tsx:1364 text-white/90 on a lightened card
- **−0.75** Onshore escapes after round 2: 273 of 2863 slate/gray utility occurrences (9.5%) have no .theme-onshore rule — placeholder colors (placeholder-gray-500 x30, placeholder-gray-600 x16, placeholder-slate-500 x11) and ~100 gradient stops (to-slate-900/40 x16, from-slate-900 x11, via-slate-950 x7) plus the bg-slate-950 opacity family (~24) stay cool slate on the warm stone theme.
    - Evidence: index.css:190-408 covered set (38 classes, verified present in compiled dist/assets/useFocusTrap-CGo99RjQ.css); diff against app-source usage: 2590 covered / 273 escaped occurrences; top escapes enumerated by class with counts at HEAD 1781864a
- **−0.25** Structural: both theme modes are implemented as literal compiled-class override sheets, so every newly introduced opacity variant or family silently escapes until an audit catches it — the root cause of the recurring hole-patching cycle (round 1: 6 holes; round 2: 8+7 families; this audit: 273 + ~150 still open), and dead rules are already accreting.
    - Evidence: index.css:190-700 (per-class .theme-onshore/.display-light rules keyed on exact compiled selectors like .bg-slate-900\/85); dead rule example: .display-light .text-gray-600 exists (index.css) but text-gray-600 has 0 uses in app source
- **−0.25** The 32 arbitrary-hex utilities escape both theme mechanisms by construction — near-black chips (bg-[#0a0e14], bg-[#1a1d2e]) persist unchanged on onshore stone and on the light display, a visible mode break distinct from the token-hygiene billing.
    - Evidence: components/passage/TripOverviewSheet.tsx:238, components/passage/GalleyCookingMode.tsx:218, components/crew-finder/CrewListConversation.tsx:84,87,211 (bg-[#050a18]); no .theme-onshore or .display-light selector can match an arbitrary-value class (index.css:190-700 covers only stock utilities)
- **−0.25** Night/dusk is an app-level red scrim (rgba(69,10,10,0.25)) plus text-red-600 with a tested z-index ceiling — the ENC chart beneath is not re-paletted to S-52 DUSK/NIGHT color tables, so the chart itself stays day-bright under the filter; billed lightly as chart internals are on the certified ENC track.
    - Evidence: App.tsx:423-427 (night container = bg-slate-950 text-red-600), App.tsx:1323-1330 (fixed red scrim overlay, NIGHT_SCRIM_Z_INDEX); types/units.ts:14 (DisplayMode 'night'); no S-52 palette-table switch in components/map/EncVectorLayer.ts night/dusk hits

### visual-consistency — 5.75/8 (pre-verify)

- **−0.75** Header drift unchanged on three surfaces, two default-path: SettingsModal gradient text-2xl headers, VesselHub bespoke text-lg 'Boat Binder', GalleyPage text-base h1 with a hand-rolled back button — all diverge from the PageHeader standard (text-xl extrabold uppercase) now used by 22 files
    - Evidence: components/SettingsModal.tsx:474 and :654 (text-2xl font-black bg-clip-text bg-gradient-to-r from-white to-sky-300); components/VesselHub.tsx:570 (text-lg font-black tracking-wide); components/vessel/GalleyPage.tsx:253 (text-base font-black h1) + :245-251 (bespoke back-button SVG duplicating components/ui/BackButton); standard at components/ui/PageHeader.tsx:63
- **−0.75** Sub-11px floor unmoved by the label-token fix: 73 hardcoded text-[9px] + 325 text-[10px] across components/pages (10px count grew from 322 at prior-audit commit c25b662e), violating the app's own 8-tier scale where caption=11px is the smallest tier; worst on the default-path anchor-watch hero at 9px/40% opacity
    - Evidence: theme.ts:277 (caption = 11px, smallest documented tier); components/dashboard/hero/EssentialAnchorView.tsx:547-604 (eight text-[9px] spans at text-white/40 and /50); components/map/MapHub.tsx:4301,4726; counts: git grep at c25b662e = 73/322 vs HEAD = 73/325
- **−0.5** EmptyState adoption still incomplete after the 4-site migration — at least six ad-hoc emoji/icon empty blocks remain, exactly the pattern EmptyState's own doc says it replaces
    - Evidence: components/passage/VoyageCleanupSheet.tsx:188-191 (⚓ + grey text); components/chat/ChatAttachmentSheets.tsx:629-631 (🚫 'No voyages to share'); components/chat/CaptainsTable.tsx:956-961 (text-4xl 🧭/🔍); components/passage/WatchAssignSheet.tsx:159 (text-3xl 👥); components/settings/LocationsTab.tsx:25-30; components/DateGroupedTimeline.tsx:94-99; contra components/ui/EmptyState.tsx:4-5 ('Replaces ad-hoc emoji + grey text patterns')
- **−0.25** Icon-language residue: inline emoji leaks onto primary surfaces (VesselHub device row, dashboard hero cards) amid an otherwise coherent all-SVG icon system (helm menu, VesselHub tiles, nav)
    - Evidence: components/VesselHub.tsx:1358 ('⚓ Skipper device'); components/dashboard/HeroSlide.tsx:1170 (🌊 text-lg); components/dashboard/hero/DailySummaryCard.tsx:121 (🌊); contrast with all-SVG components/map/RadialHelmMenu.tsx:203-278 and VesselHub tile icons :577-715

### component-polish — 5.75/7 (pre-verify)

- **−0.5** 44pt sweep stragglers on safety/default surfaces: 24px dismiss and ~26px Accept buttons on the anchor-watch hero suggestion strip, and a 36px close button on ForecastSheet (core weather flow)
    - Evidence: components/dashboard/hero/EssentialAnchorView.tsx:450 (w-6 h-6 dismiss = 24px) and :437-446 (Accept px-2.5 py-1 text-[11px] ≈ 26px tall); components/ForecastSheet.tsx:141-144 (close: p-2 + w-5 h-5 icon = 36px)
- **−0.25** MusicPage primary CTAs at white-on-bg-sky-600 measure 4.10:1 with text-sm (14px) extrabold labels — below WCAG AA 4.5:1 for that size (14px bold is not 'large text'); a real improvement over sky-500's 2.77:1 but still short of the bar
    - Evidence: components/music/MusicPage.tsx:773,1240,1494,1663,1803 (bg-sky-600 text-sm font-extrabold text-white); computed contrast #FFFFFF on #0284c7 = 4.10:1
- **−0.25** components/ui orphan persists: DataFreshness.tsx has zero importers anywhere (the nmea 'DataFreshness' hits are an unrelated NmeaStore type) and is absent from the ui barrel — flagged in the prior audit, still dead at HEAD
    - Evidence: components/ui/DataFreshness.tsx:96 (export const DataFreshness) — grep for ui/DataFreshness imports across components/pages/modules/src/App.tsx returns nothing; components/ui/index.ts has no DataFreshness export; components/nmea/TheGlassPage.tsx:18 imports the type from services/NmeaStore, not the component
- **−0.25** Three overlapping skeleton/loading primitives coexist — SkeletonLoader (1 importer), ui/Skeleton (1 importer), ui/ShimmerBlock (12 importers, the de-facto canonical) — no single loading-state system despite ShimmerBlock's 'replaces bare spinners' charter
    - Evidence: components/SkeletonLoader.tsx (imported only by App.tsx); components/ui/Skeleton.tsx:15 (imported only by components/ChatPage.tsx); components/ui/ShimmerBlock.tsx:1-11 (12 importers)

### Verifier adjustments

- **+0.5** [design-system adherence] Cross-dimension double-bill: the sub-11px floor violation (73x text-[9px] + 325x text-[10px] + 5x text-[10.5px] = 403 occurrences) is charged 0.5 here AND 0.75 in visual-consistency by the other auditor — identical occurrence set, identical root cause. It is also a strict subset of this auditor's own 1.5-point arbitrary-type-sprawl charge (the 2005 text-[Npx] count includes all 403). Keep the visual-consistency 0.75 charge where the user impact (illegible tiny text on the anchor-watch hero) lives; refund this one. Both underlying counts re-verified exactly at HEAD (73/325).
- **+0.25** [theming] Cross-dimension double-bill: the same 32 arbitrary-hex utility occurrences (18 files, re-counted exactly at HEAD: bg-[#1a1d2e] x8, bg-[#0a0e14] x7, etc.) are charged 0.5 under design-system (token hygiene) and 0.25 here (theme escape). One root cause — arbitrary-value classes unreachable by any override sheet. Combined 0.75 for 32 occurrences is also disproportionate next to 0.75 for the 273 onshore escapes. Keep the 0.5 design-system charge, refund the 0.25 theming rider.

**Group verdict:** UI = 18.75/30 at HEAD 1781864a (design-system 3.5/8, theming 3.75/7, visual-consistency 5.75/8, component-polish 5.75/7), down from the night audit's 20.5/30 — the round-2 fixes are real but the two fresh auditors dug past where the night pass stopped, and the deeper holes they found survive verification. Every count re-ran within noise: text-[11px] x1414 / text-[10px] x325 / text-[9px] x73; t.button = 0 consumers vs 1056 raw <button>s; theme.ts imported by 20/346 tsx files, useTheme by 1; tailwind.config.js theme.extend = {} (opened, confirmed empty); typeScale.ts (caption=12/body=13, LABEL_STYLE hardcodes #64748b) contradicts theme.ts (caption=11px) with index.css --text-body:14px as a third voice and its --text-body/title/hero vars at 0 consumers; text-gray-500 x405 + text-slate-500 x102 at a recomputed 3.69:1 on slate-900 (AA needs 4.5), with 257 lines co-locating gray-500 and 9-11px type; all ten claimed display-light hole classes confirmed ABSENT from index.css by escaped-selector fixed-string grep (text-white/90 x25, text-slate-200 x21, bg-black/70 x27, bg-black/80 x17 usages, zero rules), including the concrete PlanSignOutButton.tsx:55 break (bg-slate-900/80 IS lightened at index.css:459, text-slate-200 is not overridden — near-white on near-white in light mode); onshore placeholder-\* (30+16+11) and gradient-stop escapes confirmed rule-less; the dead .display-light .text-gray-600 rule confirmed (0 app usages). Auditor 2's lane verified equally clean: header drift live at SettingsModal.tsx:474/654, VesselHub.tsx:570, GalleyPage.tsx:253 vs 22 PageHeader consumers; 24px dismiss (w-6 h-6) at EssentialAnchorView.tsx:450 and 36px ForecastSheet close (p-2 + w-5 icon) at ForecastSheet.tsx:141; MusicPage sky-600 CTAs recomputed at exactly 4.10:1 with 14px extrabold labels (under AA, genuinely up from sky-500); DataFreshness.tsx has zero component importers (the two nmea hits are a type import from services/NmeaStore); all six ad-hoc emoji empty blocks opened and real; three skeleton primitives coexist (SkeletonLoader→App.tsx only, ui/Skeleton→ChatPage only, ShimmerBlock x13). CI run 30791179481 for HEAD completed SUCCESS, superseding auditor 1's in-progress note (no points were staked on it). Nothing was refuted — every deduction I opened stands on its cited line. The only corrections are two cross-dimension double-bills the group merge exposes: the 403-occurrence sub-11px floor was charged in both design-system (0.5) and visual-consistency (0.75) — and is a subset of the 1.5 sprawl charge besides — so the 0.5 is refunded and the user-impact-framed 0.75 kept; the same 32 arbitrary hexes were charged 0.5 (design-system) + 0.25 (theming), so the 0.25 rider is refunded. Honest picture: the round-2 patches (label token to 12px, 15 onshore families verified into compiled dist CSS lifting coverage to ~90.5%, 4 EmptyState migrations, sky-600 CTAs) all landed and all verified — but the design system remains documentation the app does not execute, both theme modes remain per-compiled-class override sheets that structurally leak every new variant, and the gray-500 AA failure at 500+ sites is untouched since the night audit. The safety-surface trio (RadialHelmMenu, StalenessBanner, GlobalAnchorAlarmGate) is the one area genuinely at the Windy/ORCA-beating bar.
